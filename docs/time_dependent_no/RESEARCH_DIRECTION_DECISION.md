# Research Direction Decision: Frozen Evidence And Line 3 Restart

Updated: 2026-07-30
Status: Accepted normative source

## Authority And Change Protocol

This file is the sole normative source for scientific scope, claim language,
sealed populations, and experiment authorization on branch `time-dependent-no`.
[`HANDOFF.md`](HANDOFF.md) is a derived operational snapshot,
[`MECHANISTIC_DIAGNOSTIC_TRACKER.md`](MECHANISTIC_DIAGNOSTIC_TRACKER.md) is the
evidence ledger, and [`README.md`](README.md) is non-authoritative onboarding and
the sole active-code inventory. If they disagree, this file governs.

A result, artifact, TODO, available GPU, or open evidence gap never authorizes a
run. Changing the claim register or reopening research requires an explicit
human direction decision. Before such a change, bind the proposal to tracker run
IDs and immutable artifacts, separate observation from mechanism and claim,
predeclare the population, intervention, metric field, aggregation, threshold,
direction, seed scope, and cost, and preserve every existing non-claim. Update
derived documents only after this decision is changed.

Private paths, hosts, credentials, and machine-specific dataset locations remain
in ignored local context and are not part of this record.

## Current Verdict And Causal Thesis

The completed 2026-07-23 result-to-claim gate is `partial`, with high
confidence. The intended method claim - that this campaign identifies and
validates a generally improved, shock-stable geometry-aware neural-operator
solver - is not supported. The bounded scientific claim is supported: a serious
residual PCNO is useful on one validated dynamic finite-volume family, and
matched interventions separate several distinct failure mechanisms within that
frozen scope.

The report must use one causal thesis. On this shock-bearing finite-volume
family, the residual PCNO has a useful global map, but its error is not governed
by one removable branch. Propagated shock-supported state error, freshly
generated smooth high-pass error, geometry-conditioned transfer,
discrete-decoder conditioning, and unstable front identity are distinct coupled
obstacles. Do not reduce this to generic instability, optimizer failure, or
Gibbs ringing.

Explicit human direction following mentor review reopened Line 3 on 2026-07-26.
The frozen 2026-07-23 verdict remains the historical evidence boundary; it is
not rewritten as a positive method result. L3R-M0 closed first from local
evidence, and a later explicitly authorized read-only retrieval closed the
checkpoint, command, log, processed-data, and output-payload gaps that were
recoverable. Residual collaborator-training and dirty-source gaps remain
declared.

On 2026-07-27 the human owner explicitly changed the next-stage premise: do not
spend the next serious run reproducing or attributing the historical checkpoint;
instead apply the accumulated training and boundary evidence to one fully
trained bump baseline. L3R-R0 and L3R-A0 are therefore superseded as
prerequisites, not failed experiments. This forfeits any causal claim about the
P/D019-to-R/D041 improvement. The historically authorized L3R-B0 contract
below preserved validation-only selection, exact source/data/optimizer
accounting, family separation, and the sealed-evaluation boundary; B0P later
failed, so B0 was not launched. Later on 2026-07-27 the owner explicitly
authorized a new prospective replacement, L3R-B1P followed conditionally by
L3R-B1, and required the selected model to be at least as good as the exact
R/D041 comparator under the bound validation and post-selection contracts below.
This does not reopen, relabel, or relax the failed B0P row.

Later on 2026-07-27 the owner explicitly authorized a separate zw-gpu
continuation study from exact R/D041 while B1 continues unchanged on AutoDL.
This amendment is L3R-BC0P followed conditionally by paired L3R-BC0. It tests a
complete causal-boundary training/deployment block against a matched extra-
training control; it does not add multistep loss, smoothing, a larger model, or
holdout/sealed selection. BC0P passed source, data, replay, policy, stream, and
finite-backward checks, but failed its frozen peak-reserved-memory ceiling.
The stop rule therefore closed BC0P before causal timing, and no BC0
continuation process started.

On 2026-07-28 the owner explicitly authorized a new, smaller zw-gpu study after
reviewing the boundary-loss mechanism. L3R-BG0P first audits whether a projected
wall/outflow target or a near-boundary normal-node band supplies a finite,
nonconflicting gradient relative to the normal-node objective. Conditional
L3R-BG0 then compares only the passing auxiliaries against a matched causal
normal-node-only continuation from exact R/D041. This is a new contract, not a
retry or reinterpretation of closed BC0P/BC0.

L3R-BG0P subsequently passed and froze both candidate weights. All three matched
BG0 rows completed, but both auxiliary rows failed the preregistered utility
gate: relative to the selected causal control, projected-target and three-hop
band training increased BF16 H79 state error by `3.70%` and `4.13%` and H20
state error by `5.04%` and `2.77%`. Both preserved 30/30 completion and slightly
reduced boundary-node error, but worsened normal-node and deployed near-boundary
error. BG0 is therefore done/closed with neither auxiliary selected.

After reviewing that result, the owner explicitly authorized one distinct
zw-gpu test on 2026-07-28: supervise the model's **raw** boundary proposals
directly against reference next states during training while retaining the same
causal hard closure for every teacher input, deployed proposal, validation
rollout, and recurrent state. L3R-RB0P/RB0 below is not a BG0 coefficient retry:
it changes the primary loss tensor and node population, uses no auxiliary
coefficient, and reuses the completed BG0 causal control rather than launching
another control arm.

RB0P subsequently passed and the one RB0 arm completed. Direct supervision
reduced fixed-pair raw-boundary one-step error by `84.00%` and BF16 H20 state
error by `10.97%`, but increased selected BF16 H79 state and H79 normal-node
errors by `9.30%` and `10.50%`. The selected checkpoint retained 30/30 H79
completion; this is a long-horizon state/front-shape regression rather than an
admissibility blow-up. RB0 is done/closed, and the exact full-weight
`raw_all_nodes` objective is rejected.

After reviewing that result, the owner explicitly authorized L3R-RA0P and one
conditional L3R-RA0 arm on zw-gpu on 2026-07-28. RA0 retains the deployed
normal-node primary objective and adds only a raw-boundary reference auxiliary
whose coefficient is frozen from the exact-parent gradient ratio. It is not an
RB0 retry or coefficient sweep: there is one predeclared coefficient, no new
control training, and no other optimizer, exposure, recurrence, architecture,
population, smoothing, or filtering change.

RA0P subsequently bound the registered source, checkpoint, data, stream,
policy, cohort, and environment and passed 73 focused CPU tests, but its repeated
BF16 raw-boundary/interior gradient ratio was `53.34499581396788`, a relative
difference `0.000648222725830791` from registered
`53.379597682281236`. This exceeds the prospectively frozen `1e-4` tolerance.
RA0P is failed/closed; its smoke and RA0 training were not launched. The near-
equivalent realized gradient fraction does not authorize a post-hoc tolerance
change.

AutoDL L3R-B1 also completed under its unchanged wall guard: 34 of 40 requested
full-coverage passes, 725,220 presentations, and 183,600 optimizer steps. No
checkpoint passed the registered parity conjunction, so no D041 holdout or
sealed population was opened. The retained pre-eligibility epoch-22 checkpoint
is SHA-256
`221d12c3cd5f3df65546bb02537354fce334b481ab36478e7ff9f35f8e4323dc`.
Its normal-node one-step error improves 21.79% over exact D041, but the causal-
closure boundary metric is 10.48 times the raw-D041 value and the resulting
all-node gate fails. B1 is done/closed as a replacement miss, while its
interior/short-horizon gain remains valid descriptive evidence.

## Line 3 Long-K2 Interior Attribution Amendment (2026-07-30)

After reviewing the full-scale attached-recurrence result and the completed
boundary-information closeout, the owner explicitly authorized one
validation-only error-source replay, L3R-K2D0, followed by one matched
projected-teacher control, L3R-K2T0, if the replay identities and provenance
gates pass. This is general interior-PCNO attribution, not boundary-line
re-entry. The boundary closeout remains binding: hard causal closure is the
deployment invariant, tested boundary losses/adapters remain rejected, and no
holdout or sealed population is opened.

The frozen attached result is run `l3r_k2_b1_cont_20260729b`. It starts from
retained B1 epoch 22, SHA-256
`221d12c3cd5f3df65546bb02537354fce334b481ab36478e7ff9f35f8e4323dc`,
and selects epoch 1, SHA-256
`830f81c4ff8cfb4a09b44919201a7dc64f0acd13341712e0c28469fff914e0b9`.
The training-data manifest SHA-256 is
`5d5373fdcc682544bf330fba6d54fe65509dabe936baee7443c71a8c0c8d9fa7`;
the recoverable seven-file training-source digest is
`048d2ce2ed0dfb75fdbe5bf26710f4213ad175f7878206c1a9f67ef622047eaf`.
Its isolated source has no `.git` metadata, so Git identity is `unknown`; the
retained base archive, overlays, launcher, per-file source manifest, and hashes
are the recovery contract. This is weaker than a clean-commit-only identity and
must not be relabeled.

The attached run completed eight full-coverage passes before its wall guard:
`170,640` one-step anchors, `168,480` valid second-step comparisons, `339,120`
learned calls, and `43,200` optimizer steps. On the exact 30-trajectory
validation population, its selected checkpoint improves FP32 H20/H40/H60/H79
state error over the exact B1 parent by
`7.42%/13.70%/21.33%/27.23%`, with `30/30` completion for both. At H79 it also
improves smooth high-pass RMS, front IoU, symmetric Chamfer, and mean shock
thickness/strength error, but H20 thickness and strength worsen and the H79
front-centroid result is case-heterogeneous. The gain is absent at the first
call, begins at call 7, and grows with recurrent depth. This supports a
recurrence-aware mechanism as an inference, not an attribution: extra calls,
extra targets, fresh AdamW, continuation, and validation-H79 selection are
still bundled.

### Authorized K2 attribution rows

| Run ID | Frozen purpose and contract | Status |
| --- | --- | --- |
| L3R-K2D0 | Replay the exact B1 parent and attached-K2 selected checkpoint in FP32 on only their identical 30 validation trajectories and calls 1--79 under checkpoint-native `P_B`. At each call record the exact identity `G(uhat_t)-u_(t+1)=[G(uhat_t)-G(u_t)]+[G(u_t)-u_(t+1)]`, cross energy/cosine, normal/smooth/front support, linear graph-high-pass terms, incoming-error gains, admissibility, and policy/source/data identities. Require all `2*30*79` rows, call-1 zero propagation, relative full/high-pass identity residual at most `1e-6`, exact policy digests, and no smooth-mask fallback. Reconstructed bump weights remain diagnostic proxies. | **DONE / CLOSED 2026-07-30; ALL CONTRACT GATES PASSED** |
| L3R-K2T0 | If every K2D0 contract gate passes, start from the same B1 bytes and run exactly eight projected-teacher K=2 full-coverage passes. Match the attached row's source, 270/30 split, time blocks, targets, normalized coefficients, `170,640` anchors, `168,480` future comparisons, `339,120` calls, `43,200` steps, architecture, seed, fresh AdamW at constant `2e-4`, BF16, effective batch 4, validation exposure, and checkpoint-selection rule. The sole causal difference is that call 2 consumes legal `P_B(U_(t+1))` instead of attached `P_B(G(P_B(U_t)))`. | **20260730B REPAIRED PREFLIGHT STOPPED BEFORE STAGED TESTS; DUPLICATED TRACKER HASH OMITTED TWO HEX CHARACTERS; ZERO TRAINING; NO RELAUNCH AUTHORIZED** |

`P_B` is deliberately retained for both rows. Switching to the later `P_B^*`
would change the learned map and violate the one-variable comparison; B1 is
also not post-hoc compatible with `P_B^*`. K2T0 adds no boundary auxiliary,
noise, smoothing, filtering, clipping, limiter, capacity, split, or new
population. Stop without retry for a parent/source/data/policy/stream mismatch,
nonfinite loss/gradient/state, inadmissibility during registered validation,
OOM, or the 23-hour wall guard.

The primary selector is all-30 FP32 H79 all- and normal-node state error, with
completion and positive density/internal-energy/pressure margins required.
H20/H40/H60 state, one-step defect, the six boundary channels, H20
thickness/strength, and H79 smooth high-pass, front IoU, centroid, Chamfer,
thickness, and strength are mandatory anti-smearing/no-harm fields. Attached
recurrence receives mechanism support only if both H79 state ratios versus
K2T0 are at most `0.90` and no registered H79 structural error ratio exceeds
`1.05` (IoU must be at least `0.95`); this would still be one-seed,
validation-selected evidence. Projected-teacher dense supervision is sufficient
to explain the gain if both of its H79 state ratios versus the attached row are
at most `1.05` with the same structural envelope. A projected-teacher win
rejects the attached-gradient explanation at this scale. Any other conjunction
is mixed/inconclusive. No outcome establishes Jacobian causality, physical
conservation, D041 replacement, repeat-seed robustness, holdout/sealed
performance, or cross-family transfer.

### L3R-K2D0 result and L3R-K2T0 execution failure

**Verified evidence.** Both K2D0 summaries close every prospective gate on the
exact 30 validation trajectories and calls 1--79: `2,370` rows per map,
30/30 completed finite and admissible rollouts, exact checkpoint-native policy
digests, zero call-1 propagation, no test keys or smooth-mask fallback, and
maximum relative vector/high-pass identity residual below `8.86e-16`. Parent
and attached summary SHA-256 values are respectively
`197aafb39fed4cb0895c3f9fc46e1b1e6eeaf76bec08dc829b8d3d0d5846f619`
and
`f937f5c0230be0f2320abbb1c374ac5a89d5381c46f1c6595d6ea9ece96e09d5`;
their row-ledger hashes are
`a8824858f824ddbb2562b03bbbe5a4daa0dec415a3f981f157e25361a77c6e6a`
and
`21e0af438abb0e8c920810a61ab5b77a0b6e82c45f725b6a43df7b0446ef92b3`.
The node measure remains a reconstructed bump proxy, not a physical control
volume.

At H79, attached versus parent normal-node scaled full-state error changes from
`0.160892` to `0.112972` (`-29.78%`), while the propagated term changes from
`0.158322` to `0.110911` (`-29.95%`) and fresh teacher-forced defect changes
only from `0.021732` to `0.021204` (`-2.43%`). Propagated energy contributes
`96.96%/96.58%` of parent/attached H79 normal-state error. The incoming normal
error is already about 30% smaller, whereas the finite-amplitude propagation
gain is essentially unchanged (`0.998322/1.00154` at H79 and
`0.967288/0.967927` averaged over calls 2--79). The attached fresh normal
defect is nevertheless lower at 78/79 call indices, so small systematic source
changes may compound into the better recurrent path.

Bump smooth graph-high-pass is a different mixture. At H79 its total changes
`0.026235 -> 0.024423` (`-6.91%`), its propagated term changes
`0.020580 -> 0.018453` (`-10.34%`), and its fresh defect is unchanged
(`-0.08%`). Across all calls, total and propagated high-pass improve
`8.58%/12.17%`, while fresh high-pass worsens `0.20%` and wins only 24/79
call-index means. H79 propagated/fresh/cross energy fractions are
`63.74%/39.05%/-2.79%` for the parent and
`59.90%/45.04%/-4.93%` for the attached map. This bump evidence therefore does
not inherit the dynamic-family D053b conclusion that smooth high-pass is mostly
fresh. It also does not show that attached K2 cured a one-step ripple source.

Spatially, the H79 attached reduction is `30.18%` in reference-smooth full
state and `29.28%` on front support, but only `6.31%` on boundary nodes.
Across all calls the boundary total is `0.88%` worse and becomes persistently
better only at call 73. This agrees with the frozen boundary-splice result that
the useful change is primarily interior rather than a better boundary proposal.
The independent FP32 evaluator's H79 state/high-pass changes
(`-27.23%/-6.87%`) closely reproduce K2D0's all-node/high-pass changes
(`-26.99%/-6.91%`), while the registered H20 shape and H79 centroid caveats
remain.

L3R-K2T0 then stopped in argument parsing before creating a training directory:
the launcher required frozen trainer SHA-256
`8373671920d98adff35aa0394948d0e633c79616af82ae22fb8f07e7de6bc0b1`,
whose parser implements attached K2 only, and invoked its absent
`--multistep-recurrent-input projected_teacher` option. The failure-log hash is
`934a21d73f6530d44c95df6ea1dae6fcedb85ae8cdcbb213e1c3b24a8282b6f0`.
No optimizer step, sample presentation, checkpoint, or FP32 control evaluation
exists. This is an execution-harness/source-snapshot incompatibility, not a
negative projected-teacher result.

**Plausible mechanism or inference.** The attached checkpoint follows a much
better recurrent error path without a lower normal-state finite-amplitude gain.
Its small, time-consistent teacher-defect reductions and mildly lower high-pass
gain may keep the incoming error smaller, after which essentially the same
normal-state response transmits it. This is consistent with compounding
defect-direction/path shaping, not proof of a more contractive Jacobian.

**Missing evidence.** The full-scale projected-teacher control is absent, so
attached-gradient credit remains confounded with extra target comparisons,
fresh AdamW continuation, and H79 validation selection. The gain ratios use
each checkpoint's own incoming perturbation, not one common Jacobian-vector
probe. Seed confirmation, D041 replacement, holdout/sealed evaluation,
physical bump conservation, and cross-family transfer remain absent.

**Alternative explanation.** Call-matched projected-teacher dense supervision
may equal or outperform the attached row, as it did in the earlier bounded
D041 screen; that screen uses a different parent and scale and cannot answer
the full-scale question. Fresh optimization and checkpoint selection may also
explain part of the gain.

**Claim implication.** L3R-K2D0 closes the fresh/propagated reporting gap and
supports a recurrence-path improvement, but not the stronger statement that
attached gradients are the missing recipe or that the map became globally more
contractive. L3R-K2T0 supplies no method evidence and its registered selector
cannot be evaluated.

**Minimum decisive next experiment.** If this attribution remains worth one
run, a human-reviewed repair must derive an isolated control source from the
exact attached snapshot, apply only the projected-teacher recurrent-input
switch and provenance fields/tests, bind the new source hashes, and run those
tests against the exact staged source before spending the unchanged registered
K2T0 budget. Do not copy the current trainer wholesale or relabel the failed
zero-step process as the control.

**Human-reviewed repair authorization (2026-07-30).** The owner explicitly
authorized one repaired execution after reviewing the zero-step source-staging
failure. The isolated source was reconstructed from the three retained attached
archives and first reproduced attached source-set digest
`048d2ce2ed0dfb75fdbe5bf26710f4213ad175f7878206c1a9f67ef622047eaf`.
Only the frozen trainer and focused multistep test were changed: the repair adds
the attached/projected-teacher recurrent-input switch, call-matched projected
teacher routing, K=1 validation, explicit recurrent-input/gradient/reference-use
provenance, and focused routing/gradient tests. It does not import the maintained
trainer's fixed-horizon or node-type changes.

The repaired trainer SHA-256 is
`87c4418591bb5ce033f027e81cef33336c138f98fff7ad7fce6dab7aaf893641`,
the focused test SHA-256 is
`29c5ac7ce2fbc7e1b3ec9011e76842a7510f2da2d2cbb281975cda7a9d5ee376`,
and the seven-file source-set digest is
`65332ec10e9cc9398503cf18c230e540ca2e5a8a2335a9cfe865c58ef691533c`.
The exact isolated source passes 40/40 focused CPU tests. One AutoDL execution
attempt may proceed only after the same exact staged bytes repeat those tests
and all parent/data/K2D0/environment gates pass. The failed 20260730a process
remains a separate zero-step record; no holdout or sealed population opens.

**Repaired execution outcome (2026-07-30).** The exact staged archive and its
remote SHA-256 matched the reviewed bundle; parent, data, K2D0, environment,
storage, and Bash-syntax checks also passed. Launcher SHA-256
`1321ea6044bc45e145cff1371d6795e9a16ee769a6f12b594fd41c47b8f23d61`
then stopped with exit 125 during source identity preflight, before staged CPU
tests, the GPU queue, or training. The archived and extracted tracker both have
SHA-256
`4855f7f0e6d4e00b9aa59188fbef8c28fbecb84fe8a46c5d604ead677a7234ed`,
but the launcher's duplicated 62-character expected literal was
`4855f7f0e6d4e00b9aa59188fb8c28fbecb84fe8a46c5d604ead677a7234ed`,
omitting `ef`. An exhaustive comparison of all 17 duplicated launcher identity
constants against their authoritative manifest/input values found this as the
only mismatch. There is no staged-test log, training directory, sample
presentation, optimizer step, checkpoint, or post-evaluation from 20260730b.
This supplies harness/provenance evidence only and no K2T0 method result.

A further execution is not automatically authorized. The minimum recovery for
human review is the one-line expected-tracker correction in a new unique attempt,
plus a complete launcher-constant-versus-manifest/input assertion before staging
and a new launcher hash. The scientific source, data, budget, evaluator, and
registered one-variable contract must remain unchanged.

## Four-Line Status

| Line | Status | Frozen conclusion | Non-claim |
| --- | --- | --- | --- |
| 1: large-step flow maps | Closed | Under the frozen 1D contracts, the useful stride depends on horizon and metric: a harder one-call map can win after fewer recurrent compositions. | No universal optimal stride, learned CFL limit, timestep-conditioned solver, ripple cure, or native-grid transfer follows. |
| 2: CPGNet validity and mechanism | Closed | Corrected 1D controls support message reach rather than width alone; interface coordinates act as functional controls rather than verified physical traces. Legal-boundary training helps without closing the oracle-boundary gap. | No paper-faithful reproduction, implicit scheme, physical-interface, conservation, or PCNO-transfer claim follows. |
| 3: geometry-aware 2D rollout | Boundary-information subline closed; K2D0 closed; repaired K2T0 preflight stopped and no retry is authorized | Minimum-change projection remains a deployment result with a thickness caveat. Full-scale attached K2 strongly improves B1 long rollout; K2D0 shows the H79 state gain is almost entirely a smaller propagated term on an already-better incoming-error path, while fresh smooth high-pass is unchanged. The exact repaired scientific source passes local tests, but its launcher duplicated one tracker hash incorrectly and stopped before staged tests or training. | No selected D041 replacement, boundary method, or full-scale projected-teacher result exists. Attached-gradient attribution remains open; Jacobian causality, exact DG replay, conservation, seed robustness, cross-family transfer, holdout, and sealed performance remain unsupported. |
| 4: latent forecasting and assimilation | Stopped before forecast training | Smooth-decoder and fixed-Haar capacity tests isolate a representation-capacity limitation; discontinuous regularity helps but still misses the reconstruction/front hierarchy. | No latent transition, autonomous recurrence, geometry transfer, neural-operator, or data-assimilation claim was tested. |

## Line 3 Serious Bump Baseline Amendment (2026-07-27)

This amendment governs wherever the older restart text below says that
L3R-R0 or L3R-A0 must precede training. Those rows remain historical planning
records and are not reinterpreted as completed evidence.

### Active Claim And Non-Claim

The active question is whether the existing 19,155,720-parameter conservative-
residual PCNO can provide a stronger autonomous bump baseline when trained from
scratch with exact transition coverage, a single uninterrupted annealed
optimizer history, and the same legal boundary closure in teacher forcing,
validation, and recurrence. A successful run can support only a one-seed,
fixed-family baseline result and the engineering value of that complete
contract. It cannot attribute the P/D019-to-R/D041 difference, identify an
architecture improvement, establish physical conservation, or support dynamic,
strength-OOD, test, or general shock-stability claims.

### Authorized Run Contract

| Run ID | Stage | Frozen purpose and gate | Status |
| --- | --- | --- | --- |
| L3R-B0P | Implementation and throughput preflight | Implement and CPU-test the exact L3R-B0 contract, audit every train/validation graph boundary policy, overfit a fixed tiny pair bank, then measure one bounded AutoDL smoke on the same architecture, batch shape, BF16 precision, and boundary closure. No holdout or sealed data. Stop for a source/data digest mismatch, boundary-stencil fallback, future-truth dependency, nonfinite loss/gradient/state, OOM, or projected serious-run wall time above the rule below. | **FAILED / CLOSED 2026-07-27: the fixed tiny bank missed its frozen normal-node error gate; the throughput smoke was not run** |
| L3R-B0 | Serious bump baseline | Train one clean seed on the deterministic 270/30 bump train/validation split and all 79 eligible stride-1 transitions per trajectory. Select only on all 30 validation trajectories; evaluate the already-open 20-case D041 holdout only after selection. | **NOT LAUNCHED; STOPPED BY THE L3R-B0P GATE** |
| L3R-B1P | Replacement readiness and parity preflight | Preserve B0P as failed; test the revised evaluator/selection code, replay exact R/D041 validation metrics, re-audit all 300 boundary policies, and run one representative full-coverage AutoDL pass for health, memory, and throughput. No holdout or sealed data. | **PASSED / CLOSED 2026-07-27; exact replay, boundary, numerical-health, storage, and throughput gates passed** |
| L3R-B1 | Serious replacement baseline | If every B1P gate passes, train one clean seed from scratch under the exhaustive causal-boundary contract and select only from the frozen 30-case validation population with strict historical parity eligibility. | **DONE / CLOSED 2026-07-28; WALL GUARD STOPPED AFTER 34/40 PASSES; NO PARITY-ELIGIBLE CHECKPOINT; NO HOLDOUT/SEALED ACCESS** |
| L3R-BC0P | Exact-parent continuation preflight | On zw-gpu, bind exact R/D041, the complete 300-shard tree, current source, dependency environment, raw replay, all-300 causal policy audit, effective-batch equivalence, real-graph memory, and paired runtime. No holdout or sealed data. | **FAILED / CLOSED 2026-07-27: ALL PRIOR GATES PASSED, BUT THE RAW REAL-GRAPH PREFLIGHT RESERVED 14.493 GIB AGAINST THE FROZEN 14 GIB CEILING; CAUSAL TIMING WAS NOT RUN** |
| L3R-BC0 | Paired D041 boundary continuation | If every BC0P gate passes, run one raw native continuation control and one causal-boundary continuation from identical D041 bytes, sample streams, optimizer budget, and effective batch. | **NOT LAUNCHED; STOPPED BY THE L3R-BC0P MEMORY GATE** |
| L3R-BG0P | Boundary-gradient and memory preflight | On zw-gpu, audit exact-D041 gradients for the normal-node objective, projected wall/outflow target, one-to-three-hop normal bands, raw boundary output, and fixed inflow; then verify microbatch-1/accumulation-4 causal training fits. | **PASSED / CLOSED 2026-07-28; PROJECTED-TARGET AND THREE-HOP BAND WEIGHTS FROZEN, REAL-GRAPH MEMORY AND 67-TEST CPU GATES PASSED** |
| L3R-BG0 | Matched causal boundary-objective screen | Continue exact D041 under causal closure with a normal-node-only control and only the BG0P-passing auxiliary rows. | **DONE / CLOSED 2026-07-28; BOTH AUXILIARIES FAILED THE H20/H79 UTILITY GATE AND NEITHER IS SELECTED** |
| L3R-RB0P | Raw-boundary objective preflight | Prove direct raw-boundary gradients, preserve the dormant default path, bind exact D041/data/stream identities, and verify the causal microbatch-1/accumulation-4 path on zw-gpu. | **PASSED / CLOSED 2026-07-28; 71-TEST CPU GATE AND REAL-GRAPH BF16 MEMORY/FINITE-GRADIENT GATE PASSED** |
| L3R-RB0 | Matched raw all-node continuation | Continue exact D041 for the same ten-epoch BG0 stream, but train the raw pre-closure proposal against reference next states on every valid node; retain causal closure for deployment and compare with the already-completed BG0 causal control. | **DONE / CLOSED 2026-07-28; RAW BOUNDARY AND H20 IMPROVED, BUT H79 STATE/NORMAL AND FRONT/HIGH-PASS GATES FAILED; NOT SELECTED** |
| L3R-RA0P | Gradient-normalized raw-boundary auxiliary preflight | Add one raw-boundary-only reference auxiliary to the unchanged deployed normal-node primary; prove default-path parity, reproduce the exact-parent gradient ratio and frozen coefficient, and pass identity plus real-graph BF16 gates. | **FAILED / CLOSED 2026-07-28; RECOMPUTED RATIO MISSED THE FROZEN RELATIVE TOLERANCE; SMOKE NOT RUN** |
| L3R-RA0 | Matched weak raw-boundary auxiliary continuation | Continue exact D041 for the same ten-epoch stream with `normal_closed + 0.0018733749 * raw_boundary_reference`, retaining causal closure and comparing with the retained BG0 causal control. | **NOT LAUNCHED; STOPPED BY RA0P** |

L3R-B0 keeps the R/D041 backbone exactly: `kmax=8`, Fourier periods `(6,2)`,
widths `[128,128,128,128,128]`, four operator blocks, `fc_dim=128`, normalized
conservative current state, normalized conservative residual, four node-type
channels, one Mach channel, and 19,155,720 trainable parameters. Capacity is not
scaled because the retained run has little train--validation gap and no
capacity-limitation evidence.

The recurrent map is

`Uhat_(t+1) = P_B(G_theta(P_B(Uhat_t)))`.

`P_B` uses only static current-trajectory coordinates, graph edges, node types,
recovered nodal normals, a deterministic at-most-three-hop current-interior
stencil, known Mach/freestream data, and the current proposal. It fixes
supersonic inflow, extrapolates slip-wall and supersonic-outflow primitive
states from current interior nodes, removes wall-normal velocity, and then
returns to conservative coordinates. It uses no future reference, primitive
floor, clipping, limiter, smoothing, or sealed information and is not exact DG
boundary replay. The same closure is applied to teacher-forced inputs, model
outputs, validation, and recurrence. Training loss is the fixed-normalization
proxy-weighted conservative next-state loss on normal nodes only; all-node and
boundary-stratified errors and the raw-to-closed correction remain diagnostics.
No soft boundary, PDE-residual, conservation, high-pass, noise, or multistep
loss is added to this baseline.

Each training epoch is one exact shuffled coverage pass over
`270*79=21,330` unique transitions, with trajectory-homogeneous batch size 4
and `270*ceil(79/4)=5,400` optimizer steps. Use AdamW with peak learning rate
`1e-3`, weight decay `1e-5`, BF16, gradient clipping at `1.0`, a 2% linear
warmup from `0.1x` peak, then cosine decay to `2e-5`. There is one optimizer
history and no continuation or result-dependent early stopping.

The preflight freezes the largest integer pass count

`P in {20,22,24,26,28,30,32,34,36,38,40}`

whose smoke-measured training plus all-validation-rollout projection is at most
22 wall-clock hours. The expanded ceiling records the owner's 2026-07-27
authorization to scale the epoch count when throughput permits; it is still one
run, not an epoch sweep. This rule uses throughput and memory only, never loss
or validation quality. Before serious launch, record `P`, `21,330*P`
presentations, `5,400*P` optimizer steps, the literal command, source/data/
boundary/config digests, environment, and projected runtime. The hard job cap
is one RTX 5090 and 23 wall-clock hours; retained training artifacts are capped
at 1 GiB.

Every pass evaluates all 30 validation trajectories at H20/H40/H60/H79 under
the same closure. Checkpoint eligibility first requires 30/30 H79 completion.
Eligible checkpoints are ranked by mean all-trajectory H79 scaled proxy
relative L2, then H20 error, then one-step validation error. Before any eligible
checkpoint exists, completion and mean survival precede admissible-prefix error.
The first excluded proposal is never included in an error curve. Raw density,
internal-energy, pressure and outward-outflow-Mach margins, boundary correction,
front position/strength/thickness, downstream signed oscillation, smooth graph
high-pass, throughput, gradient norm, learning rate, and peak memory must be
retained. Bump weights remain diagnostic proxies, not physical volumes.

After selection, the already-open 20-case D041 population is a post-selection
audit only. Report H20/H79 completion and survival, state and primitive errors,
front and anti-smearing metrics, boundary strata, and same-process
teacher/propagated/cross terms. Do not open a new or sealed split. A later
filtering or smoothing diagnostic is not bundled into L3R-B1 and requires review
after the unfiltered baseline is frozen.

L3R-MR0 and the dynamic family remain separate and deferred pending review of
L3R-B1. L3R-MR1 remains blocked on a future MR0 pass, L3R-L0 remains
mechanism-conditional, and L3R-F0 remains sealed.

### L3R-B1 closeout

The 23-hour guard stopped B1 cleanly before epoch 34, after 34 complete passes,
725,220 presentations, and 183,600 optimizer steps. Training summary and metrics
SHA-256 are
`0c76dbfa66fa9b2b66f0fe8acad24a715aaaee6305422a5d4e3247e005729b5e`
and
`994cd2e175d07fd79e73e289bca275ac6457cac0a134866f3d38331d87b8f77e`.
No epoch passes the full eligibility conjunction. Epochs 25--33 pass the five-
key H20 threshold, but none completes 30/30 H79, and no epoch passes the fixed-
128 all-node one-step threshold.

The retained pre-eligibility epoch-22 checkpoint has 30/30 H79 completion,
five-key H20 `0.02434854879975319`, fixed-128 all/normal/boundary one-step
`0.010221920889307512/0.004201662173727527/0.07026912062428892`, and all-30
H20/H40/H60/H79 state error
`0.02196161032964786/0.02082274357477824/0.028162645796934765/
0.03320156975338857`. Exact D041 replay is `0.005402316681283992` all-node,
`0.005372487043132423` normal-node, and `0.006707466100124293` boundary-node on
the fixed pairs. B1 therefore improves the normal-node map 21.79%, while the
non-identical causal-boundary contract drives an all-node parity miss. These
regional means are descriptive; the evaluator's mean-of-relative-norms does not
permit an exact numerator decomposition after the fact.

Against retained BG0 causal control, B1 improves all-30 H20 21.71% and normal
one-step 20.91%, but worsens H79 state/normal 0.78%/0.96%. An independent exact-
source FP32 validation-only audit completes 30/30 and improves H20/H40/H60
state, H79 smooth high-pass, front IoU, Chamfer, and front-centroid distance;
by H79 state is 0.41% worse and shock-thickness/strength log errors are
38.21%/4.49% worse. This selects neither generic smoothing nor architecture
scaling. It indicates separate fresh-interior, boundary-contract, front-shape,
and recurrent-propagation bottlenecks.

Across all checkpoint evaluations, every one of 80 terminations is local
nonpositive internal energy; no NaN or other cause is recorded. Epoch 22 has no
termination. The completed miss authorizes no continuation, threshold change,
holdout/sealed access, second seed, smoothing, or new training row. The ignored
evidence bundle is
`artifacts/time_dependent_no/l3r_b1_serious_20260728a/`.

### L3R-B0P closeout

L3R-B0P stopped before the full-data throughput smoke and before L3R-B0. The
implementation and numerical-health checks passed: the focused residual suite
reported 18 passing tests, the related CPG suites reported 38 passing tests,
the isolated AutoDL clone used the exact seven-file source snapshot with source-
set digest
`30fd6fa3a0c9bddefd4a98cf33ad1b8d8063544c02f050c9dacf40ada46b808f`,
and the 300-trajectory graph audit used policy-set digest
`c7d9f92dadeaef46bcb0c62c0f26458f08b7bfc6d6ec4e124aa840e9a246c13c`
with zero fallback targets. The reference endpoint outflow-normal Mach minimum
was `1.56672287`. The causal closure's reference-boundary primitive RMS
correction was `[0.134581,0.090080,0.022456,0.148901]` for
`[rho,u,v,p]`, with maximum absolute correction
`[2.711418,1.118094,0.691179,7.753138]`; these values reinforce that it is a
legal diagnostic closure, not exact DG boundary replay.

Artifact `b0p_20260727a_tiny`, the first eight-epoch, 1,024-update fixed-bank
run, reached best normal-node
relative L2 `0.00448034` and loss ratio `0.0110737`. Because its cosine schedule
ended with the short run, one fresh 20-epoch schedule was declared before
execution; it completed all 2,560 updates with best/final normal-node relative
L2 `0.00380627` and loss ratio `0.00798375`. The frozen gates were `0.0015` and
`0.05`, respectively, so the loss-ratio gate passed and the absolute-error gate
failed. Parameters and gradients remained finite, clipping never activated,
maximum gradient norm was `0.719972`, mean throughput was `2.37569` optimizer
steps/s, and peak allocated GPU memory was `7,653,307,904` bytes. A
same-checkpoint replay gave normal-node relative L2 `0.00380651` in BF16 and
`0.00382364` in FP32; BF16 precision is therefore not a supported explanation
for the miss.

Artifact `b0p_20260727b_tiny`, the final run, binds data-manifest digest
`5d5373fdcc682544bf330fba6d54fe65509dabe936baee7443c71a8c0c8d9fa7`,
configuration digest
`9b527796cf4c774501086df6918ed986f49b6658492141f7e3f9bdf067d84d73`,
summary SHA-256
`8bb1280d915ef82d5e3ca13f535f751c9f09b5296108341b76e657423a06b2ab`,
and last-checkpoint SHA-256
`66cc57878aa3c35557448b7012d024ecb5070f984163c003004ac7e45b3848f0`.
No full-coverage epoch, pass-count projection, serious checkpoint, holdout
evaluation, smoothing result, or capacity conclusion exists. Plausible
remaining mechanisms are an optimization or representation floor for this
four-pair discontinuous target, the mismatch between the proxy loss-ratio and
normal-node relative-error fields, or an unusually hard fixed bank. The result
does not distinguish them. Per the registered execution gate, no threshold
relaxation, longer tiny retry, throughput smoke, or L3R-B0 launch is authorized
without a new human-reviewed prospective contract. Both retained preflight
artifacts are 440 MiB, total 880 MiB, and no PCNO training process remains.

### L3R-B1P And L3R-B1 Prospective Replacement Contract

Status: **L3R-B1P passed and is closed; the one 40-pass L3R-B1 request is
running under the frozen 23-hour guard below**. This is the new human-reviewed contract required by the
B0P closeout. B0P remains a failed, closed experiment: its `0.0015` fixed-bank
normal-node gate is neither relaxed nor retroactively reinterpreted. The new
premise is that overfitting four discontinuous pairs is not a necessary
condition for a useful exhaustive-data model, especially when the B0P proxy-loss
ratio passed while its differently aggregated normal-node relative error did
not. The owner explicitly authorized the replacement and requires a
noninferiority sanity check against the last strong checkpoint.

**Exact comparator and metric identity.** The comparator is R/D041 continuation
epoch 4, checkpoint SHA-256
`2bb5ee3ca831a6ffc498f01e309ae7ea957b2df0f411023fb3812919a6732964`,
parent SHA-256
`4fd5f5688e8ac1c8746e29e82c72000b8a2bf1c7eb49416f476ba692f0c6dbfc`,
19,155,720 parameters, raw conservative `model_all_nodes` recurrence, bump data
manifest SHA-256
`5d5373fdcc682544bf330fba6d54fe65509dabe936baee7443c71a8c0c8d9fa7`,
seed and split seed `20260718`, and the recovered 270/30 split. Its exact H20
validation cohort is keys `16,60,128,141,235`; 5/5 complete with mean final
scaled proxy relative L2 `0.024284941703081132`. Its one-step comparator is
all-node scaled proxy relative L2 `0.005402445773142972` on the deterministic
128-transition validation sample produced by the historical committed sampler
at seed `20260718+991`. The recovered dirty training-source delta remains
unavailable; exact checkpoint and sidecar identity do not repair that gap.

Before training, the current evaluator must replay that exact checkpoint on the
same manifest and populations. The identity gate requires the exact hash,
5/5 H20 completion, absolute H20-mean difference at most `1.25e-4`, and absolute
BF16 one-step difference at most `5e-5`. These tolerances test evaluator and
environment parity only; they are not the B1 scientific thresholds. The source,
evaluator, data, split, literal validation-pair list, normalization, environment,
and output digests must be retained. R/D041 uses its native raw recurrence;
B1 uses its native causal closure. Their comparison is therefore outcome-level
noninferiority under declared deployment contracts, not a controlled boundary
ablation or historical-gain attribution.

**B1P gates.** The local focused residual suite and adjacent mesh/legal-boundary
suites must pass; the revised suites currently report 20 and 43 passes. All 300
train/validation graph policies must reproduce policy-set digest
`c7d9f92dadeaef46bcb0c62c0f26458f08b7bfc6d6ec4e124aa840e9a246c13c`,
zero fallback targets, no future-reference dependency, and outward-supersonic
reference outflow. An isolated AutoDL source copy must then complete at least one
full shuffled coverage pass over all `270*79=21,330` transitions with the exact
19.16M architecture, trajectory-homogeneous batch size 4, BF16, hard causal
closure, and all-30 H20/H40/H60/H79 validation attempt. Stop for a source/data/
configuration digest mismatch, fallback, nonfinite loss/gradient/parameter,
nonfinite accepted state, OOM, or unavailable full-pass timing. The smoke is a
health and throughput measurement, not a tiny-fit replacement or a scientific
checkpoint.

Let `t_smoke` be the measured complete-pass wall time including all-30
validation. Freeze

`P = max {p in {20,22,24,26,28,30,32,34,36,38,40}: 1.10*p*t_smoke <= 22 h}`.

If the set is empty, B1 is not launched. Otherwise record `P`, `21,330*P` sample
presentations, `5,400*P` optimizer steps, the literal command, source/data/
boundary/configuration digests, and projected runtime before launch. B1 uses one
RTX 5090, a 23-hour hard wall cap, and at most 1 GiB of retained training
artifacts.

**B1P closeout and B1 pass-count freeze.** The exact R/D041 replay passed:
the five-key H20 mean was `0.024169041690760524`, an absolute difference
`0.00011590001232060845` within the registered `1.25e-4` identity tolerance,
and the deterministic 128-pair all-node one-step value was
`0.005402316681283992`, an absolute difference
`1.290918589802459e-7` within `5e-5`. All five H20 trajectories completed.
The all-300 boundary audit reproduced policy-set digest
`c7d9f92dadeaef46bcb0c62c0f26458f08b7bfc6d6ec4e124aa840e9a246c13c`,
zero fallback, no future-reference values, and outward-supersonic reference
outflow. Preflight summary SHA-256 is
`54e366dd4329c08c2d6cf99d4b80cc36afc7ebb8a03be8f0d20bb4c05d2dbcad`.

The isolated RTX 5090 smoke then completed exactly one full pass: 21,330
presentations and 5,400 optimizer steps. The pass, including the fixed 128-pair
validation and all-30 H20/H40/H60/H79 rollout attempt, took
`t_smoke=625.5151684433222 s`; training alone took `598.8660355564207 s`.
Parameters and recorded accepted states remained finite, gradient norm
mean/max were `0.0183937/0.285222`, clipping fraction was zero, peak allocated
GPU memory was `14,611,556,864` bytes, and retained artifacts used
`460,953,239` bytes. Fourteen early-model rollouts stopped on local
nonpositive internal energy and 16 completed H79; this is a health/throughput
smoke, not a checkpoint-quality result. Summary SHA-256 is
`b66612249ffcd0a960291e9ba857ed846bbce492e64621c2474d07d3ac0976ab`;
the smoke binds source-set digest
`e84fab761a5d4135ed00a2c9ad585692401c6237312edae8d50fbb9cc68eab18`,
data-manifest digest
`5d5373fdcc682544bf330fba6d54fe65509dabe936baee7443c71a8c0c8d9fa7`,
and smoke-configuration digest
`a29e662e999b13741919b1d98afe4b58909136360cb93c72af738b3ded8a2659`.

The frozen formula selects `P=40`. B1 therefore requests `853,200` transition
presentations and `216,000` optimizer steps. Its direct projection is
`6.95017 h`; the registered `1.10x` projection is `7.64519 h`, below 22 hours.
Before process start, the exact machine-specific command and fresh launch-time
source/data/boundary/configuration digests were written to the private B1
artifact. Machine-specific paths are not copied into this committed decision
file.

**B1 launch and epoch-0 operational audit.** The process started on 2026-07-27
only after private prelaunch-contract SHA-256
`495163a89bd484d99d57d1ce6ef026130db944836604220556ca041f37c98269`
bound source-set digest
`61ea3d2c650a0d5d67b2020c4a97a08dc5ffdfb55498da0a3b26129437c5b730`,
configuration digest
`191748e7c1d08e7f59ef4c6882ca2565ca47efff27192b8cc7a75c1d412d2936`,
the unchanged data and boundary digests, the exact command, 40 passes, 853,200
presentations, 216,000 steps, and an empty test-key list. The live run contract,
source snapshot, split, scheduler, environment, and boundary fields all matched
that record before epoch 0.

Epoch 0 is a health observation, not a selected result. It completed in
`2419.311 s`, of which `2290.435 s` was training at `2.35763` optimizer steps/s.
Parameters were finite, gradient mean/max were `0.0184022/0.250271`, clipping
fraction was zero, validation proposals were all admissible, peak allocated GPU
memory was `14,611,556,864` bytes, and retained artifacts remained below 1 GiB.
Validation H79 completion was 25/30 with mean survival `0.975527`; five-key H20
was `0.0977281` and fixed 128-pair all-node one-step was `0.0133078`, so the
checkpoint is not parity-eligible and D041 remains unopened.

The epoch was `3.86771x` slower than the smoke and optimization throughput was
`0.261464x` the smoke value. At a constant epoch-0 rate, 40 passes would take
`26.8976 h` before the registered margin, while the unmodified 23-hour guard
would stop between complete epochs after approximately 34 passes. This changes
neither the requested `P=40` nor any scientific threshold: the frozen process
continues and the guard, not an outcome-dependent intervention, determines the
completed-pass count. No affinity, worker, batch, schedule, checkpoint, or
selection change is authorized mid-run.

**B1 intervention and controls.** Keep the B0 architecture and scientific
contract: normalized conservative-state residual, node-type/Mach conditioning,
`kmax=8`, periods `(6,2)`, widths `[128]*5`, `fc_dim=128`, seed/split seed
`20260718`, stride 1, exhaustive transition coverage, AdamW `1e-3`, weight decay
`1e-5`, BF16, gradient clip `1.0`, 2% warmup from `0.1x`, and cosine decay to
`2e-5` in one uninterrupted from-scratch optimizer history. Apply
`P_B(G_theta(P_B(U)))` to teacher inputs, proposals, validation, and recurrence;
optimize normal-node proxy-weighted fixed-scale state loss. Add no soft boundary,
noise, smoothing, filtering, limiter, floor, PDE/conservation, high-pass, or
multistep term, and do not scale the model.

**Checkpoint eligibility and ranking.** Every completed pass evaluates the
literal fixed 128-transition validation sample and all 30 validation trajectories
at H20/H40/H60/H79. A checkpoint is parity-eligible only if all 30 complete H79,
the exact five-key cohort completes H20 with mean all-node scaled proxy relative
L2 no greater than `0.024284941703081132`, and the fixed 128-transition all-node
one-step metric is no greater than `0.005402445773142972`. Among eligible
checkpoints rank mean all-30 H79 error, then all-30 H20 error, then fixed all-node
one-step error, then the normal-node objective metric. Before eligibility,
completion and survival precede admissible-prefix error. The five keys are all
members of the already-open validation split and are reused prospectively only
because the owner required historical noninferiority; this supersedes older B0
language reserving them for post-selection use, but does not open D041 or any
new/sealed population.

Only after an eligible checkpoint is frozen may the already-open 20-case D041
holdout be evaluated. The outcome-level noninferiority audit requires 20/20 H20
completion, mean H20 reconstructed-weight proxy L2 no greater than
`0.0222573616825449`, H79 completion at least 17/20, and mean H79 survival at
least `0.9056962025316455`; also report one-step error against
`0.006662991680788455`, complete-case H79 error against `0.04366830388060171`,
front strength/position/thickness, smooth high-pass, boundary strata, and
admissibility margins without silently turning differently populated H79 errors
into a gate. A miss makes B1 a completed but noninferior-failed one-seed run. It
does not authorize a retry, threshold change, continuation, alternate seed,
larger model, soft constraint, smoothing/filter, dynamic transfer, or sealed
access. A pass supports only one clean fixed-family baseline under this exact
training/deployment contract.

### L3R-BC0P/BC0 Exact-D041 Boundary-Continuation Contract

Status: **BC0P failed / closed at the reserved-memory gate; BC0 not launched**.
This amendment follows the owner's explicit request to use the idle
zw-gpu concurrently with, but independently of, the unchanged AutoDL B1
process. No BC0 observation may stop, restart, retune, or select B1.

**Question and evidence boundary.** Exact R/D041 is a strong, selected
19,155,720-parameter parent. A frozen inference-only causal projection improved
survival but harmed state/front accuracy, so a plausible mechanism is
checkpoint-to-boundary-contract mismatch: the model never adapted to the map
used at deployment. Extra low-rate optimization alone is an alternative
explanation. The minimum decisive experiment is therefore one paired
continuation, not a multistep/smoothing bundle or a factorial sweep. Because
the raw arm predicts and scores every node while the causal arm deterministically
closes boundary nodes and scores normal nodes, BC0 tests the complete native
boundary training/deployment contract. It is not a projection-only or loss-mask-
only ablation. The frozen D041 inference-only counterfactual and the raw
continuation control are required interpretive anchors.

**Immutable parent and data identity.** Both arms must initialize model weights
strictly from R/D041 epoch-4 checkpoint SHA-256
`2bb5ee3ca831a6ffc498f01e309ae7ea957b2df0f411023fb3812919a6732964`,
whose recorded parent is
`4fd5f5688e8ac1c8746e29e82c72000b8a2bf1c7eb49416f476ba692f0c6dbfc`.
They must use manifest SHA-256
`5d5373fdcc682544bf330fba6d54fe65509dabe936baee7443c71a8c0c8d9fa7`,
the checkpoint's literal recovered 270/30 keys and normalization, seed and
split seed `20260718`, stride 1, `kmax=8`, periods `(6,2)`, widths `[128]*5`,
and `fc_dim=128`. Missing shards may not be replaced by a mini-manifest or
reconstructed identity. The exact retrieval request is: the complete prepared
300-trajectory shard tree corresponding to every entry of that manifest,
including each entry's declared folder, state digest, geometry digest, and all
required graph arrays. Equivalently, the declared 36,636,903,837-byte source
HDF5 may be retrieved only together with sufficient identity evidence to
regenerate and reproduce the exact manifest and every per-entry digest.

**BC0P gates.** Before training, all of the following must pass:

1. the focused residual-PCNO plus mesh/legal-boundary CPU suites, syntax, and
   strict-JSON/diff checks under the reviewed source;
2. exact checkpoint, manifest, split, normalization, parameter, architecture,
   and all-300 shard/digest closure;
3. same-host raw D041 replay with 5/5 H20 completion, H20 mean within
   `1.25e-4` absolute of `0.024284941703081132`, and the literal 128-pair
   one-step metric within `5e-5` of `0.005402445773142972`;
4. all 300 causal policies reproduce zero fallback, no future-reference value,
   outward-supersonic reference outflow, and policy-set digest
   `c7d9f92dadeaef46bcb0c62c0f26458f08b7bfc6d6ec4e124aa840e9a246c13c`;
5. one representative real-graph BF16 backward smoke for microbatch 2 and two
   accumulation steps, with effective batch 4, finite loss/gradient/parameters,
   no OOM, and peak reserved memory at most 14 GiB; and
6. measured raw and causal epoch projections satisfying
   `1.10*(10*t_raw + 10*t_causal) <= 22 h`, no competing zw-gpu compute
   process, and projected retained output at most 2 GiB.

Any failure stops BC0 without changing a tolerance, environment, batch,
checkpoint, dataset, or arm count. Environment differences from the surviving
D041 candidate environment are recorded, not hidden; both arms must use one
identical zw-gpu interpreter and package set.

**Paired intervention and exposure.** Both arms start fresh AdamW and scheduler
state from the exact parent weights. They use constant learning rate `2e-4`,
weight decay `1e-5`, BF16, gradient clip `1.0`, ten epochs, and exactly `2,048`
balanced presentations per epoch: `20,480` presentations per arm. The literal
sample stream and trajectory-homogeneous optimizer groups are identical.
Microbatch 2 with two accumulation steps preserves effective batch 4 and the
historical `540` optimizer steps per epoch. The raw control retains native
`model_all_nodes`, all-node state loss, and raw recurrence. The causal arm uses
the explicit fresh-initialization transition, does not inherit optimizer or
scheduler state, and applies `P_B(G_theta(P_B(U)))` with normal-node loss in
teacher forcing, validation, and recurrence. Neither arm adds soft boundary
penalty, generated-state exposure, multistep/BPTT loss, input noise, smoothing,
filtering, clipping, floor, limiter, PDE/conservation loss, or capacity.

**Selection and decisive comparisons.** Every epoch uses the same literal
128-pair validation sample and all 30 validation trajectories at
H20/H40/H60/H79. Each arm is ranked under its declared native deployment
contract with the existing parity-eligibility hierarchy. The fixed final
20,480-presentation checkpoints are additionally compared under one common
causal deployment: causal-native versus the raw final checkpoint with the
already registered causal counterfactual. This fixed-exposure comparison, not
a favorable epoch pair, tests adaptation to the boundary contract. Report
completion, survival, all-/normal-/boundary state error, density/internal-
energy/pressure margins, front position/strength/thickness, smooth high-pass,
and boundary corrections. Evidence for useful adaptation requires no loss of
completion, lower H79 state error or a strictly better survival/margin outcome
under the common causal deployment, and no more than 2% H20 state regression or
5% front-strength/thickness regression. A checkpoint is called a serious D041
replacement only if it also passes the frozen five-key H20 and 128-pair
one-step parity thresholds and completes 30/30 validation H79. A miss remains a
completed diagnostic, not permission for multistep loss, smoothing, a third
arm, more epochs, or holdout-driven rescue. The 20-case D041 holdout and every
sealed population remain closed pending later human review.

**BC0P closeout evidence.** The reviewed 19-file source snapshot has SHA-256
`14c80f64b7132f72526da6fb74d6b617d218e3e65cd7700e7cbbe4df62b52a67`.
The exact D041 checkpoint and manifest retain the hashes above. The complete
300-shard tree was transferred and audited: all 300 state digests and all 300
geometry digests pass after applying the manifest-declared pre-save dtype
contract. The selected common environment is Python `3.12.3`, PyTorch
`2.7.0+cu128`, CUDA `12.8`, cuDNN `90701`, NumPy `2.1.2`, and SciPy `1.15.3`.

The current-source five-key raw H20 replay completes 5/5 at
`0.024173890981444107`, an absolute difference of
`0.00011105072163702587` from the registered comparator and within the
`1.25e-4` gate. The literal 128-pair BF16 microbatch-2 one-step metric is
`0.005401879385317443`, an absolute difference of
`5.663878255290911e-7` and within the `5e-5` gate; every proposal is
admissible. All 300 causal policies have zero fallback, no future truth,
outward-supersonic reference outflow, and exact policy-set digest
`c7d9f92dadeaef46bcb0c62c0f26458f08b7bfc6d6ec4e124aa840e9a246c13c`.
The frozen ten-epoch paired stream contains exactly 2,048 presentations and
540 effective-batch-4 optimizer groups per epoch. Its declared logical
pair-sequence digest is
`bf5f0e1915b53bd296192c80ceafaca05d46c858cda1414e883472f7a5236456`;
the retained JSON file SHA-256 is
`97140442b237af7381bf90da7771554825c8256aa304358dec7e1a7404590ffe`.

The representative raw BF16 preflight epoch completed finite, without OOM, in
`241.6534160003066 s` for 2,048 presentations and 540 optimizer steps. It
recorded training relative L2 `0.005194369841888147`, all-node validation
relative L2 `0.005375912152885576`, and H79 completion 29/30. Peak allocated
memory was `7,656,668,160` bytes, but peak reserved memory was
`15,562,964,992` bytes (`14.493 GiB`), exceeding the registered 14 GiB
(`15,032,385,536` byte) ceiling by `530,579,456` bytes. The preflight summary
has SHA-256
`a62ffa59dcd20aef5677acf4dd1d73e3602d2dbb54028fc329a393b651d1beed`.
Gate 5 therefore failed. Per the frozen stop rule, causal timing, the paired
runtime projection, and both continuation arms were not run.

This outcome verifies source/data/replay/policy compatibility and shows that
the registered raw microbatch-2 path is finite, but it is only engineering
preflight evidence. It cannot support or refute causal-boundary adaptation,
extra-optimization benefit, D041 replacement, or multistep loss. Allocator
reservation rather than live allocation is a plausible explanation for the
ceiling miss, but the registered quantity was peak reserved memory, so that
does not change the result. Any allocator setting, memory ceiling, batch shape,
environment, host, or arm-count change requires a new human-reviewed contract;
none is authorized automatically by this failure.

### L3R-BG0P/BG0 Boundary-Gradient Study Contract (2026-07-28)

**Question.** Hard boundary closure removes explicit deployed boundary
violations but does not by itself teach the interior sources used by wall and
outflow extrapolation. The prospective question is whether a legal auxiliary
can improve those learned interior dynamics without spending capacity on raw
boundary outputs that recurrence overwrites. The two candidate signals are
kept distinct: (1) projected-target wall/outflow loss compares
`P_B(G_theta(P_B(U_t)))` with `P_B(U_(t+1))` only on nonfixed boundary targets;
(2) near-boundary loss upweights the ordinary next-state target only on normal
nodes within three graph hops of any boundary. Fixed inflow is excluded from
the projected auxiliary because its closed value has identically zero model
gradient. Neither signal changes the recurrent state, clips, limits, smooths,
or uses future reference values at inference.

**Immutable identity and population.** Every row starts from R/D041 checkpoint
SHA-256 `2bb5ee3ca831a6ffc498f01e309ae7ea957b2df0f411023fb3812919a6732964`
and manifest SHA-256
`5d5373fdcc682544bf330fba6d54fe65509dabe936baee7443c71a8c0c8d9fa7`.
Use the checkpoint's recovered 270/30 split, normalization, architecture, seed,
stride, and the already-bound BC0P ten-epoch logical sample-stream digest
`bf5f0e1915b53bd296192c80ceafaca05d46c858cda1414e883472f7a5236456`.
Training and selection use train/validation only. The D041 20-case holdout and
all new or sealed populations remain closed.

**BG0P gradient gate.** First pass syntax, strict JSON/diff, the complete
residual-PCNO plus mesh/legal-boundary CPU suites, source/data/checkpoint/stream
hashes, zero policy fallback, no future truth, an idle GPU, and sufficient
storage. The gradient audit takes the first four stream pairs in each fixed
time stratum `0--25`, `26--52`, and `53--78`. It averages parameter gradients
for the full 12-pair cohort and each stratum. A candidate passes only if all
values are finite, the full-cohort cosine with the normal-node gradient is
nonnegative, every stratum cosine is at least `-0.1`, the gradient norm is
nonzero, and the scalar needed for a `0.10` auxiliary-to-primary gradient-norm
ratio is in `(0,10]`. That scalar is then frozen; no loss or rollout result may
retune it. Raw-boundary and fixed-inflow gradients are diagnostics only. Before
training, one real-graph BF16 microbatch-1/accumulation-4 causal backward must
finish without OOM or a nonfinite value and with peak allocated memory below
the physical device capacity. This explicitly replaces BC0P's old 14 GiB
reserved-memory rule for BG0 only; it does not retroactively pass BC0P.

**Matched training rows.** If at least one auxiliary passes, run one causal
normal-node-only control plus each passing candidate, at most three rows total,
sequentially on the single zw-gpu device. All rows load only the exact parent
weights and initialize fresh AdamW/scheduler state. Freeze constant learning
rate `2e-4`, weight decay `1e-5`, BF16, gradient clip `1.0`, ten epochs, exactly
`2,048` presentations and `540` optimizer steps per epoch, microbatch `1`, four
accumulation steps, effective batch `4`, and the identical literal stream.
Every row uses `P_B(G_theta(P_B(U)))` for teacher input, proposal, validation,
and recurrence. Add no generated-state exposure, multistep/BPTT loss, noise,
smoothing, filtering, floor, limiter, PDE/conservation term, capacity change,
or alternate seed. Hard caps are eight zw-gpu hours and 2 GiB retained output.

**Selection, evidence, and stop.** Every epoch evaluates the same 128 validation
pairs and all 30 validation trajectories at H20/H40/H60/H79. Use the existing
completion/survival/H79/H20/one-step hierarchy and historical parity thresholds;
never select on training auxiliary loss. After each row freezes its
validation-selected checkpoint, compare completion, survival, all-/normal-/
boundary state error, admissibility margins, boundary correction, front
position/strength/thickness, smooth high-pass, and one-to-three-hop interior
bands on validation only. Evidence for a useful auxiliary requires no loss of
completion, lower all-30 H79 state error than the matched control, at most 2%
H20 regression, and at most 5% front-strength or thickness regression. Calling
a row a D041 replacement additionally requires 30/30 H79 completion and both
frozen validation parity thresholds. Stop for any identity/policy mismatch,
nonfinite value, OOM, failed gradient gate, missing matched control, wall cap,
or output cap. A successful result supports only one-seed, fixed-family evidence
that the selected auxiliary helps this causal continuation. It cannot establish
a general boundary method, explain the historical checkpoint gain, validate
physical conservation, or justify holdout/sealed access.

### L3R-BG0P/BG0 Result (2026-07-28)

**Verified identity and execution.** The parent checkpoint SHA-256 is
`2bb5ee3ca831a6ffc498f01e309ae7ea957b2df0f411023fb3812919a6732964`;
the data-manifest and logical ten-epoch pair-stream digests are
`5d5373fdcc682544bf330fba6d54fe65509dabe936baee7443c71a8c0c8d9fa7`
and
`bf5f0e1915b53bd296192c80ceafaca05d46c858cda1414e883472f7a5236456`.
The retained stream JSON SHA-256 is
`97140442b237af7381bf90da7771554825c8256aa304358dec7e1a7404590ffe`.
The source-snapshot digest is
`9fec7e423d521c2ff24984266602c4c5d6cab66fd4924eb351100f0662e3e942`.
Deep comparison of the run contracts found only the registered auxiliary,
weight, output-directory, and boundary-objective identity differences. Every
row completed 10 epochs, 20,480 presentations, and 5,400 optimizer steps with
the same literal stream and fresh optimizer state. All selected epoch 5. No
holdout or sealed population was read.

BG0P passed projected target at full/minimum-stratum gradient cosines
`0.320644/0.048298` and frozen weight `0.0099245903`; the three-hop band passed
at `0.056853/0.159458` and weight `0.014404898`. Fixed-inflow gradient was
exactly zero. The real-graph preflight peaked at 2,059,098,624 allocated bytes,
and the complete CPU gate reported `67 passed`.

**Frozen-selection evidence.**

| Row, selected epoch | H20 state | H79 state | H79 normal | H79 boundary | H79 completion |
| --- | ---: | ---: | ---: | ---: | ---: |
| causal normal-node control, epoch 5 | 0.0280508 | 0.0329458 | 0.0312939 | 0.0922880 | 30/30 |
| projected target, epoch 5 | 0.0294648 | 0.0341638 | 0.0326338 | 0.0911913 | 30/30 |
| three-hop band, epoch 5 | 0.0288267 | 0.0343065 | 0.0327718 | 0.0915600 | 30/30 |

Projected target and the three-hop band reduce boundary-node H79 error by
`1.19%` and `0.79%`, but increase normal-node H79 error by `4.28%` and `4.72%`.
Their H20 regressions are `5.04%` and `2.77%`, over the registered `2%`
allowance, and their H79 regressions are `3.70%` and `4.13%` rather than the
required improvement. They improve casewise H79 state error on only 6/30 and
7/30 trajectories. Neither reaches the historical D041 five-key H20 or
fixed-128 one-step parity thresholds.

The separate FP32 post-selection audit preserves 30/30 H79 completion and finds
no broad shock-smearing signature: some front descriptors improve, but H79
state error rises by `3.95%/3.07%`, smooth graph-high-pass energy rises by
`1.24%/0.73%`, and the directly targeted one-to-three-hop interior bands are
`2.35%--3.12%` worse. Every Mach and node-count stratum worsens. Worst pressure
remains positive for every row, so admissibility does not distinguish them.

**Plausible mechanism and alternatives.** Hard causal closure already provides
legal deployed boundary states. These auxiliaries appear to trade a small
improvement in overwritten boundary values for a worse interior recurrent map.
Normal one-step error changes by only about `-0.1%`, while auxiliary-versus-
control parameter updates remain highly aligned (`0.9946/0.9974` cosine), so
recurrent amplification of a small map perturbation is plausible. It is also
possible that the approximate closure target rewards self-consistency rather
than exact PDE boundary dynamics. This is one continuation seed, one frozen
weight per objective, and one approximate closure; gradient compatibility was
measured only at the parent.

**Claim and next decision.** Reject these exact soft objectives and weights for
this exact D041 causal-continuation contract. Do not infer that all boundary-
aware training is harmful, and do not launch a coefficient sweep. A future
serious bump baseline should retain legal hard closure and the normal-node
primary objective but omit these auxiliaries. Reconsider a soft boundary term
only after a same-process diagnostic isolates a persistent deployed near-
boundary defect and a solver-consistent legal target can be preregistered.
Compact training/post-evaluation archives have SHA-256 values
`175f9f4e6c321eadaf414ee148b90e1d1418a469f936f1961f79dd0c4ee48bfd`
and
`197ae9a5c05609b3fdbf4f29b558c8d1400d75ae9758ede57e2662b35668c61a`;
the full local analysis is retained under ignored
`artifacts/time_dependent_no/l3r_bg0_boundary_objective_20260728a/`.

### L3R-RB0P/RB0 Raw-Boundary Supervision Contract (2026-07-28)

**Diagnosed mechanism and causal prediction.** BG0's projected-target loss acts
through the closure's interior source stencil; it does not supervise the raw
boundary values that the model emits, and fixed-inflow projected gradient is
zero. The parent audit instead found a finite raw-boundary gradient with
interior-gradient cosine `0.263689` and a boundary-normalized gradient-norm ratio
`53.3796`. The approximate closed wall target also differs substantially from
the recorded reference wall states. The plausible mechanism is therefore that
BG0 optimized closure self-consistency rather than the missing raw boundary
map. If raw reference supervision is useful, it should lower fixed-population
raw-boundary one-step error without sacrificing the deployed normal-node map,
H20, or H79. If it only spends capacity on overwritten outputs, raw boundary
error may improve while recurrent state error stays flat or worsens.

**Exact intervention and legality.** For teacher input `U_t`, form
`X=P_B(U_t)`, compute raw proposal `Z=G_theta(X)`, train with proxy-weighted,
component-scaled conservative next-state MSE between `Z` and reference
`U_(t+1)` over every valid node, and deploy/recur only with `P_B(Z)`. Reference
boundary states are legal supervised targets during training and validation;
they are never read by inference or recurrence. There is no clipping, floor,
limiter, smoothing, auxiliary coefficient, multistep loss, input noise, or
generated-state exposure.

**Matched identities and budget.** RB0 starts from parent checkpoint SHA-256
`2bb5ee3ca831a6ffc498f01e309ae7ea957b2df0f411023fb3812919a6732964`,
uses data-manifest SHA-256
`5d5373fdcc682544bf330fba6d54fe65509dabe936baee7443c71a8c0c8d9fa7`,
and replays logical ten-epoch pair-stream digest
`bf5f0e1915b53bd296192c80ceafaca05d46c858cda1414e883472f7a5236456`
from retained JSON file SHA-256
`97140442b237af7381bf90da7771554825c8256aa304358dec7e1a7404590ffe`.
Architecture, normalization, seed, split, 2,048 presentations and 540 optimizer
steps per epoch, ten epochs, fresh AdamW, constant `2e-4` learning rate,
`1e-5` weight decay, BF16, gradient clipping at 1, microbatch 1, accumulation 4,
all-30 H20/H40/H60/H79 validation, parity thresholds, and selection tuple match
BG0. The retained comparator is BG0 causal control selected epoch 5, checkpoint
SHA-256
`0f6ee8ea02924ad4fd55de356a9b30290452551282ff61994025d49e1397c1e5`;
no new control training is authorized. Expected training is about 45 minutes;
the total zw-gpu cap is 2.5 hours and retained output is capped at 2 GiB.

**RB0P gate.** Before training, syntax and focused CPU tests must pass; the
default `normal_closed` path must retain its prior loss/recurrence semantics;
the raw objective must give nonzero raw-boundary gradient while leaving the
causal deployed proposal unchanged; exact parent, data, sample stream, source
snapshot, policy digests, and environment must bind; no holdout or sealed key
may appear; and one real-graph BF16 microbatch-1/accumulation-4 step must be
finite and fit the device. Any mismatch, fallback stencil, nonfinite value, OOM,
or cap miss closes RB0 without launch.

**Selection, utility, and anti-smearing gate.** Use the unchanged validation-only
selection rule. Relative to the retained BG0 control, the selected RB0 row must
retain 30/30 H79 completion, lower mean BF16 H79 state error below
`0.03294580398748318`, keep H20 no more than 2% above
`0.02805080624918143`, lower fixed-128 raw-boundary one-step error, and keep
fixed-128 normal one-step plus H79 normal-node error within 2%. FP32
post-selection evaluation must show no more than 5% regression in shock
strength or thickness and no broad smooth-high-pass/front conjunction
consistent with smearing. Calling RB0 a D041 replacement additionally requires
five-key H20 at most `0.024284941703081132` and fixed-128 deployed all-node
one-step error at most `0.005402445773142972`.

**Claim boundary and stop rule.** A pass supports only one-seed, fixed-family
causal-continuation evidence that raw proposal supervision is more useful than
normal-only training under this exact contract; it does not prove that boundary
supervision generally helps, explain D041's historical training recipe, validate
physical conservation, transfer to the dynamic family, or authorize holdout,
sealed evaluation, a coefficient sweep, a second seed, or architecture scaling.
Reject the intervention if raw boundary error improves but deployed H79 fails
the joint gate. Stop for nonfinite loss/gradient/parameters, recurrence
inadmissibility termination, identity drift, missing comparator, wall cap, or
output cap.

### L3R-RB0P/RB0 Result (2026-07-28)

**Status: DONE / CLOSED; EXACT OBJECTIVE REJECTED.** RB0P passed 71 focused CPU
tests, exact parent/data/stream/source/policy checks, and a real-graph BF16
finite-gradient smoke with 3.50 GiB peak reserved memory. The single RB0 arm
then completed ten epochs, 20,480 sample presentations, and 5,400 optimizer
steps in 2,554.7 training seconds. The selected epoch-8 checkpoint SHA-256 is
`7a7d65586ca7175a5ac07c646a6498fcfa990ccf2d41adc85afe3fdb8066b6af`;
the retained epoch-5 causal-control checkpoint SHA-256 is
`0f6ee8ea02924ad4fd55de356a9b30290452551282ff61994025d49e1397c1e5`.
The exact parent, data manifest, and ten-epoch stream identities remained those
frozen above. Deep run-argument comparison found no trained scientific
difference beyond `normal_closed` versus `raw_all_nodes`.

**Verified evidence.** On the fixed 128-pair audit, raw-boundary one-step error
fell from `0.0610170` to `0.00976047` (`-84.00%`), while deployed all-node,
normal-node, and boundary-node one-step errors improved only `0.87%`, `0.92%`,
and `0.75%`. BF16 all-30 H20 state error improved from `0.0280508` to
`0.0249742` (`-10.97%`), but H79 state error increased from `0.0329458` to
`0.0360086` (`+9.30%`) and H79 normal-node error increased `10.50%`. Both
selected checkpoints completed 30/30 H79 rollouts. Independent FP32 evaluation
confirmed the crossover: state change was `-7.85%`, `+1.08%`, `+6.56%`, and
`+11.71%` at H20/H40/H60/H79. At H79, smooth-region graph-high-pass error rose
`8.29%`; shock strength and thickness improved `25.06%` and `1.88%`, but front
IoU fell `4.96%` and symmetric Chamfer worsened `47.66%`. This is not evidence
of broad shock blurring.

The hard-closure correction RMS, `P_B(Z)-Z`, increased from `0.0606613` to
`0.164411` (`+171.03%`). The candidate update from the shared parent had
`1.733x` the control update norm and cosine `0.5598` with the control update.
Thus direct raw supervision was not a small extra signal: it substantially
reoriented the shared operator even though hard closure later overwrote its
boundary outputs.

**Plausible mechanism or inference.** Reference boundary states and the
approximate legal current-state closure are different contracts. Full-weight
raw supervision therefore teaches model outputs that are much closer to stored
boundary labels but farther from the states actually deployed. A useful shared
representation signal reaches the interior and improves H20, while the larger,
differently oriented update accumulates more recurrent normal-node,
smooth-high-pass, and front-shape error by H79. This is consistent with, but
does not prove, a fresh-versus-propagated decomposition for RB0.

**Missing evidence and alternatives.** This is one continuation seed and one
implicit raw-boundary weight. Constant-learning-rate continuation has material
epoch variation: at matched epoch 8 RB0 beats control epoch 8 at H79 by `3.18%`,
although RB0's best registered H79 remains worse than the control's selected
best. Seed/optimizer-path variation therefore remains an alternative
explanation for effect size, but it does not rescue the failed selection-rule
comparison. No exact RB0 fresh-versus-propagated audit, sealed or holdout
evaluation, dynamic-family transfer, physical boundary-flux measure, or
from-scratch seed was performed.

**Admissibility and protocol note.** The selected RB0 checkpoint has no NaN,
global blow-up, or termination. Epoch 0 transiently terminated trajectories 47
and 227 through nonpositive internal energy, and epoch 1 terminated trajectory
227; epochs 2--9 recovered 30/30 completion. The process continued because
gradients and parameters were finite and later checkpoints remained selectable.
That continuation is a deviation from the strictest reading of the registered
termination wording and precludes a positive preregistered-pass claim. The
selected method independently fails H79, H79-normal, and front/high-pass gates.

**Claim implication and minimum next experiment.** Reject only the exact
full-weight `raw_all_nodes` continuation. The result supports the narrower
claim that raw boundary labels contain useful short-horizon information; it
does not show that boundary supervision is generally harmful. At RB0 closure no
follow-on was authorized. The owner later authorized RA0P/RA0 below: retain the
deployed normal-node primary loss and add a raw-boundary-only reference
auxiliary whose coefficient is fixed by a parent-gradient target rather than
swept. A predeclared auxiliary-to-primary gradient-norm target of `0.1` and the
measured norm ratio `53.3796` imply coefficient `0.0018733749`, subject to the
fresh implementation audit. The causal prediction is that H20/raw-boundary
gains remain while update rotation, closure-correction growth, H60/H79 drift,
and smooth-high-pass growth shrink.

Main summary, fixed-pair audit, FP32 post-evaluation, compact archive, and NPZ
manifest SHA-256 values are
`15ec289f6fff38bffe4e39ae64cf76f3f4dc899e971d2edf31ec4e6ba48a79a4`,
`097247421cc8520d6146ace3e0e20f0d7b140525098ee4d98223ea8a3644e9c2`,
`9f40af0c4f57c1bc9591e6e9f2861d408917825338de320dcd86054f0d14074f`,
`18404b4f78bb50390d083b46e33176bd0b18236a05a22a05e0fd76e804a26f46`,
and
`b05ae8638fc0a00105f680288d9835a4ab08df917b4854caec7017fb48260467`.
The full local analysis is retained under ignored
`artifacts/time_dependent_no/l3r_rb0_raw_boundary_20260728a/`.

### L3R-RA0P/RA0 Gradient-Normalized Raw-Boundary Auxiliary Contract (2026-07-28)

**Diagnosed mechanism and causal prediction.** RB0 proved that raw reference
boundary values are learnable and can improve H20, but its full all-node primary
rotated the shared parameter update: candidate/control update cosine was
`0.5598`, update-norm ratio was `1.733`, and closure-correction RMS rose
`171.03%`. The smallest mechanism-matched intervention is therefore not another
primary objective. RA0 keeps the deployed normal-node map primary and adds only
a weak raw-boundary reference gradient. If raw boundary information benefits
the shared interior representation without dominating it, RA0 should improve
raw boundary and H20 while retaining or improving H79 normal/state and avoiding
RB0's high-pass/front-shape regression.

**Exact intervention and legality.** With teacher input `U_t`, form
`X=P_B(U_t)`, raw proposal `Z=G_theta(X)`, and deployed proposal `P_B(Z)`. Train

`L_RA0 = L_normal(P_B(Z), U_(t+1)) + 0.0018733749 * L_boundary(Z, U_(t+1))`,

where both terms use the existing proxy-node weights and conservative component
scales; `L_normal` is restricted to normal nodes and `L_boundary` to valid
non-normal nodes. Stored reference boundary values are legal training and
validation targets only. Inference and recurrence use `P_B(Z)` and never read a
future reference. There is no clipping, floor, limiter, smoothing, filtering,
generated-state exposure, input noise, multistep loss, new control row, or
architecture change.

**Frozen coefficient and identities.** The exact-parent raw-boundary/interior
gradient-norm ratio is `53.379597682281236`; a `0.1` auxiliary-to-primary target
gives `0.00187337492866`, frozen to CLI value `0.0018733749`. RA0 starts from
checkpoint SHA-256
`2bb5ee3ca831a6ffc498f01e309ae7ea957b2df0f411023fb3812919a6732964`,
uses data-manifest SHA-256
`5d5373fdcc682544bf330fba6d54fe65509dabe936baee7443c71a8c0c8d9fa7`,
and replays logical ten-epoch pair-stream digest
`bf5f0e1915b53bd296192c80ceafaca05d46c858cda1414e883472f7a5236456`
from retained JSON file SHA-256
`97140442b237af7381bf90da7771554825c8256aa304358dec7e1a7404590ffe`.
The retained comparator remains BG0 causal control epoch 5, checkpoint SHA-256
`0f6ee8ea02924ad4fd55de356a9b30290452551282ff61994025d49e1397c1e5`.

**Matched training and selection.** Architecture, 19,155,720 parameters,
normalization, split, seed, ten epochs, 2,048 presentations and 540 optimizer
steps per epoch, fresh AdamW, constant `2e-4` learning rate, `1e-5` weight decay,
BF16, gradient clipping at 1, microbatch 1, accumulation 4, all-30
H20/H40/H60/H79 validation, parity thresholds, and selection tuple match BG0
and RB0. Training/validation remain the existing 270/30 population; holdout and
sealed populations remain closed. Expected training is about 45 minutes; total
zw-gpu wall cap is 2.5 hours and retained output cap is 2 GiB.

**RA0P gate.** Focused CPU tests and syntax must pass. The dormant
`normal_closed + none` and existing projected/near/raw-primary paths must retain
their prior semantics. The new auxiliary must produce nonzero raw-boundary
gradient, zero loss support on normal nodes, and no change to the deployed
proposal or recurrent state. On the exact parent and frozen diagnostic cohort,
the recomputed BF16 norm ratio must match `53.379597682281236` within `1e-4`
relative and recommend CLI weight `0.0018733749`; otherwise stop rather than
retune. Parent/data/stream/source/policy/environment identities and zero fallback
stencils must bind. One real-graph BF16 microbatch-1/accumulation-4 smoke must be
finite and reserve at most 12 GiB. Any mismatch, nonfinite value, OOM, or cap
miss closes RA0 without launch.

**Utility and mechanism gate.** Relative to the retained causal control, the
selected RA0 checkpoint must retain 30/30 H79 completion; lower BF16 all-30 H20
below `0.02805080624918143`; lower H79 state below
`0.03294580398748318`; lower H79 normal-node error below
`0.031293914467096326`; lower fixed-128 raw-boundary one-step error; and keep
fixed-128 deployed normal one-step error within 2%. The selected update from the
parent must have at least `0.90` cosine with the control update and at most
`1.25x` its norm. Closure-correction RMS must be at most `0.075826625`. FP32
H20/H60/H79 shock-strength and thickness errors may not regress more than 5%;
H79 smooth high-pass may not rise more than 2%, front IoU may not fall more than
2%, and symmetric Chamfer may not rise more than 5%. D041-replacement language
still separately requires five-key H20 at most `0.024284941703081132` and
fixed-128 deployed all-node one-step at most `0.005402445773142972`.

**Stopping and claim boundary.** Individual checkpoint-wise rollout
inadmissibility is recorded and makes that checkpoint ineligible; it does not
stop later training epochs. Stop immediately for nonfinite training loss,
gradient, or parameter; identity drift; missing comparator; OOM; wall cap; or
output cap. A joint pass supports only one-seed fixed-family evidence that a
weak raw boundary gradient benefits this exact causal continuation. A miss
rejects only this coefficient and formulation. Neither outcome explains D041's
historical recipe, establishes physical conservation, transfers to the dynamic
family, or authorizes a coefficient sweep, second seed, holdout/sealed access,
smoothing/filtering, learned boundary module, or architecture scaling.

**RA0P closeout.** The corrected exact-source BF16 audit bound every registered
identity and passed the 73-test CPU gate. It recomputed gradient ratio
`53.34499581396788`, recommending coefficient `0.0018745901`; relative
difference from the frozen ratio is `0.000648222725830791`, above the `1e-4`
gate. At frozen coefficient `0.0018733749`, realized auxiliary/primary gradient
fraction is `0.09993517619849249`, but numerical closeness does not override the
prospective stop rule. Peak reserved memory was 6,144,655,360 bytes. RA0P is
failed/closed; no smoke, optimizer step, rollout, or RA0 output exists. A future
repeat-distribution or FP32/deterministic numerical contract requires explicit
human review and is not an RA0 retry.

### Boundary-Compatible Learning And Minimal Hard-Projection Direction (2026-07-28)

Status: **SCIENTIFIC DIRECTION RECORDED; NO NEW EXECUTION ROW AUTHORIZED**.
This amendment records the owner's post-BG0/RB0 design conclusion and governs
future boundary-method proposals. It does not reopen RA0, alter any completed
gate, select a checkpoint, or authorize training, inference diagnostics,
holdout access, or sealed evaluation.

**Hard enforcement principle versus present implementation.** Causal hard
boundary enforcement remains the deployment invariant: teacher inputs, model
outputs, and every recurrent state must satisfy a boundary contract using only
the current state, static geometry/type data, and known physical parameters.
This does not establish that the present nodal operator `P_B` is optimal. The
current operator fixes inflow, fully replaces wall and outflow primitive states
from a current-interior stencil, removes wall-normal velocity, and zeroes both
velocity components at low-normal-coherence wall corners. It is a legal nodal
closure, not exact DG boundary replay or a verified minimum-change
characteristic boundary operator.

**Why the prior losses conflict.** Let `Z=G_theta(P_B(U_t))` and deploy
`Uhat_(t+1)=P_B(Z)`. Under the current full wall/outflow replacement,

`d (P_B(Z))_B / d Z_B = 0`,

while the corrected wall/outflow values have nonzero derivative with respect to
the interior stencil values `Z_I`. A post-correction boundary-to-reference loss
therefore pushes the source interior predictions toward reproducing a boundary
trace that the nodal closure does not exactly represent. A raw-boundary loss
instead trains `Z_B`, which recurrence overwrites; through shared parameters it
can still rotate or enlarge the interior update without a corresponding
deployed boundary benefit. BG0 and RB0 are consistent with these two mechanisms,
but one continuation seed does not prove that they explain every boundary
regression.

**Boundary metric semantics.** Future contracts must not call one all-node
reference error "the boundary loss." They must separately report:

1. the physical boundary-contract residual after hard projection, which should
   be at numerical zero for the enforced components;
2. the corrected boundary-to-reference trace error, which includes mismatch
   between the nodal projection and the source discretization;
3. the raw proposal-to-reference error and raw-to-corrected intervention norm,
   neither of which is itself deployed accuracy; and
4. near-boundary normal-node and multistep interior error, which measure whether
   the boundary treatment improves the learned flow map.

The registered B1 all-node parity rule is not changed after seeing results.
Any later hard-boundary model must prospectively use a lexicographic selection
rule: first require completion and interior H20/H79 noninferiority, then compare
boundary trace/correction fields among the eligible checkpoints.

**Projection-compatible training direction.** The first candidate is a short
differentiable rollout objective under the unchanged hard recurrence,

`L_dyn = sum_(k=1)^K w_k L_normal((P_B o G_theta)^k(U_t), U_(t+k))`,

with `K=2` as the minimum screen and at most `K=4` in a later separately
reviewed contract. It supplies boundary-derived gradients only when the
corrected recurrent path improves future normal-node states. It is distinct
from a one-step projected-boundary loss, a near-boundary reweighting, raw
all-node supervision, and detached generated-state exposure.

A later boundary-trace objective, if still justified, must be secondary to
`L_dyn`. Prefer a boundary-specific adapter over an undifferentiated shared
head, supervise only boundary degrees of freedom not fixed by the physical
contract, and admit a shared-trunk auxiliary gradient only under a
prospectively frozen nonconflict rule and norm cap. One admissible formulation
projects away an auxiliary-gradient component that opposes the dynamics
gradient and caps the retained auxiliary norm at a small fraction of the
primary norm. This is a candidate constrained optimizer, not evidence that
gradient surgery will improve rollout behavior.

**Inference improvement hypothesis.** A more faithful hard operator may enforce
only incoming or physically constrained modes while preserving learned outgoing
or tangential modes. In local primitive or characteristic coordinates, a
minimum-change projection has the form

`P_B^*(Z) = argmin_V ||V-Z||_W^2 subject to C_B(V)=b_B`,

where `C_B` contains only the legal current boundary constraints. For a
linearized constraint this has the weighted projection

`Z - W^(-1) C_B^T (C_B W^(-1) C_B^T)^(-1) (C_B Z-b_B)`.

The type-specific hypothesis is:

- keep the full known freestream prescription at supersonic inflow;
- at a slip wall, impose zero normal velocity or mass flux while retaining
  learned density, pressure, and tangential velocity in the legal null space;
- at a verified supersonic outflow, avoid prescribing outgoing characteristic
  information and replace only an incoming component if the boundary regime
  actually requires one; and
- at corners and boundary-type junctions, enforce all incident face constraints
  by a small constrained solve rather than using one averaged nodal normal or a
  blanket zero-velocity heuristic.

This proposal retains hard legality but is not equivalent to the present full
interior extrapolation. It may require a positive/admissible parameterization
for free boundary components; any such mechanism must be declared explicitly
and must not be reported as an unmodified hard projection, clipping-free
rollout, or conservation guarantee. A later face/edge ghost-state or
characteristic-message implementation could be more solver-faithful than nodal
overwrite, but the bump bundle lacks exact DG boundary replay and validated
finite-volume boundary impulses, so it cannot support a physical flux claim.

**Minimum decisive diagnostics.** Before retraining with a modified boundary
operator, a separately authorized validation-only audit should compare the
current full nodal closure with exactly one frozen minimum-change
wall/outflow/corner projection on the same frozen checkpoint, trajectories,
evaluator, and raw proposals. Primary fields are H20/H79 completion and
normal-node state error. Boundary-contract residual, corrected-reference error,
correction norm, near-boundary normal error, admissibility margins, front
position/strength/thickness, smooth high-pass, and runtime are mandatory
no-harm fields. A post-hoc gain would justify training/evaluation consistency
tests; it would not establish that the alternative is the better learned
boundary method. The smallest later training comparison is one matched `K=2`
projection-consistent candidate against the retained normal-closed causal
control, with learned calls, sample presentations, optimizer steps, population,
seed, and selection exposure matched prospectively.

**Claim boundary.** The verified evidence supports hard causal enforcement as a
useful deployment discipline and rejects the exact BG0 auxiliaries and RB0
full-weight raw objective. It does not show that the current `P_B` is optimal,
that a raw-boundary auxiliary benefits interior dynamics, or that
minimum-change, characteristic, null-space, multistep, or conflict-controlled
training will succeed. Exact source-solver boundary semantics remain missing;
model propagation error, closure bias, and reference-trace representation are
still alternative explanations that a future contract must separate.

### Minimum-Change Boundary Protocol Audit And Pilot Closeout (2026-07-29)

Status: **VALIDATION AUDIT COMPLETE; BOUNDED PILOTS CLOSED; NO SERIOUS NEW
CHECKPOINT LAUNCHED**. The owner explicitly authorized implementation and
validation on zw-gpu using the two retained serious checkpoints, with permission
for serious training only after a convincing bounded protocol passed. No
post-hoc L3R run ID is assigned. This entry records the evidence under its date
without reopening or relabeling BG0, RB0, RA0P, B1, or any sealed population.

**Source and population binding.** The repository parent is commit
`549a724542eba5f961423c77b2ee8ab844f05ced`. The bump data-manifest digest is
`5d5373fdcc682544bf330fba6d54fe65509dabe936baee7443c71a8c0c8d9fa7`.
Exact D041 and retained B1 checkpoint SHA-256 values are respectively
`2bb5ee3ca831a6ffc498f01e309ae7ea957b2df0f411023fb3812919a6732964`
and
`221d12c3cd5f3df65546bb02537354fce334b481ab36478e7ff9f35f8e4323dc`.
The all-30 D041 audit binds minimum-change utility SHA-256
`ba17a75daaaa38d8172f9c619f5a7bae48cd385e434175a56b3fa002d7b319d4`,
boundary evaluator
`4a40ee0d7e37a9a23b7ccf1c7093cf1d56c42dbaeb6305593f3543ce1054c3d0`,
official evaluator
`0f0ab0dd49297cee88297c114cb3ed07ed2dd22725467bf41987ed89b1f65af`,
and its immutable trainer snapshot
`f37edfaa6a04cbf5b19b8b4ca92d2d224cacae09a6ae1876fc21bded482df039`.
Later local trainer changes are not retroactively attributed to that evaluator
artifact. All reported cases are from the existing 30-case validation split;
`test_split_opened=false`.

**Verified implementation.** The implemented `P_B^*` uses Euclidean
minimum-change projection in primitive coordinates. It fixes the full known
freestream at supersonic inflow; at a slip wall it preserves density, pressure,
and tangential velocity while removing only wall-normal velocity; at a wall
corner it stacks all incident wall-normal constraints and uses a pseudoinverse
projector; and at an outward-supersonic outflow it preserves the complete
proposal because every characteristic is outgoing. Incident-type junctions
apply all compatible face constraints. Evaluation stops if the outflow ceases
to be outward-supersonic rather than inventing an incoming state. The
implementation makes no exact-DG, physical-flux, or conservation claim.

The corresponding `learned_dofs_closed` objective compares `P_B^*(Z)` with
`P_B^*(Y)` on all nodes. Fixed inflow components and constrained wall-normal
velocity receive zero direct gradient, while interior nodes, wall density/
pressure/tangential velocity, and supersonic-outflow state remain densely
supervised. Teacher input, proposal closure, and every recurrent state use the
same `P_B^*`. Focused wall-null-space, corner-rank, idempotence, gradient-routing,
BF16, trainer, and evaluator tests pass as part of a 39-test focused suite.

**Verified D041 validation result.** Summary SHA-256
`2fd94666e6ce3578ee45b0c6907c18411ea397a74e32a1fb2d58697975a18688`
contains the all-30 validation comparison:

| Field | Native D041 | D041 plus `P_B^*` | Disposition |
| --- | ---: | ---: | --- |
| one-step all-node error | `0.005303016` | `0.005357904` | `+1.04%` |
| one-step normal-node error | `0.005274852` | `0.005284192` | `+0.18%` |
| corrected boundary/reference error | `0.006485145` | `0.008448299` | `+30.27%`; reference-trace no-harm fails |
| all-30 H20 state error | `0.022919620` | `0.022321912` | `-2.61%` |
| H79 completion | `28/30` | `30/30` | two nonpositive-energy failures rescued |

On the 28 common H79 survivors, `P_B^*` wins 26 trajectories; the median and
mean candidate/native state-error ratios are `0.925889` and `0.920730`.
Candidate/native H79 structural median ratios are `1.0213` for front centroid,
`0.99045` for shock-strength log error, `0.95886` for smooth-region high-pass,
and `1.10278` for shock-thickness log error. Thus state, completion, strength,
front-position, and smooth-region evidence is favorable, but the prospective
`1.05` thickness no-harm condition fails. Maximum physical boundary residual is
approximately `2.38e-7`; minimum recurrent outward-normal outflow Mach is
`1.443`; minimum density, internal energy, and pressure remain positive.

**Verified checkpoint/closure compatibility counterfactual.** The retained B1
checkpoint under `P_B^*` fails all five sampled H20 rollouts, usually at call 1
or calls 6--7 through nonpositive internal energy. Its summary SHA-256 is
`2a572223eb348876a8348d20fca60eddb9bbef71095987e8b5869de97c7ff0c3`.
B1 was trained under full wall/outflow replacement, so its free raw boundary
modes were not required by deployment. This supports a train/inference closure-
compatibility requirement; it does not by itself prove that untrained free
modes are the unique cause or reject `P_B^*` for a consistently trained model.

**Verified bounded training pilots.** Two D041-initialized `P_B^*` continuations
used identical 256 balanced presentations, 256 realized optimizer steps, seed,
model calls, parameter count, recurrence, BF16 precision, constant `2e-4`
learning rate, and fresh optimizer state. Dense `learned_dofs_closed` training
produces one-step all-node error `0.0055366` and five-case H20 error `0.0360436`;
its training/evaluation summary SHA-256 values are
`4dc8c2756bf6e7edd280380403887e379356504c1708922da9b773545cda8369`
and
`8b9c0cb7fcb4f21bf72021303691e7ebfef165ecea008f338732db7ecf79cc45`.
The matched `normal_closed` control produces `0.0055043` and `0.0367315`;
its training/evaluation summary SHA-256 values are
`9d236b4805f119f024460e89937902681a18d04996b430a699b13a500b62a058`
and
`00b2c63fc5b1aa9cfa6ffce81b64b870f30e23dae721e1f502de32dc765d5202`,
with checkpoint SHA-256
`9a7a88d15eb06da62e0c4c8387bed0a7a72d93b2f6f31e4c2f54b99a5c6d90ad`.
The frozen D041-plus-`P_B^*` parent has corresponding five-case H20 error
`0.0225576`. Both continuations therefore fail, while the dense row is slightly
less harmful than the normal-only control. Dense boundary supervision is not
isolated as the failure cause.

**Plausible mechanism or inference.** Preserving legal outgoing and tangential
modes reduces recurrent boundary intervention and artificial reflection, which
can explain the D041 completion and common-survivor gains. The H79 thickness
regression is consistent with a changed wall/outflow trace broadening some
fronts. B1's immediate failures are consistent with exposing free modes that its
training closure always overwrote. The shared pilot regression is more
consistent with the aggressive fresh-optimizer continuation and realized
256-step schedule than with a boundary-specific loss conflict.

**Missing evidence.** The bundle lacks DG face traces, ghost states, numerical
flux provenance, subsonic exterior incoming data, validated physical
control-volume/face measures for bump conservation, optimizer-state restoration,
a repeat continuation seed, and any test or sealed-population evaluation.

**Alternative explanation.** `P_B^*` may improve D041 mainly by moving proposals
away from a local inadmissible basin rather than by improving learned boundary
dynamics. Corrected boundary/reference degradation may partly measure the
difference between a legal nodal state and the DG solver's weak flux-level
trace. Pilot damage may be caused by optimizer reset, batch construction, or
update count, and therefore cannot rank the two objectives.

**Claim implication.** For the current validation population, D041 plus
`P_B^*` at teacher/current input, model proposal, and every rollout recurrence
is the preferred state/completion deployment protocol, with an explicit H79
shock-thickness caveat. Do not apply `P_B^*` post hoc to B1. No serious
checkpoint is justified by the two failed continuation pilots. The result
supports nodal constraint satisfaction and train/inference contract matching,
not exact DG replay, conservation, universal characteristic correctness, or
test generalization.

**Minimum decisive next experiment.** The next candidate is one matched `K=2`
differentiable recurrence screen from exact D041 under `P_B^*`. Use 256 fixed
two-step windows arranged as four same-geometry windows per batch, exactly 64
optimizer steps, 512 learned sample-level calls, the same 19,155,720 parameters,
seed, optimizer recipe, targets, checkpoint selection, and validation
population. The candidate's second call consumes
`P_B^*(G_theta(P_B^*(U_t)))` with its gradient attached; the control executes
the same two learned calls but consumes teacher `P_B^*(U_(t+1))` on call 2.
Call 1 uses dense deployed-degree supervision and call 2 uses normal/interior
state loss. Interior one-step, H20, and completion gates precede H79.

Stop if one-step or H20 normal/all-node error exceeds `1.02` times the frozen
D041-plus-`P_B^*` parent, completion falls, a state becomes inadmissible,
outflow Mach is at most one, or any H79 front/thickness/strength/high-pass ratio
exceeds `1.05`. Report the six distinct boundary quantities, boundary-distance
shells, front centroid/IoU/Chamfer, shock strength/thickness, and smooth-region
high-pass. Success would support learning the future interior consequence of a
recurrent boundary interaction. It would not establish solver-boundary
identity, conservation, or family/test transfer. This contract is recorded for
human review and was not launched during that closeout; the later authorized
result is recorded immediately below.

A frozen-base boundary null-space adapter is second in order if the `K=2`
screen identifies a boundary-local residual: predict only wall density,
pressure, tangential velocity, and outward-supersonic outflow modes while
leaving exact D041 interior output unchanged. Conflict-controlled auxiliary
gradients are third and require an FP32 repeatability preflight before any
training because RA0P supplies no method evidence. Explicit normals/tangents,
incident-type masks, current outward Mach/characteristic regime, and freestream
state belong first in this boundary-local path; broad backbone conditioning or
boundary-distance features remain later hypotheses.

### Projected-Teacher K=2 Screen Result (2026-07-29)

**Verified evidence.** The authorized screen used exact D041 checkpoint
SHA-256 `2bb5ee3ca831a6ffc498f01e309ae7ea957b2df0f411023fb3812919a6732964`
and the same validation manifest, split, normalization, 19,155,720 parameters,
BF16 execution, seed, fresh AdamW recipe, 256 fixed two-step windows, four
same-geometry windows per effective batch, 64 optimizer steps, and 512 learned
sample-level calls. Both arms used dense `learned_dofs_closed` supervision on
call 1 and normal-node supervision on call 2. Their presentation-stream digest
is `45bf46588770fa903ff4e6f81f334641c6cf549a2ab3c786cf03f5f5bdf31820`;
the only causal arm difference is whether call 2 consumes attached
`P_B^*(G_theta(P_B^*(U_t)))` or teacher `P_B^*(U_(t+1))`.

The source is commit `17361889aa2746161f1257eab3776cb3efe8b54c`, archive
SHA-256 `fdf5daa9d22c40094c1064eb65aa59a4b4a9d12b3b7492242b07217b133ff870`,
trainer SHA-256 `90361dd444cbebd6ac72fcf782f3709c6db7da2e773e91d8b97c76a3e2fa09fd`,
minimum-change utility SHA-256
`ba17a75daaaa38d8172f9c619f5a7bae48cd385e434175a56b3fa002d7b319d4`,
and boundary evaluator SHA-256
`cbd8c1c316b0bb60feec0a144dcebc72ee4aea9caddc2b00c7e03d99f3aa4b31`.
The attached and teacher training summaries have SHA-256 values
`0a9b6e5df6fcb2054dffe9e68fbc3ebd8b446db5a99723e0ec853e26e7d310d9`
and `ef7724fd07adfba1b77acb619ccd48fc59082c2b9eb43376773846fe49369d6c`.

| Validation field | Frozen D041 plus `P_B^*` | Attached recurrence | Projected-teacher control |
| --- | ---: | ---: | ---: |
| one-step all-node | `0.005357904` | `0.005474580` (`1.0218x`) | `0.005348566` (`0.9983x`) |
| one-step normal-node | `0.005284192` | `0.005399953` (`1.0219x`) | `0.005275214` (`0.9983x`) |
| all-30 H20 all-node | `0.022321912` | `0.030456286` (`1.3644x`) | `0.021134931` (`0.9468x`) |
| all-30 H20 normal-node | `0.022236208` | `0.030411876` (`1.3677x`) | `0.021025871` (`0.9456x`) |
| H20 completion | `30/30` | `30/30` | `30/30` |

The attached arm therefore failed the prospective `1.02x` one-step and H20
gates and did not spend H79 evaluation. Its state remained admissible, its
parameters remained finite, and no gradient clipping occurred, so this is an
accuracy failure rather than a numerical failure. The teacher control passed
the H20 gate and advanced to the exact all-30 H79 population. Its selected
checkpoint SHA-256 is
`769d5f8c3c995d0297e62f4132d00513b7e11139d7a54bc953d05f67bc69efdf`;
the H79 summary SHA-256 is
`c48c7b63dad42cbc051483a830fd323999505415d609fc93734d3e0ef8894341`.
Against frozen-parent summary SHA-256
`2fd94666e6ce3578ee45b0c6907c18411ea397a74e32a1fb2d58697975a18688`,
the teacher control improves H79 all/normal error from
`0.041737124/0.041552611` to `0.039093455/0.038858550`, ratios
`0.93666/0.93517`, with `30/30` completion. It improves 22/30 H20 and 21/30
H79 trajectory-wise state comparisons; the corresponding median case ratios
are `0.91687/0.94791`.

The six boundary channels remain distinct. Teacher one-step corrected and raw
boundary/reference errors are `0.008422063/0.008501329`; the raw-to-corrected
intervention RMS is `0.003998305`, `1.0334x` the parent. Cumulative one-, two-,
and three-hop normal-node boundary-band errors are
`0.006328983/0.006109460/0.006115665`, parent ratios
`0.98462/0.98715/0.99327`. H79 boundary error is `1.0041x` parent while H79
normal error is `0.9352x`. Wall and inflow constraint RMS values are
`5.77e-9/8.02e-8`; maximum absolute residuals are at most `3.58e-7`. Minimum
recurrent outward-normal outflow Mach is `1.433`, and density, internal energy,
and pressure stay positive.

The paired H79 structural median ratios relative to frozen D041 plus `P_B^*`
are `0.97084` smooth-region high-pass, `1.15102` front-centroid distance,
`1.04306` symmetric Chamfer, `1.02095` shock-thickness log error, and `0.63923`
shock-strength log error. Front IoU improves in aggregate, but front-centroid
distance improves on only 8/30 trajectories. The `1.15102` front-centroid
ratio fails the prospective `1.05` anti-smearing/phase envelope. The teacher
checkpoint is therefore a promising state-error recipe, not a selected final
protocol.

**Plausible mechanism or inference.** Two adjacent projected teacher states
supply dense, predictable supervision without asking the model to memorize
prescribed boundary coordinates, and the fully autonomous validation gains
show that this training signal can improve the deployed map. By contrast,
backpropagating call-2 error through the model's own call-1 state produces a
larger gradient path and a large rollout regression at this update scale. The
teacher control's near-boundary and high-pass gains, coupled with its front-
centroid loss and slightly larger intervention norm, are consistent with a
useful local correction accompanied by a global phase-speed change from
updating the full backbone.

**Missing evidence.** There is no repeat seed, FP32 gradient-repeatability
audit, optimizer-state restoration, test or sealed-population evaluation,
face/ghost/flux provenance, or physical bump conservation geometry. The screen
does not isolate teacher-input distribution from its smaller realized gradient
norm, and cumulative hop bands are not Euclidean distance shells.

**Alternative explanation.** The teacher advantage may be a lower effective
gradient-magnitude continuation rather than a benefit unique to projected
teacher states. The front-centroid failure may reflect a BF16/one-seed phase
shift, a global interior-weight update, or sensitivity of a small-denominator
front metric rather than a boundary-local defect. Aggregate IoU improvement
does not override the preregistered paired centroid failure.

**Claim implication.** Reject attached differentiable `K=2` recurrence in this
form. Retain projected-teacher two-time dense supervision as a useful recipe
and mechanistic lead, but do not select its checkpoint, open sealed data, claim
conservation, or call it a boundary method: the full backbone changed and the
H79 front gate failed. The evidence supports closure-consistent dense training
more strongly than self-generated-state backpropagation.

**Minimum decisive next experiment.** Before training a boundary adapter, run
one validation-only, no-training two-checkpoint splice diagnostic. At every
call evaluate frozen D041 and the projected-teacher checkpoint on the same
legal recurrent state. One hybrid keeps D041 raw normal-node output and takes
the teacher checkpoint's raw whole-state boundary proposal; the shared
`P_B^*` is then applied, so prescribed inflow and wall-normal coordinates are
identical and only legal free boundary degrees can differ. The reverse hybrid
keeps the teacher raw normal-node output and D041 raw boundary proposal before
the same projection. Both hybrids use exactly two learned calls per sample and
the same validation identities, BF16 evaluator, and H20 state/front/high-pass
metrics. The boundary-only hybrid must retain at least half of the
teacher-parent H20 gain without a `1.05` structural regression before spending
H79. If the reverse interior hybrid retains the gain or the front failure,
route the result back to general interior PCNO work and do not train a boundary
adapter. If boundary-only output retains the gain and removes the phase failure,
the next trainable row is a frozen-D041 legal-null-space adapter, not another
full-backbone continuation. This diagnostic cannot establish parameter
efficiency, conservation, exact DG replay, or transfer. The authorized result
is recorded immediately below.

### Boundary/Interior Output-Splice Result (2026-07-29)

**Verified evidence.** The diagnostic used frozen D041 and projected-teacher
checkpoint SHA-256 values
`2bb5ee3ca831a6ffc498f01e309ae7ea957b2df0f411023fb3812919a6732964`
and `769d5f8c3c995d0297e62f4132d00513b7e11139d7a54bc953d05f67bc69efdf`,
the exact 30-trajectory validation population, BF16, the same normalization
digest `d2d07a4000acc3cfd45ff105a19e7437177d553cee4c18d50858f5572de7efec`,
and minimum-change recurrence. Each hybrid evaluated both frozen models on the
same recurrent input, spliced raw whole-state node proposals, and applied one
shared `P_B^*`. No optimizer step or parameter update, test population, or
sealed population was used. Each H20 variant consumed 2,912 learned calls.

The bound source is base commit
`17361889aa2746161f1257eab3776cb3efe8b54c`, sealed source-archive SHA-256
`eedf3793dcdc7c854a71e279fbcd1a41748c5f28608e5838921763e415aa6a99`,
splice-evaluator SHA-256
`c7de7f3fa7ade94fb895d188bf9436fe81ca8a743022beccd908c438508df17c`,
and focused-test SHA-256
`158f9a4c5a2dacce74a45e039b9f388fb18d3e7175790eee1c59582467e77031`.
The one-trajectory smoke summary SHA-256 is
`d5470284c3712f6c2a4e6933b561d4dcf0b996a54380494f507390bae2ae3294`;
the matched H20 summary SHA-256 is
`3e3b33ce25f4b9f0fd8f5ee288a5b43ce379628206a5597741f82fe51b3ab17a`.
The first smoke preflight stopped before evaluation because legacy D041 has no
declared normalization-digest field. Its complete normalization payload is
exactly equal to the teacher payload; the corrected evaluator recomputes both
digests and still rejects any declared digest inconsistent with its payload.
At the result freeze, six focused tests passed, and 47 tests passed in the
combined splice, multistep-training, and residual-PCNO CPU suite.

| H20 validation field | Frozen D041 | Teacher checkpoint | D041 normal + teacher boundary | Teacher normal + D041 boundary |
| --- | ---: | ---: | ---: | ---: |
| one-step all-node | `0.005357904` | `0.005348566` | `0.005356823` (`0.99980x`) | `0.005349466` (`0.99843x`) |
| one-step normal-node | `0.005284192` | `0.005275214` | `0.005283704` (`0.99991x`) | `0.005275368` (`0.99833x`) |
| one-step boundary-node | `0.008448299` | `0.008422063` | `0.008421638` (`0.99684x`) | `0.008448661` (`1.00004x`) |
| all-30 H20 all-node | `0.022321912` | `0.021134931` | `0.022098656` (`0.99000x`) | `0.020697869` (`0.92724x`) |
| all-30 H20 normal-node | `0.022236208` | `0.021025871` | `0.022028075` (`0.99064x`) | `0.020572650` (`0.92519x`) |
| all-30 H20 boundary-node | `0.025569010` | `0.025250686` | `0.024480292` (`0.95742x`) | `0.025949623` (`1.01489x`) |
| teacher-parent all/normal gain retained | - | `1.0/1.0` | `0.1881/0.1720` | `1.3682/1.3745` |

The boundary-from-teacher hybrid improves paired H20 all-state error on 20/30
trajectories, but its paired median ratio is only `0.99354` and it retains much
less than the required half of the aggregate teacher gain. The reverse hybrid
improves 25/30 cases with paired median all-state ratio `0.90343`; it is also
`0.98019` median relative to the full teacher checkpoint at H20. Thus the
teacher normal-node proposal carries the dominant state gain under this splice
and combines more favorably with the D041 boundary proposal than with its own.

The H20 paired structural median ratios relative to D041 are, respectively,
`0.99470/0.98396/1.19086/1.07505` for high-pass/front centroid/shock-thickness
log/shock-strength log in the boundary-from-teacher hybrid and
`0.99101/0.94451/1.46613/1.31560` in the reverse hybrid. The registered
boundary-only gate therefore fails both state-gain checks and both shock-
thickness/strength checks. Both variants complete 30/30, remain admissible,
keep minimum recurrent outflow Mach above `2.44`, and retain wall/inflow
constraint residuals no larger than `3.58e-7`. H79 was not spent.

**Plausible mechanism or inference.** The projected-teacher checkpoint's state
improvement is primarily an interior-map effect, not a consequence of better
raw boundary proposals. Teacher boundary output does improve the H20 boundary
trace when attached to D041, but that smaller local change propagates to only
about one fifth of the teacher's aggregate interior gain. D041 boundary output
slightly improves the teacher interior map's H20 state result, suggesting that
the teacher checkpoint's boundary proposal is mildly antagonistic to its own
interior prediction under this nodal recurrence. The shock-thickness and
strength regressions in both directions also show that structural behavior is
not cleanly localized to one output population.

**Missing evidence.** The failed H20 gate intentionally leaves no hybrid H79,
repeat seed, FP32 repeatability measurement, one-call adapter, gradient
attribution, test evaluation, DG face/flux provenance, or conservation result.
The experiment cannot localize the teacher checkpoint's H79 front-centroid
failure. It tests two frozen output populations under recurrence, not a model
whose boundary and interior channels were trained to be independently
composable.

**Alternative explanation.** The reverse hybrid's extra state gain may be
nonlinear complementarity between separately trained proposals rather than a
general statement that D041 boundary output is superior. Conversely, the
boundary-only structural regressions may arise from splice incompatibility or
small phase changes, not intrinsically bad teacher boundary values. Corrected
nodal boundary-reference error is also not a direct measure of the missing DG
face flux that drives future interior evolution.

**Claim implication.** Reject a frozen-D041 boundary null-space adapter derived
from this checkpoint pair and reject the two-call splice as an inference
protocol. The evidence strengthens the conclusion that projected-teacher dense
training improved the interior operator while boundary accuracy was not the
dominant H20 state bottleneck. Keep causal `P_B^*` as the rollout invariant;
do not add another boundary loss to the full backbone, spend H79, launch a new
boundary training run, or open sealed data from this result.

**Minimum decisive next experiment.** There is no justified next boundary
experiment for this checkpoint pair. Return the projected-teacher interior
signal and its structural failures to the general PCNO owner. Boundary work
should re-enter only after a one-call interior checkpoint passes the registered
state and structure gates and a fresh decomposition still shows a boundary-
local residual. The smallest re-entry experiment is then the same frozen
output-localization diagnostic; only a passing boundary-local result licenses
a legal-null-space adapter. This result still cannot support conservation,
exact DG replay, parameter efficiency, or family/test transfer.

### Boundary-Information Line Closeout Decision (2026-07-29)

Status: **CLOSED WITH CONDITIONAL RE-ENTRY** for the current bump-PCNO lineage.
This is a terminal negative/partial research result, not a claim that neural-
operator boundary conditions are solved. The local result-to-claim verdict is
`partial` with high confidence. An optional independent Codex judgment is
`[pending Codex review]` because no private-result transmission scope was
approved; operational closure does not depend on that second opinion.

| Question | Terminal evidence | Disposition |
| --- | --- | --- |
| Must rollout obey a causal hard boundary contract? | Native recurrence is fragile; D041 plus `P_B^*` improves H20 state, H79 completion, and common-survivor state error while keeping wall/inflow constraint residuals near numerical noise. | **YES as the deployment invariant**, with the registered H79 thickness and reference-trace caveats. |
| Does a stronger boundary-reference loss improve learned interior dynamics? | BG0's projected-boundary/near-band auxiliaries fail; RB0 greatly improves raw boundary fit and H20 but worsens selected H79 state/structure; RA0P supplies no method evidence. | **NO for the tested objectives.** Do not retry or retune them. |
| Should gradients traverse the model's own two-step recurrence? | The attached `K=2` arm fails one-step and H20 while the projected-teacher control improves state. | **NO for the tested continuation scale.** |
| Is the projected-teacher gain boundary-local? | Boundary-only splicing retains only `18.81%/17.20%` of its H20 all/normal gain; the reverse retains `136.82%/137.45%`. | **NO.** The useful state change is primarily in normal-node/interior output. |
| Is a frozen-D041 boundary null-space adapter licensed? | The boundary-only splice fails the half-gain and thickness/strength gates. | **NO.** No H79 or adapter training follows. |
| Is current `P_B^*` exact DG replay, optimal, or conservative? | Face, ghost-state, numerical-flux, and accepted-substep boundary provenance are absent. | **UNRESOLVED AND OUT OF CLAIM.** Do not infer it from nodal geometry. |

**Verified evidence.** B1P/B1, BC0P/BC0, BG0P/BG0, RB0P/RB0, RA0P/RA0,
the minimum-change audit, both bounded D041 continuation pilots, the matched
`K=2` screen, and the output-splice diagnostic all have terminal dispositions.
No registered boundary run remains active. No boundary result opened the sealed
population. The retained implementation consists of the causal/minimum-change
policy, evaluator, diagnostic training switches, splice evaluator, and focused
tests; it has an invocation path and a recorded result rather than speculative
scaffolding.

**Plausible mechanism or inference.** Boundary information matters chiefly as
a causal inference/rollout constraint and as known conditioning, not as an
extra reference-trace target on the current full backbone. Once prescribed
degrees are projected, errors in the learned interior map dominate the tested
H20 state gain. Boundary trace quality and future interior accuracy are not
interchangeable because recurrent source stencils, outgoing modes, and shock
phase couple them nonlinearly.

**Missing evidence.** There is no exact DG face replay, characteristic outflow
operator, corner flux provenance, physical conservation claim, repeat-seed
confirmation, sealed evaluation, or cross-family boundary result. RA0 remains
preflight-only evidence, and the projected-teacher checkpoint remains one-seed
BF16 evidence that fails a structural gate.

**Alternative explanation.** A different geometry family, checkpoint, or
face-aware representation may expose a truly boundary-local bottleneck. The
current negative result may also depend on nodal-splice incompatibility or on
the bump mesh's reconstructed boundary stencil. Those alternatives motivate a
future re-entry condition, not another experiment on this closed pair.

**Claim implication.** Safely close the boundary-information line for the
current D041/B1/projected-teacher lineage. Keep D041 plus `P_B^*` as the
preferred validation state/completion protocol with an explicit anti-smearing
caveat. Do not promote B1, the teacher checkpoint, either splice, any soft/raw
boundary objective, or an adapter. Transfer the projected-teacher interior
signal and structural failure to the general PCNO owner.

**Minimum decisive next experiment.** None inside this line. Re-entry requires
all three prospective conditions: (1) a one-call interior checkpoint passes
the registered state and structure gates, (2) the six-channel decomposition
still isolates a material boundary-local residual, and (3) the proposed method
changes only legal free boundary degrees under causal `P_B^*`. Otherwise the
line stays closed. Exact DG/characteristic work additionally requires new face,
ghost-state, and flux provenance before implementation or conservation claims.

### Post-Closeout Retained-Code Review (2026-07-29)

**Verified evidence.** A line-by-line review found four future-invocation
contract gaps in the retained splice evaluator, not a mismatch in the frozen
H20 metrics. The result-generating v1 summary did not record `start_frame`; the
reference summaries do record `step_stride` and per-trajectory minimum-change
policy digests, but v1 did not validate them; its H20 gate did not explicitly
require all 30 paired structural values; and its `trainable_parameters: 0`
field described the no-training diagnostic although the child checkpoint
parameters still had `requires_grad=True`. No backward pass, gradient
accumulation, optimizer step, or parameter update occurred. Rollout, structure,
and projection-decomposition evaluation used no-gradient contexts; the shared
one-step evaluator ran in eval mode, constructed only transient autograd graphs,
and detached every reported metric. The frozen H20 artifact has stride 1, its
policy digests exactly equal both frozen reference summaries, and all four
structural gate fields contain 30/30 pairs. The registered invocation used the
source default `start_frame=0`.

The maintained evaluator is now schema v2. It rejects nonzero start frames
because the v1 reference-summary schema cannot bind them, validates stride and
the complete policy-digest population against both references, literally
freezes both child modules, records `start_frame`, and requires 30 paired values
for every structural gate field. Its SHA-256 is
`ea48ce218f0bbeb29e5686cd248803d628af6e559895f290ea569d0fc1477a38`;
the nine-test source SHA-256 is
`2beeb3f60d01480efd65f8b143838cf878d5b766089cccf9578ad68069693c69`.
All nine focused tests and all 50 combined splice, multistep-training, and
residual-PCNO CPU tests pass; syntax, Black, and diff checks pass.

**Plausible mechanism or inference.** These were provenance and fail-closed
retention defects, not evidence of a different learned mechanism. Every newly
explicit comparison condition is already satisfied by the frozen H20 run; and
literal parameter freezing cannot change an eval-only forward with no backward
pass. No reported state, boundary, structure, admissibility, or outflow value is
expected to change.

**Missing evidence.** The v2 evaluator has not rerun either checkpoint on GPU,
so byte-for-byte v2 metric equality is not claimed. The v1 summary itself still
lacks a `start_frame` field; its frame-zero identity remains bound by the
registered invocation and source default rather than by that JSON field.

**Alternative explanation.** The four findings can be viewed as schema and
defensive-validation omissions because the exact registered invocation already
satisfied them. That does not make them safe for a reusable CLI, where a
nonzero start, changed policy, changed stride, or incomplete pairing could have
silently produced an invalid comparison.

**Claim implication.** Keep the frozen v1 evaluator/test hashes and source
archive above attached to the completed H20 result. Do not substitute the v2
hash retroactively. Use v2 for any future authorized reuse. The scientific
closeout and rejection of this checkpoint-pair adapter remain unchanged.

**Minimum decisive next experiment.** None for closeout. If a future re-entry
authorizes this diagnostic on another checkpoint pair, first run one validation
trajectory through v2 and require all new contracts to pass before spending the
registered population; that is a future preflight, not authorization here.

## Line 3 Restart Decision (2026-07-26)

### Scope And Claim Map

The restart has at most two primary claims:

| Claim | Minimum convincing evidence | Anti-claim that must be ruled out |
| --- | --- | --- |
| L3R-C1: the large improvement of the retrained bump residual PCNO is reproducible from a clean, fully bound source and training contract | A clean reproduction recovers the frozen H20 and H79 behavior within the preregistered tolerances, including the same five legacy audit trajectories, followed by a bounded attribution screen and seed confirmation of at most two influential factors. | The gain is merely a different evaluation cohort, a larger backbone, an undocumented dirty-tree change, or an unexamined bundle of data, target, conditioning, and checkpoint-selection changes. |
| L3R-C2: a jointly trained, stride-conditioned multirate PCNO can retain the D060 state advantage without surrendering the D044 high-pass and front behavior | A parameter- and exposure-accounted shared model passes raw H60 completion and the conjunctive state/high-pass/front gate on position-OOD validation, then repeats across three seeds before any sealed evaluation. | The result is scalar averaging of two frozen states, an effect of more parameters or presentations, shock blurring, a hidden limiter, or physical conservation. |

L3R-C1 belongs to the CPG supersonic-bump bundle. L3R-C2 belongs to the
audited Mach-1.1 dynamic finite-volume family. Their errors, quadrature
proxies, geometry fields, boundaries, and populations must not be pooled.
D041--D062 remain frozen evidence and retain every historical failure label.
A necessary-condition failure still rejects only its exact registered
construction.

### Run Order And Authorization

| Run ID | Stage | Purpose and population | Decision gate | Current status |
| --- | --- | --- | --- | --- |
| L3R-M0 | Documentation and provenance | Correct metric/figure semantics; locate, inventory, and hash available local copies of the bump base and continuation checkpoints; bind clean source, command, environment, seed, split, data, configuration, and evaluator identities. Prepare a retrieval request if a required artifact is remote-only. No remote access, checkpoint execution, or GPU work. | Every available artifact closes against its recorded digest. Any missing checkpoint or unrecoverable dirty diff is declared rather than reconstructed from memory. | **DONE: local audit closed; separately authorized read-only retrieval completed; no checkpoint execution, training, or new/sealed split read** |
| L3R-U0 | Frozen unchecked-continuation replay | Execute only R/D041 checkpoint SHA-256 `2bb5ee3ca831a6ffc498f01e309ae7ea957b2df0f411023fb3812919a6732964` on already-open bump holdout trajectory `05`, source frame 0 through all 79 stride-1 calls. Preserve raw `model_all_nodes` conservative recurrence while ignoring the positivity stop; do not floor, clip, smooth, replace boundaries, inject future truth, train, or open another split. | Calls 1--32 and the first excluded proposal at call 33 must replay retained D041 trajectory artifact SHA-256 `4d32a1ea9869a4eb565e3eb1769f5864f3247704857140410c5e43ea67fbb5ed` with maximum absolute difference at most `1e-5` and relative L2 at most `1e-6`. Record all 80 states only after the replay gate passes. Stop for replay mismatch or an execution exception. | **FAILED / CLOSED: AutoDL replay stopped at call 7 (`1.431e-5` max absolute, `5.373e-8` relative L2); no continuation output** |
| L3R-U1 | Exact-state-spliced unchecked continuation | Take calls 1--32 and the first inadmissible call-33 proposal verbatim from the retained D041 trajectory-`05` artifact, place that exact float32 call-33 state on the GPU, and execute only raw checkpoint calls 34--79. Concatenate the frozen prefix and new suffix for visualization and diagnostics. | Require the retained artifact and checkpoint hashes above, exact shard/population identity, an exact CPU--GPU--CPU call-33 round trip, 33 frozen prefix calls plus 46 new calls, and 80 encoded states. Record raw margins/counts, finiteness, global reconstructed-weight proxy error, amplitude growth, and an all-frame animation. | **DONE / CLOSED: 80 states retained; no numerical, 100x-amplitude, or proxy-L2-10 blow-up through call 79** |
| L3R-R0 | Clean bump reproduction | Retrain the selected bump residual-PCNO contract from a clean commit. Use the historical validation cohort for selection; use the already-open D041 holdout and the five legacy trajectories only for post-selection reproduction. | H20 has 5/5 raw completion; mean H20 rollout error and one-step error are at most `1.15x` the frozen `0.024285` and `0.005402`; D041 H79 has at least 17/20 completion and mean survival at least `0.875696`; the five legacy trajectories complete H79. Failure stops method work and routes to source/environment comparison. | **SUPERSEDED AS A PREREQUISITE by the 2026-07-27 human direction; not executed and not failed** |
| L3R-A0 | Bounded bump attribution | One serious seed per matched block: data coverage (`270`/all times versus historical `40`/first `60`); learned-map/objective bundle (normalized conservative-state residual versus direct primitive next-state with the legacy positive-primitive head); node-type/Mach conditioning; and rollout selection/clean continuation using the same checkpoint series where possible. | Rank factors using the frozen validation metrics and raw recurrence. Promote at most two influential blocks to three-seed confirmation. No factorial sweep, test-driven selection, or method claim. | **SUPERSEDED AS A PREREQUISITE; no historical-gain attribution claim will be made from L3R-B0** |
| L3R-MR0 | Primary dynamic method | Train one shared PCNO on stride-1 and stride-2 transitions with explicit stride or physical-`Delta t` conditioning, matched total sample presentations, at most `1.01x` D044 parameters, raw conservative-state recurrence, and no state averaging or post-hoc correction. Compare with frozen D044 and D060 on all 24 position-OOD validation trajectories through physical H60. | 24/24 raw completion; mean physical-volume state error at most `1.03x` D060; six-case smooth high-pass RMS and front-centroid distance each at most `1.03x` D044; the frozen D044/D060 shock-strength, thickness, vortex-core, and physical-total fields must each stay within the registered 5% non-regression envelope on at least 5/6 D013 cases. Report latency and exact parameter/presentation counts. A one-seed pass authorizes only seed confirmation. | **BLOCKED on L3R-A0 and separate execution review** |
| L3R-MR1 | Dynamic seed confirmation | Repeat the frozen L3R-MR0 configuration for three independent seeds and report seed-level values and intervals. | The conjunctive gate must hold without selecting a favorable seed. Freeze the method and thresholds before any sealed population is opened. | **BLOCKED on L3R-MR0** |
| L3R-L0 | Conditional local method | Only if L3R-MR0 fails specifically through localized fresh high-pass generation while retaining adequate state behavior, compare one jointly trained D055-sensor-gated local residual branch with a parameter-matched ungated branch. D056's fixed post-hoc correction is not reused. | One serious seed must improve the registered local defect without worsening raw state, front, admissibility, or balance; only then may a seed-confirmation contract be drafted. | **CONDITIONAL: not authorized by this decision** |
| L3R-F0 | Sealed evaluation | One-time strength-OOD and test evaluation of a frozen, seed-confirmed method. | Requires a later explicit human authorization after code, checkpoint rule, seeds, thresholds, and primary tables are frozen. | **SEALED: not authorized** |

The attribution screen is intentionally blockwise. If the learned-map/objective
bundle is influential, it may be split once in a reviewed follow-up; this plan
does not authorize a broad combinatorial ablation. L3R-MR0 is the sole primary
new method family. L3R-L0 is a conditional fallback, not a parallel branch.
The fixed Wendland span, canonical face-value objective, scalar rollout blend,
and independent-row front chart remain closed in their exact tested forms.
Geometry-conditioned objective weighting may be an ablation inside an
authorized method, not a separate search program.

### L3R-U0/U1 Authorized Diagnostic Exception

On 2026-07-27 the human owner requested continuation of the frozen D041
trajectory-`05` proposal through the complete 80-state source record despite
negative density, internal energy, or pressure, and explicitly selected AutoDL.
This authorizes only the L3R-U0 row above and, after U0's frozen replay gate
stopped, the exact-state-spliced L3R-U1 continuation. It does not authorize L3R-R0,
L3R-A0, training, checkpoint modification, another trajectory, a new or sealed
split, smoothing, branch attenuation, or a method claim.

L3R-U0 failed closed at call 7 because its maximum absolute prefix difference
was `1.431e-5`, above the prospectively frozen `1e-5` gate, although its
relative L2 difference was only `5.373e-8`, below `1e-6`. It produced no
post-admissibility rollout or animation. The gate is not relaxed after seeing
the result.

L3R-U1 instead starts from the exact retained D041 call-33 failed proposal.
Calls 1--33 are artifact bytes, not recomputed states; calls 34--79 are a new
continuation by the exact checkpoint under the currently bound evaluator and
PCNO source. This splice can answer whether the learned map remains finite or
exhibits severe growth after that exact inadmissible state. It cannot establish
the literal continuation that the unretained dirty D041 runtime source would
have produced.

The diagnostic predeclares three nonphysical-growth labels. Numerical blow-up
means any nonfinite conservative component. Severe finite-amplitude explosion
means a maximum absolute conservative component at least `100x` the maximum
absolute reference component. Severe global-error explosion means the
reconstructed-weight scaled relative L2 reaches `10`. These thresholds are
descriptive labels, not Euler admissibility or physical-conservation criteria.
Nonpositive density/internal energy/pressure is recorded but never itself stops
recurrence. Truth exists through call 79, but post-call-33 comparisons describe
the learned numerical map after its physical state contract has failed; they
are not admissible-rollout accuracy evidence.

The AutoDL cap for each diagnostic is one GPU and 20 wall-clock minutes, with
at most 250 MiB of retained output. U1 must retain the raw 79-call concatenated
sequence, per-call metrics, a digest-bound summary, one GIF encoding the initial
state plus all 79 proposals without visualization subsampling, and PNG/PDF
diagnostic figures. Its entry-point and test digests must be frozen before
launch and captured again at runtime. The frozen U1 entry-point SHA-256 is
`ba6f27c1b6e5186303579a84e5c98123b08c9df056aa5ed87ad4fe7269d20d9b`;
its four-test SHA-256 is
`17aedf73cb2b115cab6e4e571027af7d10ab0458b491af7be49661d478e9abe9`.
L3R-R0 remains the next experiment for attribution or training regardless of
the U0/U1 outcome.

#### L3R-U1 Outcome And Claim Boundary

**Verified evidence.** The D041 prefix is byte-exact for calls 1--33, the
call-33 CPU--GPU--CPU transfer is exact, and the current bound source executes
all 46 requested calls 34--79. All conservative and decoded primitive values
remain finite. Density never becomes nonpositive; the maximum conservative
amplitude never reaches even `1x` the maximum reference amplitude, the proxy
L2 never reaches `10`, and its maximum is the final `0.0317686`. Therefore the
preregistered numerical, finite-amplitude, and global-error blow-up labels are
all false through call 79.

Inadmissibility remains node-local and is not monotone: upper-left inflow node
`6` is the sole failed node on calls `33--47`; call `48` is fully admissible;
wall node `294` beside the outflow/front is the sole failed node on calls
`49--51`; calls `52--69` are fully admissible; and node `6` alone fails again
on calls `70--79`. Node `6` reaches minimum pressure `-0.427637` at call `43`
and ends at `-0.277391`, while its exact reference pressure is constant at
approximately `1`. Node `294` fails immediately before a reference pressure
front rises from `0.828446` at call `52` to `1.80708` at call `53` and
`2.83336` at call `54`.

**Plausible mechanism or inference.** The first upper-left event is a recurrent
boundary-local conservative-component imbalance, not density collapse: total
energy falls while kinetic energy rises until internal energy and pressure
cross zero. The model predicts every boundary node; the top-left node is
one-hot inflow because the left label overrides the wall label; its mixed
inflow/wall/interior three-neighbor differential stencil is extreme; and its
normalized reconstructed-weight share is only `1.11273e-5`. The same proxy
weights enter training loss, so persistent residual bias at this node receives
very little direct penalty. These facts explain vulnerability and why global
loss stays bounded. They do not uniquely identify the responsible learned
branch or prove that geometry alone causes the event. The later node-`294`
episode is temporally and spatially consistent with front-position/thickness
error at a wall/outflow junction, but occurs after an invalid state and is not
causally identified.

**Missing evidence.** D041's dirty runtime source is still unrecovered. The
bundle does not retain same-process `G(u_t)` and `G(uhat_t)` at the failing
calls, branch responses, a causal boundary-to-interior trace, or a matched
legal autonomous boundary counterfactual from the same prefix. U1 therefore
cannot separate fresh teacher-forced defect from propagation or attribute the
corner residual to spectral, pointwise, or differential branches.

**Alternative explanation.** The lower-left inflow corner has comparably low
weight and an equally extreme stencil but remains near pressure `1`, so corner
topology, label precedence, and low loss weight are not sufficient causes. A
position-specific learned residual bias, global spectral coupling, propagated
incoming error, or an interaction with the moving front can produce the same
observations. The transient node-`294` failure is a distinct candidate
front/boundary mechanism and must not be folded into the upper-left account.

**Claim implication.** D041's completion stop is a legitimate local physical
contract failure, but is not evidence of global numerical blow-up. Continuing
through the failure can even re-enter the admissible set, which does not make
the intervening states physically meaningful. U1 adds no support for blanket
smoothing, ripple suppression, uniform branch attenuation, or a conservation
claim; high-frequency content was neither the stop condition nor shown causal.

**Minimum decisive next experiment.** Do not add a separate sweep. Instrument
the already proposed L3R-R0 clean reproduction to retain, in the same process,
`G(u_t)`, `G(uhat_t)`, their additive/cross error terms, raw nodewise margins,
actual failure-node identity, and the legal autonomous boundary policy. That is
the smallest route to separate fresh versus propagated corner defect while
also resolving the remaining source-identity gap. U1 is closed and does not
authorize L3R-R0.

#### Ripple, High-Frequency, And Smoothing Disposition

For this project, `ripple` means a reference-relative nonphysical oscillatory
error pattern, preferably verified through signed alternation in a
reference-smooth region. `High-frequency energy` is a declared graph or
spectral band diagnostic that can also contain a true front, mesh or boundary
effects, and benign approximation error. `Rollout instability` means global
error growth, loss of raw admissibility, nonfinite or severe amplitude growth,
or termination, with each component reported separately. These terms are not
interchangeable.

The retained bump evidence does not promote high-frequency growth to the cause
of D041 termination. In a post-hoc descriptive all-20 comparison at common call
20, smooth-region graph-high-pass energy ranks the three future H79 failures
with AUC `0.392`; global state error and shock-thickness error each give `0.922`.
The failure nodes themselves have high-pass amplitudes at the
`95.65--98.95%` all-node percentiles, but all three lie on the boundary and the
raw pressure/internal-energy margin is the actual crossing variable. In exact
U1 states, high-pass amplitude remains extreme while nodes re-enter
admissibility. Thus local high-pass is a possible correlated precursor, not a
sufficient event condition or a demonstrated superior predictor. The comparison
has only three failures and was not preregistered; it is descriptive evidence,
not a trained or validated failure detector.

The dynamic conclusion is different and stronger. Corrected D053b shows that
smooth high-pass error energy is `94.75/84.72%` freshly generated by the
one-step map at calls 30/60, with propagated high-pass gains only
`0.189/0.406`, while full-state error is mostly propagated. D054 verifies
negative smooth-neighbor correlation, so this is a genuine oscillatory defect
on the registered cohort. It is not an admissibility-failure predictor because
D044, D060, and all D053b proposals remain raw-admissible through H60. D055 can
legally localize fresh high-pass defect, but D056 cannot correct enough state
error safely, and D060 improves state error while worsening high-pass and front
metrics.

The mentor's ripple hypothesis is therefore retained as a useful diagnostic
question, not a current causal or method conclusion. On the bump family the
signal may be born under teacher forcing and enlarged or redistributed by
recurrence; the exact split is missing. On the dynamic family the registered
smooth ripple is primarily a freshly regenerated one-step representation/map
defect. Neither conclusion transfers across families.

No smoothing or filtering row is selected or executable. If L3R-R0 is approved,
its proposed retained diagnostics should additionally bind the high-pass images
of the exact fresh and propagated terms, signed neighbor correlation, all-node
raw margins, legal proposal-only scores, boundary distance, and separate front
position/strength/thickness. Any later smoothing proposal requires a new human
review and must freeze the field and recurrence location being filtered, the
fixed/learned/shock-aware/state-dependent operator, its legal current-state
information, intervention norm and activation, boundary behavior, raw
admissibility and state error, front strength/thickness/position, and an
anti-blur control. A pressure/density projection or floor is a limiter or
clipping, not smoothing; a lower high-pass score obtained by weakening or
broadening the true shock is a failure.

#### Visual-Phenomenon Kinematics And Boundary-Transient Disposition

The report-only wave-kinematics audit binds dynamic D044/D060 trajectory
`sv_e06_y00` to array SHA-256 values `f659e035...6829bf` and
`8412d74f...e9ede`, and bump U1 trajectory `05` to
`95b806e6...970939`. No checkpoint was loaded or executed, no sealed split was
opened, and no training or intervention was run.

The two right-moving dynamic error traces are distinct Euler characteristic
families, not one unspecified ripple. For the fixed downstream state,
`u_2=1.1133` and `c_2=1.221020`; the convected entropy/contact and right-going
acoustic speeds are therefore `1.1133` and `2.334320`. Characteristic-projected
space-time ridge fits give `1.115/2.330` for D044 and `1.112/2.335` for D060.
Their measured fast/slow ratios are `2.090/2.100`, matching the theoretical
`2.0968`. The pair is seeded by the first exact-input proposal, starts near
`x=0.5`, is visible in a y band away from the vortex, and precedes the
leading-radius/core kinematic shock-arrival times `0.1537/0.1921`. It is best
described as a discontinuity-triggered learned-map defect whose components then
propagate at physical downstream characteristic speeds. Exact vortex
independence still requires a planar-shock/no-vortex control; D053b's aggregate
fresh high-pass result is not silently reassigned to these two particular
ridges.

The upward left-region wave is a different phenomenon. It is already present
in the finite-volume reference. Its pressure-density-normal-velocity ratios
match an upward acoustic mode, while its strongest support and earliest growth
are at the lower-left boundary. The upstream state has
`u_1-c_1=0.11832>0`, so `x=0` is a pure supersonic inflow, but the frozen
reference applies linear x extrapolation plus y symmetry. Reference pressure at
the first lower-left cell rises from `1.00025` at `t=0.08` to `1.02575` at
`t=0.18` and `1.19955` at `t=0.30`; the positive ridge back-extrapolates to
the lower wall at `t=0.2996`. Boundary involvement is verified, but attributing
the full reference transient to the extrapolation rule alone requires a
fixed-inflow reference control.

D044 tracks the upward wave's shape but lags its phase. On the left-region
horizontal profile, D044 at time `t` best matches reference time `t-0.0125`
(IQR `0.0120--0.0140`, median normalized-profile correlation `0.99884`).
The apparent reference/D044 peak-ridge slopes are `1.3820/1.3395`; these are
curved pattern speeds, not local characteristic speeds. The D044 vortex-core
fit reaches `x=0.5` about `0.0041` earlier than the reference fit, so delayed
vortex arrival does not explain the acoustic lag. A boundary-contract mismatch
and learned acoustic phase dispersion are plausible mechanisms, not yet
separated causes.

On the exact D041 bump prefix, the visual statement “error lies to the right of
the front” is correct only after the thin front band is removed. At calls
`20/32`, the downstream side contains `94.10/94.30%` of smooth-side
pressure-error energy and has pressure-error RMS `2.45/2.36x` the upstream
side. Including the front changes the statement: the front band itself contains
`89.11/73.93%` of pressure-error energy. Its median signed x offset is only
`-0.00404/-0.00329`, consistent with the thin signed phase line, but D013's
zero median alignment reduction still rejects a phase-only account.
Downstream pressure-error high-pass neighbor cosine is `-0.251/-0.114` at
calls `20/32`, versus `+0.374/+0.302` upstream, verifying an alternating
downstream ripple morphology on this trajectory. Reconstructed weights remain
diagnostic proxies rather than physical finite-volume measures.

U1's post-failure re-entry remains evidence that a finite local conservative
proposal can cross back into the admissible set without a global blow-up; it is
not evidence of a learned limiter or guaranteed self-correction. Calls after
the frozen prefix use the registered current-source continuation, and the
historical D041 evaluator stopped at its first rejected proposal.

These findings refine the retained diagnostic contract but do not change the
registered run order or authorize a new experiment. If separately reviewed,
the minimum decisive dynamic controls are one exact planar-shock checkpoint
probe for the two characteristic traces and one fixed-supersonic-inflow
reference replay for the left-boundary acoustic transient. L3R-R0 should retain
front-relative upstream/front/downstream metrics and signed neighbor
correlation, but it remains pending human contract approval.

#### Conditional Horizon-Extension Disposition

H79 exhausts the evaluator-bound 80-state bump HDF5 trajectories: frames
`0--79`, `dt=0.025`, 79 stride-1 calls, and physical horizon `1.975`. Under
strict R/D041, the three accepted lengths `25/31/32` are observed failures,
while all 17 H79 completers are administratively right-censored. Their H79
errors are valid endpoints, but later error and survival are unknown. On the
fixed 17-completer cohort, mean proxy error rises from `0.029295` at H40 to
`0.043668` at H79; the descriptive H40--H79 linear fit has slope
`3.42657e-4` per call and `R^2=0.98392`. The dataset endpoint therefore does
not demonstrate a plateau.

This does not authorize extension. The `270*79` eligible training transitions
use current frames only through `78`; frame `79` is target-only and no post-H79
state is observed. The autonomous model has no time input, but post-H79
state/front/boundary support remains unverified. The raw-bundle audit records
one additional `sol_80.vtu`, yet its HDF5 mapping is unbound and one interval
cannot answer the longer-horizon question.

The Trixi PDE, flux, limiter, boundary, polynomial-degree, and `0.025` save
lineage are known only at representative-script level. Exact Julia/Trixi
environment, per-case generator inputs, time-integrator/tolerance settings,
VTU-to-HDF5 converter, `sol_k` mapping, and generation logs are not retained in
the maintained repository or public `cpggnspdes` clone. An identical-contract
longer reference is therefore plausible but not currently reproducible.

After and only after L3R-B0 passes clean H20/H79 validation and its
post-selection audit is frozen, a later human review may authorize one
reference-prefix pilot. It must regenerate an
existing nonsealed case from `t=0`, close every frame `0--79` under a
predeclared dtype-aware state/schema/node/time gate, and measure reference
solver cost. Failure defines a different reference family and stops the route.

If that pilot passes, the smallest useful diagnostic is H159: 160 bound states
through physical time `3.975`, 159 learned calls, and checkpoints
H20/H40/H60/H79/H99/H119/H139/H159. It must combine survival/admissibility,
fixed-cohort state and teacher/propagated/cross error, front geometry,
reference-smooth signed high-pass, boundary strata, and training-support
distance. Stable teacher defect with smooth recurrent growth supports slow
accumulation; stable teacher defect followed by an abrupt propagated/margin
change supports a delayed recurrent basin; a teacher-defect/support jump
supports state-distribution shift. Reference relaxation with learned drift
instead supports fixed-point bias.

Observed storage scaling implies about `17 GiB` peak temporary space for a
20-case from-zero H159 reference, extended HDF5, and raw predictions, with
roughly `6 GiB` retained after verified conversion. Historical neural forward
timing projects to about `175` seconds for 20 complete H159 rollouts, but
reference-solver time is unbound and must be measured by the prefix pilot.
Truth-free post-H79 recurrence may report only qualitative finiteness and
admissibility, never longer-horizon accuracy, capability, or stability.

### L3R-M0 Closure And Next-Stage Contract

The provenance audit and authorized retrieval are recorded in
`MECHANISTIC_DIAGNOSTIC_TRACKER.md`. P/D019 is now bound to exact checkpoint
bytes, 76,634,327 bytes with SHA-256 `31364c48...796b5`, and to exact
preprocessing/evaluation commands and surviving evaluator source; its
collaborator-side training epoch, source, command, optimizer history, and
selection rule remain missing. R/D041's base and continuation best/last files
all close to their recorded hashes, including selected continuation SHA-256
`2bb5ee3c...2964`; exact launch commands, logs, split/normalization sidecars,
processed-data manifest, and already-open output hashes are recovered. Its
dirty runtime source bytes remain unretained. The two backbones have the same
width, depth, `kmax=8`, and `(6,2)` Fourier domain; the residual model has only
640 additional parameters from four node-type channels and one Mach channel.
Capacity is therefore not a fifth attribution block.

The four registered attribution headings remain useful only as qualified
bundles. Data coverage must freeze preprocessing, presentations, normalization,
evaluator, and validation population. Conservative residual coordinates,
normalization/scaling, and the legacy positive-primitive head remain one
learned-map/objective bundle. Node type plus Mach remains one conditioning
bundle. Checkpoint selection must first be isolated by offline rescoring of the
per-epoch stream retained prospectively by L3R-R0; continuation is a separate
training intervention. No historical per-epoch stream is claimed to survive.
This is a contract clarification for later human review, not authorization to
add rows or run a factorial sweep.

This was the minimum next experiment under the 2026-07-26 plan. The later
2026-07-27 human amendment supersedes that dependency with L3R-B0. The retrieval
still does not support a cause for the historical gain, and L3R-B0 must not be
reported as reproduction or attribution evidence.

### Cost And Stop Discipline

L3R-M0 uses zero GPU-hours. Historical elapsed times suggest approximately four
GPU-hours for one bump base-plus-continuation reproduction and roughly two
GPU-hours for one serious dynamic stride-1 run on the observed hardware; these
are planning estimates, not portable runtime claims. Before execution, each
registered row must replace estimates with an AutoDL cap, retained-artifact
cap, and stop condition. L3R-A0 may consume at most four one-seed training
equivalents before a review. No failed gate may be repaired by changing its
threshold, population, evaluator, or seed after results.

### Architecture And Algorithm Direction Disposition

The completed evidence audit retains exactly three program directions. They are
ordered gates, not parallel method branches. Direction 1 is deliberately a
training-contract direction: the provenance matrix does not support assuming
that architecture caused the bump improvement.

#### Direction 1: Reproduce And Attribute The Bump Training Contract

- **Diagnosed mechanism targeted:** the observed P/D019-to-R/D041 difference is
  confounded across data/time coverage, learned state map and objective,
  node-type/Mach conditioning, checkpoint selection, and clean continuation.
  The two backbones have the same width, depth, Fourier modes, and domain
  lengths; the 640-parameter difference is only the five conditioning inputs.
- **Causal prediction:** after clean R0 reproduction, changing one qualified A0
  block while holding the remaining contract fixed will move validation raw
  rollout behavior in the direction of the historical gain if that block is
  causally influential. If no block has a repeatable practical effect, the gain
  remains unattributed rather than becoming an architecture claim.
- **Smallest decisive experiment:** L3R-R0 followed, only after a separate
  review, by the four blockwise L3R-A0 rows already registered. Selection uses
  the historical bump validation population; D041 and the five legacy
  trajectories remain post-selection reproduction populations. No factorial
  combination is allowed.
- **Matched controls:** use the R0 parameterization and the same maximum
  optimizer-step and sample-presentation budget in every learned A0 row. The
  restricted-data row samples its smaller transition support with replacement
  to match presentations. Rows that remove node/Mach information retain dummy
  input channels so parameter count is unchanged. The learned-map/objective row
  retains the same conditioning width. Checkpoint selection is first tested by
  offline rescoring one prospectively retained epoch stream; continuation is a
  separate optimizer-history intervention.
- **Primary and anti-smearing fields:** raw H20/H79 completion, survival, scaled
  proxy state error, same-process teacher/propagated/cross terms, and nodewise
  density/internal-energy/pressure margins are primary. Front position,
  strength, thickness, downstream signed-neighbor oscillation, smooth graph
  high-pass, and boundary-stratified error/margins prevent a gain from being
  credited to shock blur or boundary neglect. Bump weights remain diagnostic
  proxies, not finite-volume conservation measures.
- **Failure and stopping:** any R0 provenance, replay, population, selection, or
  reproduction-gate failure stops A0. A parameter, presentation, normalization,
  evaluator, or selection mismatch invalidates an A0 comparison. No factor is
  retried under a changed seed or threshold, and no holdout result chooses a
  row.
- **Evidence supported by success:** clean reproducibility of R/D041 and
  bounded causal evidence that at most two training-contract blocks influence
  the bump gain.
- **Still unsupported:** internal attribution within the bundled
  conservative-residual/normalization/head change, general superiority of the
  architecture, transfer to the dynamic family, physical conservation, seed
  robustness before confirmation, or sealed performance.

#### Direction 2: Shared Stride-Conditioned Multirate PCNO

- **Diagnosed mechanism targeted:** D044 carries mostly propagated
  shock-supported state error, while its smooth ripple is mostly regenerated by
  the one-step map. D060 halves recurrent calls and improves state error but
  worsens graph high-pass and front-centroid behavior. D061 shows that averaging
  two phase-displaced frozen states does not combine those strengths.
- **Causal prediction:** a single jointly trained map conditioned on physical
  step size can preserve the D060 stride-2 state advantage while using
  stride-1 supervision to reduce the fresh front/smooth defect toward D044. A
  successful result must improve the direct learned map: it cannot arise from
  state blending, clipping, or a post-hoc correction.
- **Smallest decisive experiment:** L3R-MR0 is one serious seed on the dynamic
  `84/24` train/position-OOD-validation split. Train one shared model on a
  balanced stride-1/stride-2 transition mixture. The primary path is 30 raw
  stride-2 calls to physical H60; also report the same checkpoint's 60-call
  stride-1 path and direct-stride-2 versus two-composed-stride-1 consistency.
- **Matched controls:** retain D044/D060 architecture, optimizer, BF16,
  training-only primitive noise, seed, split, raw conservative recurrence, and
  rollout-selection rule. Use exactly `50*1024=51,200` transition
  presentations and the retained D044/D060 count of `50*268=13,400`
  optimizer steps, split equally between strides. Mixed-stride examples must
  share trajectory-homogeneous batches rather than create an extra optimizer
  step per stride. On this fixed Mach-1.1 family, replace the identically
  constant standardized Mach input with normalized physical `Delta t`; do not
  append a channel, so the parameter count remains exactly D044's
  `19,155,720`.
- **Primary and anti-smearing fields:** the registered conjunctive primary gate
  remains 24/24 raw H60 completion, mean physical-volume state error at most
  `1.03x` D060, and six-case smooth-high-pass RMS and front-centroid distance
  at most `1.03x` D044. Shock strength, thickness, symmetric Chamfer,
  vortex-core error, physical-total/reference-boundary mismatch, signed smooth
  neighbor correlation, and the outgoing entropy/contact and acoustic
  characteristic-error traces are anti-smearing and mode-transfer guards.
  Report latency and both teacher-forced and recurrent source terms.
- **Failure and stopping:** stop for any source/split/presentation/parameter
  mismatch, nonfinite training state, or raw recurrent inadmissibility. A
  one-seed miss of any conjunctive gate is a failed MR0, not permission to tune
  the stride mixture, seed, threshold, or rollout schedule. Strength OOD and
  test remain sealed.
- **Evidence supported by success:** at one seed, a parameter- and
  exposure-matched shared step-conditioned map combines the registered
  state/front/high-pass behaviors on this dynamic validation family. L3R-MR1
  must repeat the frozen configuration across three seeds before robustness is
  claimed.
- **Still unsupported:** a universal best stride, timestep or mesh
  generalization, bump transfer, physical conservation, a general ripple cure,
  strength-OOD or test performance, or a generally shock-stable neural
  operator.

#### Direction 3: Conditional Proposal-Sensor-Gated Local Residual Branch

- **Diagnosed mechanism targeted:** this direction exists only if MR0 preserves
  adequate state/front behavior but still fails through localized freshly
  generated smooth high-pass defect. D055 shows that the legal current
  proposal localizes that defect; D056 shows that the same support is not a safe
  post-hoc state-correction oracle.
- **Causal prediction:** an end-to-end trained local branch gated only by the
  current state and raw proposal will reduce teacher-forced fresh smooth
  high-pass energy and signed alternation on its declared support without
  weakening or broadening the shock. A parameter-matched ungated branch should
  not reproduce the conjunction if proposal localization is causal.
- **Smallest decisive experiment:** L3R-L0 is one serious-seed A/B between the
  D055-sensor-gated branch and an otherwise identical ungated branch, both
  jointly trained with the MR0 backbone. It may be drafted only after the exact
  registered MR0 failure selects it; it is not a parallel fallback.
- **Matched controls:** gated and ungated rows have identical parameters,
  initialization, stride mixture, `51,200` presentations, `13,400` optimizer
  steps, optimizer, precision, seed, split, and checkpoint rule. The hard
  support uses no target, future state, reference mask, margin floor, or
  boundary replacement. Correction support and norm are reported rather than
  tuned after inspection.
- **Primary and anti-smearing fields:** primary endpoints are fresh
  teacher-forced smooth-high-pass RMS/energy, signed-neighbor cosine, support
  capture, raw H60 state error, and completion. The complete MR0 front,
  characteristic-mode, vortex, balance, boundary, and admissibility fields are
  no-harm guards; front jump strength and thickness rule out blur.
- **Failure and stopping:** do not start L0 unless the MR0 failure is both
  localized and fresh-defect dominated while state behavior remains adequate.
  Stop for truth leakage, support/parameter/presentation mismatch, raw
  inadmissibility, or any registered state, front, balance, boundary, or
  anti-smearing regression. No support-width, sensor, coefficient, or seed
  sweep follows a miss.
- **Evidence supported by success:** a legal proposal-conditioned, jointly
  trained local branch can reduce the selected one-step dynamic defect beyond a
  same-capacity ungated branch without smearing the validated front.
- **Still unsupported:** correction of propagated shock error, prevention of an
  admissibility event, benefit on the bump family, physical conservation,
  generic smoothing, broad OOD behavior, or seed robustness.

This was the registered sequence when the architecture audit closed:
`L3R-M0 -> L3R-R0 -> L3R-A0 -> L3R-MR0 -> L3R-MR1`. The 2026-07-27 human
amendment prospectively replaced the unexecuted R0/A0 prerequisites with
`L3R-B0P -> L3R-B0 -> human result review`. B0P then failed and B0 remained
unlaunched. The later explicit replacement is now
`L3R-B1P -> conditional L3R-B1 -> human result review`; MR0 remains deferred,
L3R-L0 remains specific-failure conditional, and L3R-F0 remains sealed. U1 shows
that bump inadmissibility need not become a global blow-up; the ripple audit
does not make high-pass a superior failure predictor; the wave audit identifies
front-emitted characteristic families without identifying a responsible
architecture branch; and the horizon audit remains blocked on R0 and reference
parity. None supports inserting smoothing, a limiter, a longer horizon, a
larger backbone, or a front-warp model before reproduction and attribution.

### Algorithm Before Scale And Paper-Comparison Gate

The strong bump checkpoint makes additional optimization and capacity plausible,
but does not establish that the present model is undertrained or that scale is
the cause of its gain. After L3R-R0 and L3R-A0, the preferred order is to review
one bounded, inexpensive algorithmic screen before increasing training
presentations, model capacity, dataset size, or reference horizon. This ordering
is a planning decision only: it creates no executable run and does not displace
L3R-MR0 as the sole registered primary method family.

The later screen is subordinate to Direction 1. Its items are reserve
one-change implementations, not additional program directions or execution
rows. A0 evidence must select the mechanism, and a later review may authorize
at most one item at a time from the following distinct hypotheses:

1. a differentiable two-call objective that trains on raw recurrence, distinct
   from D015's stopped detached depth-one generated-state-exposure continuation;
2. a reference-defined smooth-region high-frequency prediction-error objective,
   distinct from blanket smoothing, total-variation suppression, shock
   attenuation, and the closed D041/D052 post-hoc branch interventions; or
3. an auxiliary primitive-variable and admissibility objective decoded from the
   conservative prediction, without changing conservative recurrence.

Before any such screen is authorized, freeze its optimizer-step and presentation
budget, seed, coefficient, checkpoint rule, validation population, and promotion
threshold. Use the historical bump validation cohort for selection; the D041
holdout and five legacy trajectories remain post-selection reproduction
populations. Do not combine candidates until one passes individually. A
candidate must preserve raw completion, positivity, boundary legality, shock
strength and thickness while improving the declared rollout error or
smooth-region high-frequency field. A short warm start may diagnose mechanism,
but only a clean from-scratch confirmation can support a method or
sample-efficiency claim.

#### Deferred Front-Capture Candidate From The Wave-Kinematics Audit

The D044/D060 visual-phenomenon audit motivates a more targeted hypothesis than
generic smoothing: the first exact-input shock update emits entropy/contact and
right-going acoustic error with nearly correct downstream characteristic
velocities. The supported defect is therefore the amplitude and compatibility
of the front-local update, not an inability to transport every wave family.
This observation does not establish whether the responsible approximation is
spectral, pointwise, differential, objective-level, or data-coverage-level.

After the remaining analysis tasks, L3R-R0, and L3R-A0, a later human review may
substitute the following training-only candidate for one item in the bounded
screen above. It is not a fourth executable row and does not change the current
run order:

1. keep the reproduced architecture, recurrent conservative state, data,
   optimizer, presentation count, seed, checkpoint rule, and evaluator fixed;
2. detect a front band and local normal from the legal teacher-forced current
   state, not from future inference-time truth;
3. project the one-step prediction-minus-target primitive error into local
   normal Euler modes,
   `a_+=(delta p/c^2+rho delta u_n/c)/2`,
   `a_-=(delta p/c^2-rho delta u_n/c)/2`, and
   `a_0=delta rho-delta p/c^2`; and
4. add one preregistered front/downstream weighting of the observed emitted
   `a_+` and `a_0` error to the unchanged base objective. Retain `a_-`, shear,
   and total characteristic error as no-harm diagnostics so improvement cannot
   be obtained by moving defect into an unpenalized family.

Because the quantities being penalized are prediction-minus-target errors, this
objective must not suppress a physical acoustic or entropy wave that is present
in the reference. Its primary mechanistic endpoint is exact-input first-step
outgoing characteristic-error energy. Promotion additionally requires no
worsening of front position, jump strength, thickness, raw admissibility,
global state error, boundary leakage, or the unweighted characteristic modes.
The planar-shock/no-vortex control remains prerequisite evidence for interpreting
the dynamic traces; it is not silently bundled into the training result.

If this one-change A/B improves emission without blurring the front, random
subcell shock position, strength, and angle augmentation may be reviewed as a
separate data intervention to test discrete front-phase coverage. Do not combine
it with the loss pilot. If the training-only candidate fails, the next
architectural hypothesis is transport plus correction,
`U_hat_(t+1)=W(U_t,d_theta)+r_theta`, so translation reuses the current sharp
state rather than resynthesizing it spectrally. On the audited dynamic
finite-volume family only, a later conservative-remap or antisymmetric
face-flux correction may be considered. The bump bundle lacks validated control
volumes and faces, so neither a warp nor a graph-edge correction may be called
physically conservative there.

Uniform smoothing, uniform branch attenuation, primitive clipping, simply
adding Fourier modes, or a long-horizon loss without a first-step front
endpoint are not substitutes for this candidate. Any transport, remap, local
flux, or shock-aware gate requires its own human-reviewed contract and
anti-blur, boundary, admissibility, state-error, and computational-cost guards.

If no candidate passes, scale the reproduced baseline in one dimension at a
time: optimizer presentations first, then capacity or additional training
trajectories, and finally a longer physical forecast horizon backed by longer
reference trajectories. Training loss alone cannot promote a scale point.
Checkpoint comparisons must use the same raw-rollout evaluator and report
optimizer steps, presentations, wall time, parameters, physical horizon, model
stride, and failure accounting.

No statement that PCNO beats the CPGNet result represented in the paper is
allowed until paper dataset, split, checkpoint/evaluator semantics, boundary
policy, horizon, stride, and primitive-variable metric parity are established.
The current conservative scaled proxy relative L2 is not that bridge metric.
Use the release-style per-variable primitive errors only after population and
boundary parity are bound, and report compute and seeds beside accuracy. The
released next-reference boundary row remains oracle-assisted rather than an
autonomous comparator. Novelty review begins only after the promoted algorithm
is frozen; familiar ingredients do not by themselves establish a novel method.

## Unified Claim Register

The RC-01--RC-15 entries below are the complete frozen claim set through
2026-07-23. Evidence populations are part of each claim; they cannot be silently
pooled or widened. Prospective L3R-C1 and L3R-C2 above become supported claims
only if their registered gates pass.

### RC-01. Larger learned macro steps can beat repeated smaller steps

- **Verdict:** `yes, bounded`.
- **Evidence and population:** D031-D036 and D038-D039 form the Line-1 closeout.
  Their primary population is 64 frozen 1D test cases through H96; D034 uses a
  fixed 16-case timing subset. Across three stride-8 seeds, all 192 H96 rollouts
  complete, and the seed-level mean fixed-scale conservative relative-L2 range
  is `0.01767--0.01821`. D033 same-state decomposition and D035/D038 frozen
  evolution show worse truth-state one-call error but eventual on-policy gains.
  D060 adds 24 position-OOD validation cases through physical H60 and the
  six-case D013 subset, with one 2D seed and sealed strength OOD.
- **Allowed wording:** The operating point trades one-call approximation
  difficulty against recurrence count. Lower endpoint state error can coexist
  with a rougher early defect and worse front or high-pass metrics.
- **Missing evidence and non-claim:** The winner varies with horizon and metric.
  No universal best stride, learned CFL limit, timestep transfer, ripple cure,
  or mesh invariance follows. Independent 2D seeds, strength OOD, other
  geometries, and longer uncensored horizons remain missing. Do not pool the
  full-split D031 regime with the earlier midscale D027 regime.

### RC-02. CPGNet stability depends materially on message reach

- **Verdict:** `yes, bounded`.
- **Evidence and population:** On the corrected 1D 384/64/64 split, h128/mp12
  gives one-step relative L2 `0.0334` with 34/64 raw completions, while mp28 gives
  `0.00614` with 64/64; the mp12/h193 and mp28/h85 parameter controls support hop
  coverage rather than width alone. Physical projection does not preserve the
  learned interface update. On the same 20 release-bundle 2D trajectories, the
  arithmetic mean of per-trajectory normal-node primitive RMSE
  `[rho,v1,v2,pres]` is
  `[0.177032,0.073653,0.082204,0.319612]` with released next-reference boundary
  injection, `[0.563023,0.263983,0.242344,1.137027]` for the frozen checkpoint
  under causal nodal boundaries, and `[0.368161,0.157947,0.133379,0.753202]`
  after one legal-boundary training seed.
- **Allowed wording:** Hop coverage is the supported 1D mechanism, not width
  alone. The interface coordinates are functional, nonphysical control
  coordinates; causal boundary training helps without closing the
  legal-versus-oracle gap.
- **Missing evidence and non-claim:** Paper dataset/checkpoint/evaluator identity,
  exact DG boundary replay, validation-based checkpoint selection, seed
  robustness, and grouped geometry holdout remain absent. This is not a
  paper-faithful mechanism, implicit scheme, physical trace, conservation, or
  architecture-transfer result.

### RC-03. D044 is a useful fixed-family 2D macro-map baseline

- **Verdict:** `yes, bounded`.
- **Evidence and population:** One seed completes 24/24 raw H60 position-OOD
  validation rollouts at mean physical-volume state error `0.00834190` and beats
  persistence plus both train-manifold controls on every paired case.
- **Allowed wording:** A conservative-coordinate residual PCNO gives a strong
  raw-rollout baseline on the frozen Mach-1.1 shock-vortex family.
- **Missing evidence and non-claim:** Strength OOD and test are sealed; seed
  uncertainty and other geometry or dataset families are missing. D044 bundles
  coordinates, residual prediction, primitive training noise, and rollout-based
  selection. Its recurrence is not conservative by construction.

### RC-04. D060 improves state error but is not a promoted solver

- **Verdict:** `partial for state; no for the joint claim`.
- **Evidence and population:** On the same 24 position-OOD validation cases,
  D060 completes 24/24 and lowers mean H60 state error to `0.00729369`, or
  `0.87434x` D044, winning every paired case. Direct frame 2 is `1.03246x` two
  composed D044 calls. The conjunctive mechanism fails: six-case high-pass RMS
  is `0.00548836` versus `0.00535199` (`1.02548x`), all-24 endpoint high-pass
  energy is worse in every case, and front-centroid distance is `0.0260282`
  versus `0.0181368` (`1.43511x`). The centroid ratio splits to `2.74982x` for
  `y00` and `0.74853x` for `y08`.
- **Allowed wording:** Halving recurrent calls improves accumulated state L2
  while leaving the fresh high-pass source and some front errors unresolved.
- **Missing evidence and non-claim:** There is one seed, no strength OOD, no
  extra stride, and a failed physical conjunction. Timing remains descriptive.
  Do not call D060 uniformly better, ripple-stable, or promoted.

### RC-05. Serious-PCNO ripple formation is composite

- **Verdict:** Within their respective frozen contracts, the pure
  Fourier/Gibbs, pure pointwise, pure differential, boundary-only, and
  strong-cancellation explanations are `no`.
- **Evidence and population:** D041 evaluates all 20 bump holdouts for boundary
  and failure routing; D042 uses five call-1 bump holdouts, and D043 uses two
  bump trajectories at call 10. D044/D013 and D052-D053 use six position-OOD
  validation trajectories from the separate dynamic-FV family over early,
  middle, and late calls. On the dynamic checkpoint the Fourier Gram matrix is
  well-conditioned and the spectral response is the smoothest branch; D043
  finds no strong paired cancellation on the bump checkpoint, and no repeated
  branch attenuation passes a complete D052 gate.
- **Allowed wording:** Within each frozen evidence regime, the spectral branch
  is globally coupled and comparatively smooth, and no isolated branch explains
  the failure. The mechanism is composite; the bump and dynamic populations
  are not pooled.
- **Missing evidence and non-claim:** Exact layerwise causation and any residual
  aliasing contribution remain unresolved. The dynamic causal result has one
  checkpoint and one model seed on one family; adaptive local bases, different
  global maps, and cross-family transfer remain untested.

### RC-06. Long-horizon state error and smooth ripple have distinct channels

- **Verdict:** `yes, bounded`.
- **Evidence and population:** D053 is a zero-training exact decomposition on six
  D052 validation trajectories at calls 1--60. At calls 30/60, median full-field
  propagated shares are `0.88995/0.89043`, while smooth-high-pass propagated
  shares are `0.15619/0.30733`; the additive identities close to numerical
  precision.
- **Allowed wording:** On this frozen cohort, shock-supported state error is
  carried mainly through recurrence, while smooth-region high-pass error is
  predominantly regenerated by the one-call map.
- **Missing evidence and non-claim:** There is one checkpoint, one shock-bearing
  family, and no matched smooth control. This is not a universal
  neural-operator law.

### RC-07. Native face-target fit is insufficient for a viable conservative update

- **Verdict:** `no for the exact tested objective`.
- **Evidence and population:** D048 trains on four immutable pairs for 3,200
  updates. Fifteen epochs pass every native face-space gate at roughly 10%
  face-field error, but 0/4 decoded states are admissible and relative
  divergence amplification is `1001--1581x`. D049 independently verifies the
  frequency dependence of the discrete decoder.
- **Allowed wording:** Target-space error must be checked after the physical
  discrete decoder. The tested canonical shared-face value objective is
  stopped.
- **Missing evidence and non-claim:** No serious divergence-conditioned,
  projected, or full reference-impulse model was run. This result does not
  reject every flux representation.

### RC-08. A sparse legal locator does not imply a safe correction

- **Verdict:** `yes` for the locator; `no` for the exact correction.
- **Evidence and population:** D055 and D056 are zero-training tests on six
  validation trajectories at calls 1/10/30/60, with strength OOD sealed. D055
  captures median `91.224%/80.212%` of fresh smooth-high-pass energy at calls
  30/60 using `17.304%/16.866%` interior support and passes every gate. D056
  preserves balance to `3.61508e-17`, but state-error reduction is only
  `7.185%/7.010%`, high-pass reduction is `23.763%/-3.592%`, and the
  joint-nonworse count falls from six cases at call 30 to two at call 60.
- **Allowed wording:** The proposal is a credible legal troubled-region locator,
  but the bounded balanced post-hoc correction lacks safe late-horizon
  headroom.
- **Missing evidence and non-claim:** This rejects the registered support-limited
  detail head, not every conservative local correction or an end-to-end global
  map change.

### RC-09. One static joint-objective direction is not geometry-robust

- **Verdict:** `no safe late-OOD continuation for the exact directions`.
- **Evidence and population:** D057 starts from four training pairs; D058 uses
  all 84 training trajectories and evaluates six position-OOD validation cases
  at calls 30/60. Both take zero optimizer steps. D058 closes its Frank-Wolfe
  gap to `5.55e-17`; its eight training-task cosines span
  `0.563997--0.824737`, and joint validation transfer is 6/6 at call 30 but
  3/6 at call 60. At call 60, clean-state cosines are negative for every `y00`
  case and positive for every `y08` case.
- **Allowed wording:** The objectives are jointly descendable on training data,
  but the frozen full-model direction is geometry-conditioned and unsafe as a
  late-OOD continuation.
- **Missing evidence and non-claim:** Finite-step optimization, state-dependent
  weighting, and other task parameterizations remain untested. This is not a
  generic optimizer-failure claim.

### RC-10. Scalar multirate state blending does not combine the parent gains

- **Verdict:** `no for the exact registered blend`.
- **Evidence and population:** D061 combines frozen D044 and D060 trajectories
  for six D013 cases at matched frames 2/10/30/60, producing 24 zero-training
  rows. At H60, the truth-informed scalar oracle reduces state and high-pass
  errors by only `8.14%/10.10%` versus required `10%/20%`; it is jointly
  nonworse in 0/6 and passes anti-smearing in 0/6 cases.
- **Allowed wording:** Complementary parent metrics are not composable by scalar
  state averaging. Target-free disagreement localization remains descriptive.
- **Missing evidence and non-claim:** No shared-backbone row is authorized. The
  failure does not reject every learned, state-dependent multirate method.

### RC-11. The independent-row front chart is not a stable representation

- **Verdict:** `no for the exact chart`.
- **Evidence and population:** D062 is a target-informed zero-training oracle on
  12 rows from six validation trajectories at H30/H60. Its conservative remap
  preserves row/component totals to `2.69e-15`. At H60, front-curve MAE improves
  by `62.82%`, while median state error worsens by `41.88%`, high-pass RMS
  worsens by `828.08%`, and joint nonworse is 0/6. Displacement reaches 10.54
  cells and adjacent-row jumps reach 10.51 cells.
- **Allowed wording:** Comparable pressure-jump branches exchange argmax
  identity; improving the scalar front coordinate can shear the 2D field despite
  exact conservation and admissibility.
- **Missing evidence and non-claim:** This rejects the exact independent-row
  argmax chart. A connected multi-front atlas, level set, or topology-aware
  chart is untested and would require a new zero-training identity and closure
  oracle.

### RC-12. The tested latent family is decoder-capacity limited

- **Verdict:** `yes, bounded`; the route is stopped before transition training.
- **Evidence and population:** L4A-002 is an 800-update matched autoencoder
  smoke, L4A-003 fits each held-out code directly, and L4A-004 is a zero-training
  fixed-Haar capacity test. The common population is 10 position-OOD validation
  states from two trajectories; strength OOD and test remain sealed. L4A-004
  changes mean L2 from `0.005430` to `0.004461`, strength ratio from `0.539` to
  `0.830`, thickness from `2.672x` to `1.554x`, and IoU from `0.390` to `0.500`.
  Only 1/10 states reaches the `0.0021` L2 gate; mean strength remains 17.0%
  weak and thickness 55.4% broad, and it is worse than L4A-003 in L2 on 10/10.
- **Allowed wording:** Discontinuous decoder regularity materially affects front
  fidelity at fixed state size, but the tested fixed chart is insufficient.
- **Missing evidence and non-claim:** No transition, recurrence, geometry
  transfer, or assimilation was tested. Any new chart must pass reconstruction
  and closure before forecast training.

### RC-13. Physical conservation claims are data-contract conditional

- **Verdict:** `yes` for benchmark diagnostics; `no` for a learned conservative
  solver.
- **Evidence and population:** D037 audits the Mach-1.1 family's 250x100 finite
  volumes, oriented faces, normals, boundary accounting, and cumulative
  accepted-substep impulses. D044 predicts state residuals, D060 changes the
  temporal stride, and D062 only post-processes frozen states.
- **Allowed wording:** Physical totals and boundary exchange are valid outcome
  diagnostics on this audited benchmark; D062 preserves its post-hoc row totals
  exactly.
- **Missing evidence and non-claim:** D044/D060 recurrence and D062 are not
  predicted-flux conservative neural solvers. Equal-node bump sums and
  unvalidated bump quadrature do not support physical conservation claims.
  The boundary-matched 2D SharpClaw reference is state-only; D037's
  cycle-impulse field is discretization-specific.

### RC-14. A general shock-stable geometry-aware neural operator is not established

- **Verdict:** `no`.
- **Evidence and population:** All serious 2D method evidence uses one seed and
  one dynamic family; every registered stabilization misses a predeclared joint
  gate. Strength OOD and test remain sealed.
- **Allowed wording:** No general solver or state-of-the-art claim is allowed.
- **Missing evidence and non-claim:** Multi-seed confirmation, new geometries,
  test OOD, and a promoted method are absent.

### RC-15. The completed campaign supports a bounded mechanistic contribution

- **Verdict:** `partial, high confidence`.
- **Evidence and population:** The validated benchmark and baseline, exact
  source decomposition, discrete-decoder audit, systematic geometry strata,
  and failed capacity oracles form one coherent evidence-graded account.
- **Allowed wording:** The contribution maps what works, what fails, and why
  several plausible shock remedies fail necessary conditions within the frozen
  scope.
- **Missing evidence and non-claim:** A positive replacement architecture is
  absent, and discontinuity specificity is not isolated by a matched smooth
  control.

## Limitations And Sealed Evidence

These limitations remain visible and do not become implicit follow-up
permissions:

1. D044 bundles coordinates, residual prediction, primitive training noise, and
   rollout-based selection; no matched 2D training ablation isolates them.
2. D044 and D060 are single-seed serious runs. Casewise repetition is not seed
   robustness.
3. Selected epochs 44 and 34 and their loss curves are provenance, not a
   controlled sample-efficiency comparison.
4. Branch traces and proposal scores are not learned hidden-state evidence for
   stable front topology, future failure, or correction sign.
5. Every current 2D cohort is shock-bearing; no matched smooth problem isolates
   discontinuity specificity.
6. Strength-OOD and test splits remain sealed and cannot strengthen a claim
   after method selection failed.
7. CPG release identity and exact DG boundary replay remain unresolved; the
   supersonic-bump HDF5 does not provide the validated finite-volume geometry
   needed for physical conservation.
8. Validation-only oracles do not license test, strength-OOD, or promoted-method
   claims.
9. Evidence populations with different splits, scales, or controls cannot be
   pooled without a new decision. In particular, D031 and the earlier D027
   regime remain distinct.
10. Reference trajectories and boundary impulses are solver- and
    discretization-specific. The boundary-matched 2D SharpClaw reference records
    states only and exports no face impulses; D037's cycle-impulse field is
    discretization-specific.

## Current Authorized Queue

The frozen-artifact report queue remains authorized:

- plot D044 and D060 state, high-pass, front, and geometry-stratum curves;
- plot D053 propagated-versus-fresh shares beside the D052 branch
  falsification;
- place D048/D049 decoder amplification beside D062 front-identity failure;
- tabulate objective, presentations, selected epoch, intervention, population,
  evidence grade, and non-claim for every result; and
- retain failed implementation attempts as provenance, not scientific trials.

L3R-M0 documentation and the later authorized read-only provenance retrieval
are complete and closed. The resulting matrix binds the recoverable checkpoint,
command, log, processed-data, environment-candidate, evaluator, and output
hashes, and records the collaborator/source identities that remain missing.
That closure alone was not execution authority. Later explicit amendments
separately authorized B0P/B0, B1P/B1, BC0P/BC0, BG0P/BG0, RB0P/RB0, and now
RA0P/RA0.
B0P failed and B0 was not launched; B1P passed and the one frozen B1 process
remains unchanged; BC0P failed and BC0 was not launched; BG0P/BG0 are
done/closed. RB0P passed and RB0 completed but failed its H79 and structural
utility gate; both are now done/closed. RA0P is open and conditionally
authorizes exactly one RA0 arm on the existing train/validation population; no
new or sealed population opens.

## Restart Execution Gate

Line 3 remains scientifically reopened. L3R-BG0P/BG0 and L3R-RB0P/RB0
execution authority is spent and all four rows are closed. The owner's latest
direction authorizes RA0P and, only if every preflight gate passes, one RA0 arm
on zw-gpu. The already-running L3R-B1 process remains unchanged under its own
frozen contract. No retry, coefficient sweep, filtering, smoothing, attribution
sweep, L3R-MR0, L3R-MR1, L3R-L0, L3R-F0, another seed, larger backbone, longer
reference, or new/sealed population is authorized.

Before any executable promotion, bind exact inputs, recurrent coordinates,
predicted quantity, target and loss normalization, topology, boundary policy,
split, seed, sample presentations, selected-checkpoint rule, evaluator,
threshold field, aggregation, direction, source and artifact digests, retained
output, wall-time/GPU cap, and stop condition. A provenance, boundary-audit,
tiny-fit, runtime-projection, or numerical-health failure in L3R-B0P stops
L3R-B0. A failed gate may not be bypassed by treating a new result as
approximately equivalent.

## Mechanism And Method Discipline

Before changing a model, distinguish at least six causal classes:

1. supervised fit or centering;
2. objective and discrete-decoder conditioning;
3. recurrent objective mismatch;
4. representation-manifold failure;
5. geometry-conditioned generalization; and
6. implementation or metric binding.

Every proposal must predeclare its mechanism variable, expected diagnostic
curve, smallest matched control, systematic strata, exact threshold field,
aggregation, population and direction, strongest alternative explanation, seed
scope, and full cost. Put optimized loss beside raw physical rollout, front,
admissibility, and budget metrics. A named gate, attention map, latent, branch,
or physical coordinate needs a readout or intervention showing that the model
uses it.

Apply the following interpretation rules:

- target-space fit is insufficient until evaluated after the physical decoder;
- fewer calls or lower endpoint state error does not imply lower roughness;
- conservation is necessary in some formulations but does not identify a
  correct front representation;
- a coordinate needs an identity and closure contract, not only a lower loss;
- a necessary-condition failure rejects the exact registered branch, not every
  method in its broad family; and
- reported lower high-pass energy is not improvement when it is caused by shock
  blurring or lost wave strength.

Keep one primary solver path and derive redundant outputs where possible. Add
only one dense-supervision family at a time. Prefer hard constraints through
shared face quantities, validated geometry, and explicit boundary accounting;
do not enforce them with whole-sample mean corrections, blanket total-variation
penalties, or independent state and flux heads that can disagree.

## Macro-Flux Contract

The solver-facing quantity is the `cumulative conservative face impulse`

`I_f[n:n+K] = sum_(k in accepted substeps) dt_k Fbar_(f,k)`,

where `Fbar_(f,k)` is the owner-oriented normal numerical flux density averaged
with the solver's actual quadrature for accepted substep `k`. For RK methods,
use the weighted stage flux; for ADER, use the returned space-time-predictor
quadrature flux. A rejected retry contributes zero, and the final accepted
retry or first-order fallback contributes exactly once.

Use `time-averaged face flux` only for `I_f / Delta T_sample`, where
`Delta T_sample = sum_k dt_k` is the actual saved-interval duration. Do not call
this a `mean field`.

The validated 1D artifact stores conservative flux density times time, oriented
owner-to-neighbor on interior faces and outward on boundaries. The discrete
decoder owns face signs and the `1 / dx` factor. In higher dimensions, state
whether face measure is included in `I` or in `D_h`; never apply it twice.

Before training, require serialized closure

`U_(n+K) - U_n = -D_h I[n:n+K]`,

where `D_h` includes the declared orientation, geometry, mass, and boundary
convention. Do not add separate boundary terms when boundary-face impulses are
already in `I`. The frozen 1D gate is maximum float64 pre-serialization closure
below `1e-12`, followed by float32 decoded closure at `rtol=3e-5` and
`atol=3e-6`. A dtype or scale change requires a replacement tolerance declared
before generation.

Temporal averaging can suppress fast temporal oscillations but does not make a
longer-horizon operator intrinsically easier. Larger horizons expand the domain
of dependence, increase unresolved-state sensitivity, and introduce kinks when
shocks cross faces or solver branches change. Treat horizon difficulty as an
empirical axis.

## State-Coordinate Contract

The completed 1D coordinate matrix held the conservative increment fixed and
separated input from loss coordinates:

1. primitive input with primitive loss;
2. conservative input with primitive loss;
3. primitive input with conservative loss;
4. conservative input with conservative loss; and
5. conservative plus primitive features with joint loss only after the matched
   four-way comparison.

The provisional 1D contract is conservative input, conservative loss,
conservative recurrence, and fixed physical scaling. It is benchmark-supported,
not a universal claim that primitive or characteristic features are inferior.
When this contract is used, keep the recurrent state conservative and derive
primitive features without hidden floors. Use fixed physical nondimensional
scales as the main normalization; empirical standardization is only a declared
control.

Every run record must bind `input_coordinates`, `recurrent_coordinates`,
`predicted_quantity`, `loss_coordinates`, target normalization, timestep,
geometry scaling, and whether primitive conversion used a floor. Do not call a
conservative decoder trained through primitive loss a conservative-space model.
D044 bundles several design choices, so its 2D result does not separately
validate this coordinate choice.

## Shared Evaluation And Artifact Contract

Every line must:

- treat one-step fit as an entry gate, never a rollout proxy;
- evaluate raw recurrence without clipping, primitive floors, future-reference
  boundaries, a nearly frozen limiter, or undeclared decode-reencode;
- report admissible completion before oscillation, shock position, strength,
  thickness, smooth-region error, conservation and boundary leakage;
- distinguish mixed-prefix, completed-case, and common-endpoint statistics;
- use grouped geometry or parameter OOD splits where possible;
- select checkpoints by a declared rollout rule; and
- report physical horizon, learned calls, characteristic travel, effective CFL,
  batch-1 latency, and amortized throughput together.

For every rollout table and animation, `Hk` means `k` recurrent calls joining
`k+1` saved states. Completion means `valid_length == k`; mean survival is
`mean(valid_length / k)`. Error curves contain only the admissible prefix, and a
failure marker identifies the first excluded inadmissible proposal rather than
an included loss value. Every animation caption or manifest must identify the
trajectory ID, requested horizon, model step stride, number of encoded frames,
and any visualization-only frame subsampling. A trajectory ID such as `60`
must not be presented as horizon H60.

Retain

`C_eff = max_x(|u| + c) Delta t / h`

as a descriptive case property, not a universal neural stability condition. On
uniform 1D grids, a local architecture may compare physical reach
`r_phys(Delta t)` with architecture propagation radius `r_arch` through

`chi = r_phys(Delta t) / r_arch`.

For arbitrary hyperbolic inputs, `chi > 1` exposes a worst-case causal
obstruction. Do not reduce an irregular 2D graph to one scalar; report
node-, direction-, state-, and time-dependent predecessor coverage in a common
physical metric. A domain-global operator remains global at every timestep.
There is no universal learned CFL claim.

For 2D artifacts, preserve `predicteds`, `targets`, `pos`, `edges`, and
`node_type` when available, plus current or initial state, physical time and
`Delta t`, trajectory/sample mapping, parameters or Mach number, boundary mode,
valid length and failure cause, coordinate convention, checkpoint and
configuration digests, intervention flags, and the exact diagnostic weights.
Physical conservation additionally requires a validated mesh-to-graph map,
volumes, face measures, normals, oriented face connectivity, and boundary
accounting. A reference-flux claim additionally requires cumulative reference
impulses.

Latent artifacts must also bind state size or token contract, hierarchical
reconstruction, closure defect, decoder conditioning, and whether recurrence
includes decode-reencode. Preserve metric definitions, aggregation level,
population and split IDs, seed, normalization, selected checkpoint, source and
artifact digests, complete cost, and any post-hoc oracle access.

## Forecast-Before-Assimilation Boundary

Data assimilation cannot be used to hide an open-loop defect. A latent forecast
must first pass all of these jointly on an authorized population:

- hierarchical reconstruction with admissibility, front position, strength,
  thickness, overshoot, contact behavior, smooth-region error, and budgets;
- approximate Markov closure, including future ambiguity and at most one
  preregistered bounded-history control when ambiguity is material;
- decoder conditioning, including physical perturbation amplification,
  front-position sensitivity, and resolution scaling;
- raw autonomous recurrence without hidden correction or truth reset;
- direct-versus-composed timestep behavior beside truth error and semigroup
  defect;
- transfer across declared front location, strength, interaction, resolution,
  and geometry strata; and
- matched utility against the frozen physical-space baseline, including every
  encode/decode, completion, accuracy, physical diagnostic, state size, latency,
  and throughput.

Lower L2 obtained through broader or weaker fronts fails. Stable latent values
without a physically valid decoded rollout fail. No current candidate passes
these requirements, so latent transition and assimilation remain unauthorized.

A later assimilation-readiness decision must also verify dynamically or
observation-relevant perturbations, for example

`J_D J_G J_E delta U approximately equals J_(Phi_dt) delta U`,

or a declared ensemble analogue, and must report discarded directions, decoded
covariance and growth fidelity, latent observability, analysis-like off-manifold
states, innovation behavior, reparameterization sensitivity, and a localization
contract for spatial latents.

Only after an explicit later authorization may a frozen accepted forecast use
`y = H(D(a)) + eta` with a standard EnKF or LETKF. The required analysis
comparison holds observation operator, ensemble budget, initialization, and
cadence fixed across physical and latent coordinates. A separate end-to-end
comparison may change forecast systems but must be labeled accordingly. Both
must include credible numerical and persistence controls, observation-blackout
continuation, spread-error and innovation calibration, shock uncertainty,
admissibility, conservation, ripple, and online cost. Open-loop evidence remains
separate.

## Retained Records

- [`README.md`](README.md): onboarding and exact active-code inventory.
- [`HANDOFF.md`](HANDOFF.md): derived operational snapshot.
- [`MECHANISTIC_DIAGNOSTIC_TRACKER.md`](MECHANISTIC_DIAGNOSTIC_TRACKER.md):
  immutable run ledger and detailed contracts.
- [`SECTION_1_2_CORRECTED_BASELINES.md`](SECTION_1_2_CORRECTED_BASELINES.md):
  corrected 1D labels, final results, and frozen-run analyzer contract.
- [`CPG_EULER_DATASET_CONTRACT.md`](CPG_EULER_DATASET_CONTRACT.md): audited CPG
  dataset and evaluation contract.
- [`BUMP_300_DATASET_AUDIT.md`](BUMP_300_DATASET_AUDIT.md): supersonic-bump
  data limitations.
- [`CPGGNSPDES_REFERENCE_AUDIT.md`](CPGGNSPDES_REFERENCE_AUDIT.md): reference
  implementation audit.
