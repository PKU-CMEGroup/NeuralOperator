# Handoff

Use this page when transferring work between human collaborators or AI agents.

## Current Objective

Medium-horizon open-loop forecasting remains the gate before data assimilation,
but the project has moved from broad 1D target discovery to four coordinated
lines:

1. close the 1D large-step flow-map question with a bounded stride frontier and
   a small causal-reach and approximation-theory package;
2. validate and explain the 1D/2D CPGNet mechanism, including dataset identity,
   legal boundaries, graph characteristic reach, conditional manifold
   ambiguity, and decoder behavior; and
3. establish a strong geometry-aware 2D residual baseline, diagnose ripple and
   recurrent instability, then promote exactly one mechanism-matched method;
   and
4. test whether discontinuity-aware latent coordinates support sharp, stable
   open-loop recurrence, then conditionally evaluate latent data assimilation
   only after the forecast gates pass.

Line 3 is the flagship algorithmic line, Line 2 is its forensic enabling line,
and Line 1 is a bounded closeout rather than a renewed 1D architecture program.
Line 4 is a cross-cutting representation line that consumes Line 3's shared
dynamic-testbed contract and frozen physical baseline; it does not duplicate
Line 3's physical-space stabilization work. Its learned 2D pilot waits for Line
3 to authorize the dynamic reference as training truth.
The complete ordering, shared evaluation contract, and stop rules are in
`docs/time_dependent_no/RESEARCH_DIRECTION_DECISION.md`. Phase-0 Codex kickoff
prompts are under `docs/time_dependent_no/prompts/`; they authorize read-only
planning only.

## Cross-Line Research Discipline

The four-line retrospective rejects a single undifferentiated optimization
bottleneck diagnosis. Before changing a model, classify the observed failure
as supervised fit/centering, objective or decoder conditioning, recurrent
distribution shift, representation capacity, geometry-conditioned
generalization, or implementation/metric binding. A falling training or one-
step validation loss is not evidence that the recurrent physical objective is
improving. Every new learned row must state the mechanism variable, expected
diagnostic curve, smallest matched control, promotion and kill thresholds,
alternative explanation, case strata, seed scope, and complete cost before it
runs. Thresholds must name the exact stored field, aggregation, population,
and direction before results are visible. If an internal mechanism is named,
verify that the model uses it through a readout or intervention rather than an
architectural label.

Operationally, Lines 1 and 2 remain closed; D060 and D061 close Line 3's
temporal-step and multirate state-blend routes without promotion; and Line 4
remains stopped before serious representation training. D061 misses its state,
ripple, joint-nonworse, and anti-smearing gates despite a positive target-free
disagreement-localization readout. D062 then closes the only front-chart
exception: exact row-total conservation and `62.82%` front-coordinate
improvement coexist with `41.88%` worse state error, `828.08%` worse
high-pass RMS, and 0/6 jointly nonworse H60 cases. The independent row argmax
switches between comparable jump branches under stronger shock--vortex
interaction, so the target-informed warp improves its own scalar coordinate
while shearing the physical field.

No shared-backbone multirate fit, front-chart training, stride 4,
optimizer/loss sweep, new latent transition, test access, data assimilation,
or successor method experiment is authorized. The frozen cross-line causal
synthesis is now complete in `RESEARCH_DIRECTION_DECISION.md`. It grades the
mechanism evidence, records each objective and intervention, preserves the
systematic failure strata, and separates transferable process findings from
architecture- or dataset-specific results.

There is no automatic experiment left in the queue. The next action requires
an explicit research-direction decision: close the current campaign around the
bounded mechanism results, or separately preregister a zero-training capacity
oracle for a discontinuous representation that defines connected front
identity and transverse regularity. Such an oracle would still need to pass
reconstruction and closure before any learned encoder, transition, or filter
is considered.

The 2026-07-23 result-to-claim gate now records the overall verdict as
`partial`, with high confidence. The new-solver claim is unsupported; the
fixed-family residual-baseline and mechanistic-decomposition claim is supported
within its one-seed, validation-only scope. The current authorized queue is
report-only: D044/D060 horizon and geometry strata, D052/D053 branch and error-
source evidence, D048/D049 decoder conditioning, D062 front identity, and a
compact objective/presentation/evidence/non-claim table. Use only frozen local
artifacts. Do not execute checkpoints, open sealed strength-OOD or test cases,
or turn the explicitly listed evidence gaps into experiments without a new
direction decision.

## Line 4 Current Status

Line 4's completed zero-training 1D preflight remains a negative mechanism
result: rank-43 POD fails the front hierarchy, the truth-derived oracle chart
does not improve closure over POD, a fixed-size history remains materially
useful, and current-state front speeds do not repair it. The existing full-rank
oracle remap also fails state, front-retention, front-precision, and thickness
gates in its favorable 243-snapshot/64-trajectory cohort. Do not build a
learned front encoder or a replacement remap from this result.

Line 3 has now frozen the explicit handoff as
`line3_to_line4_handoff_s20260718_20260721b.json`, schema
`line3_to_line4_physical_baseline_handoff_v1`, status `frozen`, SHA-256
beginning `4b56baef`. The finalizer verifies complete training/evaluation data-
contract equality, reconstructs the exact 135-case source-artifact mapping from
the passed family audit, and binds that mapping to the shard contract before
writing the report. It also binds the selected checkpoint/configuration, raw
physical rollout hierarchy, failure causes, cost, interventions, D013, and the
Line-2 D014 interface.

The machine-readable flags are
`line4_training_truth_authorized = true`, bounded strictly to the audited
Mach-1.1 shock--vortex family and frozen 250x100/time contract, and
`line4_front_candidate_available = false`, because D013 selected no mechanism
branch and Line 3 produced no validated front factorization or conservative
remap. `line4_transition_training_authorized` remains false. Line 4 may use the
dynamic states only for its next representation/closure gate; neither latent
transition training nor assimilation is thereby authorized.

L4A-002 is now authorized as that representation-only gate. A read-only audit
of the exact SHA-256 `4b56baef...` handoff confirms nonempty aggregates, all 24
front/smooth hierarchy rows, grouped parameter summaries, cost, characteristic
travel, raw recurrence, and an all-false intervention ledger. The bounded
comparison is identity, matched rank-5000 physical-volume POD, one generic
25x10x20 spatial-token autoencoder, and one parameter-matched spatial-token
candidate with four exact coarse conservative moments plus 16 learned residual
channels per token. It contains no front variable or front remap and therefore
does not manufacture the unavailable Line-3 candidate.

Only two tiny-fit smokes and two one-seed representation jobs are authorized,
with at most 24 GPU-hours total. The 27 strength-OOD trajectories remain sealed.
No latent transition, decoded recurrence, test evaluation, joint fine-tuning,
or filtering is authorized by L4A-002.

L4A-002 has now completed its first actual engineering smoke and is stopped
before rank-5000 POD or serious representation training. The strengthened
preflight hashed all 78 files in the staged four-train/two-validation cohort,
loaded both endpoints, and confirmed that no test array was present or read.
The earlier real-shard integration attempt exposed an `[N,1]` physical-measure
shape omission and stopped before model construction and zero optimizer steps;
the corrected contract has focused CPU coverage.

On the 10 matched validation reconstruction rows, the conservative-moment
candidate lowers mean relative L2 from `0.007605` to `0.005430` and wins all 10
paired rows. It also improves front IoU from `0.283` to `0.390` and makes token
and global budgets numerically exact. Those relative gains do not pass the
physical gate: mean shock strength is only `0.539` of truth and mean thickness
is `2.672x` truth, versus `0.463` and `3.540x` for the generic control. Both
also exceed the preregistered `0.0021` reconstruction ceiling. The structured
row's `38.6%` lower smooth-region high-pass energy must not be called ripple
reduction because the same decoded states have materially blurred fronts.

The structured code reduces the generic conditional-future ambiguity proxy by
only `5.74%`, not the required 20%; adding one prior code changes the proxy by
less than 1% for either row. Its doubled-query-resolution decoder gain ratio is
`1.002`, so resolution sensitivity passes this smoke but cannot compensate for
failed sharpness. The optimization trace continues to lower L2 while strength
falls and thickness remains above `2.6x`, so do not spend the second smoke on a
longer identical run. The current smooth spatial-token decoder/loss contract is
the failed causal component. Any remaining representation diagnostic must be
separately preregistered, use the same frozen train/validation data, and remain
explicitly non-forecast evidence. Transition training, test access, and Line 4B
remain blocked.

The separately authorized L4A-003 frozen-decoder code-reachability oracle has
also completed on those same 10 rows. Holding the checkpoint, decoder, and four
conservative moment channels fixed while fitting each state's 16 free channels
lowers mean relative L2 from `0.005430` to `0.003226`, but nine of 10 rows still
miss `0.0021`; mean shock strength worsens from `0.539` to `0.490`, and mean
thickness remains `2.109x` truth. All fitted codes are outside the one-training-
scale neighborhood (RMS `1.528--4.419`), and maximum overshoot increases. The
registered outcome is `decoder_manifold_rejected`; encoder remediation is
ineligible. The complete summary SHA-256 is
`e9577b437824069ff0db9dc28b9072368754eac2ba14453ef4039a41083a798a`.
Stop this smooth point-decoder/state-L2 family before serious representation
training. This is a bounded capacity result, not autonomous forecast evidence
or a universal claim against latent coordinates. Any new sharp-front
representation requires separate authorization and a capacity preflight;
transition training, test access, and Line 4B remain blocked.

L4A-004 is now registered as one CPU-only capacity preflight of a conservative
local-Haar chart. It retains the 25x10x20, 5,000-scalar state and four exact
token means, replacing only the smooth point basis by 16 training-selected
discontinuous detail atoms per token. Selection uses the same four training
trajectories and frames 0/15/30/45/60; validation uses the same two trajectories
and five frames as L4A-002/003. The L4A-003 fitted smooth decoder is the strongest
matched capacity control. No front fit, per-state optimization, test access,
transition, rollout, or analysis is present. A complete physical-gate pass may
authorize only a separately reviewed closure preflight; any failure stops this
exact Haar chart without a level, atom-count, or dictionary sweep.

L4A-004 has completed and is classified `local_haar_capacity_rejected`. The
preflight rehashed all 78 arrays, bound bitwise-identical L4A-003 targets, used
only the 20 declared training states, and read no test array. Relative to the
amortized smooth encoder, Haar improves mean L2 `17.9%` on 10/10 states, raises
strength from `0.539` to `0.830`, lowers thickness from `2.672x` to `1.554x`,
and raises IoU from `0.390` to `0.500`; admissibility, overshoot, token moments,
and global budgets pass. It still fails the absolute capability: mean L2 is
`0.004461`, only one of 10 states reaches `0.0021`, strength is `17.0%` weak,
and thickness is `55.4%` broad. It is worse in L2 than the privileged L4A-003
fitted code on 10/10 states. Summary SHA-256 begins `f91b78d5`.

This is evidence that discontinuous decoder regularity helps front fidelity but
is insufficient in this fixed 16-detail chart. Stop it without a dictionary or
dimension sweep. No closure preflight, serious representation training,
transition, test access, or Line 4B is authorized. Any adaptive-support or other
discontinuous representation would be a new hypothesis, not a retry.

Line 3 continues to own physical-space stabilization and conservative
remapping; Line 4 owns later coordinate learning, latent propagation, and
latent analysis. Line 4B remains blocked until a representation passes
reconstruction/closure and its raw open-loop forecast passes the shared
promotion gate.

## Line 3 Current Gate

The strong 2D baseline is the selected clean 19,155,720-parameter
conservative-residual PCNO with legal `model_all_nodes` boundaries and raw
recurrence. Its official all-20 held-out evaluation has mean one-step proxy
relative L2 `0.006663`, 20/20 admissible H20 completion at mean error `0.022257`,
and 17/20 H79 completion with mean survival `0.905696`. The exact checkpoint
SHA-256 begins `2bb5ee3c`. Reconstructed vertex weights are diagnostic proxies,
not validated physical control volumes, so this row supports no physical
conservation or flux claim.

The D013/D041 closeout corrects the earlier five-case route. Error is born near
the shock/front and smooth-region graph high-pass error then grows recurrently.
The PCNO basis is ill-conditioned and leaky, but an occupied-domain repair did
not improve the best mass-orthogonal shock projection enough to establish a
pure Gibbs cause. More decisively, the exact frozen `0.75` pointwise-tail
counterfactual worsens one-step error `3.74x`, reduces H79 completion from 17/20
to 13/20, and worsens common-case H79 state error `7.21x`. Reject pointwise
attenuation and any gain sweep; the learned local and global branches are
functionally coupled.

D043 closes the narrower cancellation question on the same official checkpoint.
For trajectories 16 and 60 at call 10, all 16 paired error-response
layer/proxy energy identities are valid, cancellation ranges only from
`-0.1053` to `0.00430`, and every row is below the `0.05` falsification
threshold. The pointwise response is still roughest in all four layers and
dominates RMS response in three, so preserve the learned full branch
composition rather than claiming protective cancellation. D042 separately
rejects one fixed 289-column geometry-only FPS/Wendland-C2 local span: it
reduces smooth-region high-pass projection error to `0.474/0.477x` the Fourier
control but leaves residual RMSE at `0.989/0.982x` and fails all ten decoded
anti-smearing/admissibility rows. Do not sweep that basis family from this
result; adaptive or learned discontinuity-aware local bases remain untested.

Line 2's full causal nodal policy is informative but not a repair: it raises
completion to 19/20 while its H20 median state, thickness, and strength error
ratios are `1.159`, `1.227`, and `1.112`, so it fails the accuracy and
anti-smearing gate. The narrower fixed-freestream-inflow policy keeps 17/20
completion, improves mean survival from `0.905696` to `0.925949`, changes the
common-completer H79 state error by a mean factor `0.99663`, and delays both
baseline inflow-triggered failures by 16 calls. It passes the tight boundary-
contract repair gate, authorizing fixed inflow for the next aligned serious
PCNO training/evaluation contract only. It does not authorize a stabilization
method, and the frozen row remains a checkpoint-mismatch sensitivity.

Do not train a local corrector on the bump bundle. It lacks the validated
volumes and oriented faces required by the conservation-constrained oracle
gate. D015 already rejected a full generated-exposure confirmation and weight
sweep; the clean-noise continuation was also worse than the selected clean
checkpoint. Do not spend more runs on CPGNet architecture, approximate
wall/outflow replay, pointwise gains, exposure, or noise.

D037's benchmark and data contracts are closed. The regenerated 250x100,
500x200, and 1000x400 WENO5-HLLC-SSPRK3 references use certified conservative
cell averages, including exact splitting of shock-crossing cells and tensor
Gauss--Legendre quadrature. They reach `t=0.6` in 822, 1642, and 3279 accepted
steps with zero rejected attempts or face-state fallbacks; maximum interval
finite-volume closure error is `9.38609e-13`. Final-state, time-mean-state, and
centerline-density successive ratios are `0.484166`, `0.481540`, and
`0.397123`. The artifacts contain physical cell volumes, face measures, unit
normals, oriented connectivity, and cumulative accepted-substep face impulses
on the common 250x100 mesh.

The authoritative ignored audit is
`shock_vortex_fv_convergence_cellavg_sharpclaw_matchedbc_20260720c`. Its schema
is `shock_vortex_fv_convergence_audit_v3`, status is
`benchmark_contract_closed`, all checks pass, and both benchmark and direct
reference-impulse closure booleans are true.

After conservatively restricting the medium/fine states and impulses to the
common 250x100 mesh, aggregate full-face-vector, divergence-active, cycle, and
boundary impulse-error contraction ratios are `0.463428`, `0.463362`,
`0.463482`, and `0.464003`; every componentwise ratio is at most `0.464399`.
Maximum decomposition
reconstruction, cycle-divergence, weighted-orthogonality, and decoded-transition
residuals are `2.22363e-17`, `2.71952e-9`, `3.29544e-15`, and `1.31988e-17`.
The decomposition solve tolerance is `1e-11`, while the unchanged cycle gate is
`1e-8`. On x boundaries all four components must contract; on reflecting y
walls only y momentum is active, while mass, x momentum, and energy errors and
actual primary exchanges must be at most `1e-14` absolutely.

The independent row is pinned Clawpack 5.9.0 build `py311h3d4ca6a_1`, using
`pyclaw.SharpClawSolver2D`, fifth-order WENO, SSP33, the four-wave Euler Riemann
solver, custom linear primitive-variable x extrapolation, and reflecting y
walls. Its ignored C artifact directory retains the copied conda record with
SHA-256
`7f81a8de30bfc8b33ba2889582477cc0748e27098470efc7b13003812d684161` and the
explicit environment lock with SHA-256
`c91f94bd962d77c20858c253c9b0c5f245e207214a77b71d0e2203374586df`; that lock
hash is not embedded in `summary.json`. It
matches the primary initial state to `3.18165e-16`. Final and
time-mean component-normalized state errors are `0.0315534 < 0.0466208` and
`0.0156538 < 0.0233192`; final shock-position difference is zero and vortex-core
density relative difference is `0.00215749`. This closes one frozen
boundary-matched state-agreement envelope. SharpClaw is explicitly state-only
and provides no reference face impulses.

Preserve
`shock_vortex_fv_convergence_cellavg_sharpclaw_matchedbc_20260720b`, schema
`shock_vortex_fv_convergence_audit_v2`, as superseded fail-closed provenance.
Its `failed_convergence` status came from exact saved-time/config equality at
floating-point tick differences no larger than `5.55e-17`, an LSMR solve
tolerance of `1e-10` that was insufficient for the unchanged `1e-8` cycle gate,
and relative contraction applied to analytically zero reflecting-wall
components. Its state and SharpClaw-envelope checks passed, as did its
full/divergence-active/cycle/boundary impulse ratios
`0.463428/0.463362/0.463482/0.464003`; it is not evidence of physical
nonconvergence or independent-solver disagreement. The earlier pinned
[Pyro](https://github.com/python-hydro/pyro2) 4.5.0 CTU/HLLC mismatch remains a
historical low-order comparison, not the promotion decision.

The narrow dynamic family is now frozen as
`shock_vortex_eps_y_15x9_fine1000_to_250_dt001_v1`: 135 trajectories split by
group into 84 train, 24 validation, and 27 strength-OOD test trajectories. The
family-manifest digest is
`150c589af9c7291674f502dfe30ca77930d9429fb2115b3f2a0f22042a08c69e`; the
full artifact audit passes 135/135 rows, and the prepared physical-volume PCNO
shards have manifest digest
`f8d228ae3c6e08fe6697f37df21476fe621abc354d0b0a6eed2a623ed2b9f96c`.
The seed-20260718 19,155,720-parameter conservative-variable residual PCNO is
complete. Raw H60 validation selected epoch 44 rather than the final epoch;
the selected checkpoint SHA-256 begins `c5e468c7`. On all 24 validation
trajectories, the physical-volume state error is `0.00028009` at call 1 and
`0.00834190` at H60, with 24/24 finite and admissible completion and no future-
reference boundary, floor, clip, smoothing, or limiter. H60 errors for
persistence, nearest training trajectory, and four-neighbor train-only
parameter/time interpolation are `0.0732949`, `0.0231132`, and `0.0231196`;
PCNO wins every paired trajectory. This establishes a useful dynamic global
baseline rather than a lookup surrogate.

The PCNO does not predict face exchange. Its H60 normalized physical-total
error relative to reference boundary exchange is `0.0513185`, worse than the
two train-manifold controls, so retained conserved totals are outcome metrics,
not guaranteed structure. No neural flux or physical-conservation claim follows
from the conservative state coordinates.

Frozen D013 uses the predeclared six-case cohort and consumes Line 2 D014.
Every case completes H60, but all four predeclared mechanism screens are false
and the classification is `unresolved`. The actual PCNO Fourier Gram condition
number is `1.000003`, its off-diagonal Frobenius ratio is `4.80e-7`, and mass-
orthogonal replay changes reconstruction RMSE by essentially one, which argues
against quadrature leakage as the leading cause. The spectral response is much
smoother than the local responses. Pointwise has the largest paired RMS response
in all four layers, differential is roughest in three, so the repeated selector
returns `composite_or_unresolved`; strong paired branch cancellation is also
falsified. Smooth high-pass error grows strongly with physical interaction time
under both teacher forcing and rollout, while the late rollout/teacher ratio is
only `1.0458` and perturbation gain is `0.9419`. The evidence therefore does not
support pure Gibbs aliasing, a single local-branch culprit, mesh conditioning,
front displacement, or clean-teacher recurrent amplification.

The predeclared truth-informed correction oracle is also complete on all 18
six-case/call-10/30/60 rows. Every row is admissible, internally conservative,
within 20% face support and 10% update caps, and passes anti-smearing; nevertheless
median state-error and smooth-high-pass reductions are only `0.05135` and
`0.04164`, below the `0.15/0.20` gates. The decision is
`reject_learned_local_correction_branch`. Do not relax the caps, expand support,
or train the local corrector. The front screen is false, so do not manufacture
a front route. Keep the 27-case strength-OOD test split sealed while the next
target-representation question is predeclared.

D045 has now consumed that first structured-target authorization and stopped at
the tiny-fit gate. The 19,210,028-parameter state-loss-only shared-face model
uses the unchanged full 250x100 mesh, exact owner/neighbor cancellation for
interior impulses, current-state-only boundary heads, and no reference-impulse
labels. The implementation validates the physical face geometry and exact
mesh-to-graph reconstruction before training. Its corrected smoke passes and
the decoder closes to numerical precision against its own predicted boundary
exchange.

The four immutable training pairs improve under both allowed attempts, but the
joint gate does not close. The 800-update attempt reaches best mean state
relative error `0.00166965` and loss ratio `0.0730244`. Increasing only the
presentations of those same four pairs gives 3,200 updates and best values
`0.000955675` and `0.0254377`: the absolute error gate passes, but the required
100-fold loss reduction does not. Full-bfloat16, float32 face heads, and full-
float32 checkpoint replays give nearly the same error, so mixed precision is
not the measured floor. Do not launch the serious shared-face row, relax the
registered gate, downsample a serious model, or inspect the test split.

This is a bounded optimization/identifiability failure of the current direct
state-loss-only face parameterization, not a rejection of all face or flux
targets. It supports no accurate physical-boundary-exchange claim. A later
divergence-active/minimum-norm target must remain a separately authorized row,
starting with a zero-training projection preflight rather than a supervision
sweep. The authoritative Line-4 handoff and its three flags are unchanged.

D046 has completed its zero-training preflight on the immutable 24-row train/
position-OOD-validation cohort and failed the sole canonical/reference-state
closure gate. The corrected state-plus-boundary construction reaches maximum
closure `4.94497e-7` against the frozen `1e-8` threshold. Reference closure
(`1.23427e-12`), float32-shard closure (`9.36905e-6`), independent face-field
agreement (`1.61785e-8`), compatibility (`2.94020e-12`), cycle divergence
(`5.18524e-10`), weighted-norm ratio (`0.999440`), reflecting-wall accounting
(`3.10460e-20`), provenance, finiteness, and solver-status checks all pass.
Reference cycle energy is only `0.1129--0.6691%` of interior weighted energy.

This is a proof-level numerical-projector failure, not evidence that the
validated finite-volume geometry or accepted impulses are inconsistent. The
independently constructed fields agree to `1.61785e-8`, and the canonical
field already decodes the actual float32 training shards well inside their
`1e-4` contract. Nevertheless, the frozen gate controls promotion: do not
launch the divergence-active tiny fit, relax the tolerance, retry LSMR, inspect
the test split, or promote a physical-conservation claim. A direct fixed-mesh
factorization would require a separate preregistration and would not
automatically authorize training. The Line-4 training-truth, front-candidate,
and transition-training flags remain exactly as frozen by D044.

D047 is authorized as one zero-training direct-projector check of that specific
numerical diagnosis. It reuses the identical 24 D046 train/position-OOD-
validation rows and every existing gate. A deterministic float64 weighted
graph-Laplacian factorization may explicitly project and report the tiny
componentwise compatibility residual, anchor the lowest cell as a potential
gauge, and reuse one factor per connected component. Jitter, regularization,
refinement, alternate anchors, solver/tolerance sweeps, test access, and model
training are outside D047. Its additional compatibility-projection and reduced-
solve residual gates are both `1e-10`. Passing permits only a separately
registered four-pair tiny fit; failure kills this target route. The Line-4
training-truth, front-candidate, and transition-training flags remain unchanged.

D047 has now passed every gate on the exact 24-row cohort. Direct canonical
closure is `1.09848e-9--3.40147e-9`, a `144.97--157.64x` matched improvement
over D046, while independent field disagreement is at most `8.42560e-10` and
the direct compatibility-projection/reduced-solve residuals are at most
`1.85905e-14/1.13351e-12`. One 1,130,356-nonzero factor serves all rows. This
confirms the numerical-projector diagnosis; it is not a learned-flux or
conservation result.

D048 has consumed that sole training authorization and failed. The artifact
`pcno_canonical_face_tinyfit_s20260718_20260722a` completes the exact 3,200-
update, four-pair, full-resolution contract with 19,210,028 parameters. Label
closure and every provenance gate pass; the summary/metrics SHA-256 digests are
`cbfa1e90993e.../0652829297ce...`, and no test array is opened.

Fifteen epochs satisfy all native face-space thresholds. Minimum loss ratio,
full/interior error, and boundary error are `0.00707672`,
`0.0950046/0.0953408`, and `0.0201020`. Nevertheless, no epoch is admissible or
reaches decoded-state error `0.001`; best/final errors are
`0.474958/0.500998`, versus `0.00628209` persistence. Final replay shows only
`0.09772--0.09877` face error but `98.91--155.90x` cell-increment error and
`1001--1581x` relative divergence amplification, with strongly negative
density, internal energy, and pressure. The descriptive replay JSON has
SHA-256 `c71bbff63265...`.

Classify D048 as a face-space-fit/discrete-divergence-conditioning failure, not
as bad labels, failed face optimization, proven Fourier Gibbs, or a general
failure of conservative neural solvers. Stop this exact canonical face-value
objective: no retry, state-loss mixture, weight tuning, downsampling, serious
row, test access, or conservation claim. Keep D044 as the flagship residual
PCNO. A later face-form idea requires a new zero-training `H(div)` conditioning
or deterministic residual-to-face lifting contract before authorization. The
Line-4 training-truth, front-candidate, and transition-training flags remain
unchanged.

D049 now closes the required zero-training conditioning audit. Its physical
rows stay on the validated 250x100 dynamic mesh; 500x200/1000x400 rows are
geometry-only algebraic controls. Low/mid/high decoded gains are
`0.34903/0.97641/2.25444` at median, every seed is ordered, the minimum
high/low ratio is `6.3866`, cycle gain is at most `1.24e-10`, and all six gates
pass. Fixed-`Delta t` flux top gain doubles under each analytic 2x refinement;
this is not native fine-grid shock evidence. D049 supports the discrete-
divergence-conditioning mechanism but still does not identify a Fourier,
pointwise, differential, or recurrent architectural source.

D050 is the only authorized next action: a zero-training lift of the frozen
D044 state residual on the six D013 validation trajectories at calls
`1/10/30/60`. Its legal row uses only the predicted cumulative residual and a
minimum-`W_f^-1` x-boundary allocation; that boundary is algebraic, not
physical. Its separate accepted-reference-boundary row is oracle headroom and
must never be reported as autonomous. Exact structural and headroom gates are
in `RESEARCH_DIRECTION_DECISION.md`. No checkpoint execution, model training,
test access, local-basis/face-loss sweep, or Line-4 flag change is authorized.

D050 has now stopped. Its legal algebraic lift reconstructs the frozen D044
state to `5.37e-12`, reports target residual at most `7.58e-11`, keeps all 24
endpoints admissible, and uses no future reference. The artifact nevertheless
fails because `oracle_lift_closure` was wired to the intentional projection
away from the incompatible raw D044 total (`0.07610` maximum), not to the
compatible direct-system residual. Preserve this as a metric-binding failure;
the corrected code must not be used to relabel or rerun D050.

The separate saved oracle headroom is also insufficient: future-reference
boundary impulses remove essentially all H60 budget defect but improve median
state error only `7.41%` versus the frozen `15%` gate, with a `5.09--9.54%`
six-case range. Anti-smearing and correction-norm gates pass. Therefore no
legal-boundary follow-up, learned face/boundary row, or test access is
authorized. D044 and every Line-4 coordination flag remain unchanged.

D051 is complete in `pcno_shock_vortex_coarse_cfd_d051_20260722a`. The fixed
pilot selects `25x10` and `250x100`; no resolution was added. The 250x100 CFD
row is H60 error matched (`0.00808583` versus D044 `0.00834190`), but the paired
case interval includes zero. CFD is better on front position and vortex core;
PCNO is better on shock strength, shock thickness, and smooth-region scaled
error, while CFD has less smooth high-pass energy. Treat this as a ripple--
dissipation tradeoff, not uniform superiority.

Strict timing and conservation wording both fail. PCNO timing p95/median is
`11.70` versus `2`; 250x100 CFD median core time is `13.53` s versus PCNO's
`0.449` s calibrated H60 median estimate and `2.225` s frozen observed median.
The maximum float32 own-boundary residual is `2.99e-4` versus `5e-5`. Do not
retune timing, precision, balance tolerance, or add a grid. The result supports
matched-error and descriptive implementation-cost evidence only. It makes no
production-CFD or physical-conservation claim, leaves test sealed, keeps D044
as the flagship residual baseline, and changes no Line-4 flag.


D052 is complete in
`pcno_shock_vortex_branch_sensitivity_d052_20260722a`. All 48 frozen
case/call/source rows and 576 one-sided layer/branch interventions complete;
exact replay is within `4.76837e-7`, and every output is admissible. Spectral,
pointwise, and differential each have zero passing cases under both teacher
and rollout inputs. At H60, rollout state error is `8.95x` teacher error, but
smooth high-pass error is only `1.095x`; no branch shows a rollout-only
high-band explosion. The selector is `composite_or_unresolved` and the route is
`stop_line3_architecture_tinkering`. Do not run gain, basis, smoothing, or
local-branch sweeps.

D053 is the only authorized next Line-3 action. It is a zero-training exact
rollout-error decomposition on the same six validation trajectories and all 60
calls: propagated input error `G(uhat_t)-G(u_t)` plus fresh teacher defect
`G(u_t)-u_(t+1)`. It must use validated cell-volume weights, report full and
smooth-high-pass identities/cross terms, keep all rows raw and admissible, and
pass `1e-6` closure. Repeated late propagated shares `>=0.65` may route one
short generated-state-exposure capacity test; shares `<=0.35` route target or
representation diagnosis; a mixed/split result authorizes no learned row. The
27 strength-OOD cases and all Line-4 flags remain unchanged.


D053 attempt A is preserved as a replay-binding failure, not interpreted: its
360 identities close to `4.91408e-15`, but comparing a saved D052 output with a
fresh identical-input GPU replay creates `1.65e-7--1.84e-7` absolute noise and
fails the call-1 zero-share gate. One corrected attempt B is authorized with
both map outputs evaluated in the same process and a new `1e-5` source-replay
integrity gate. All scientific thresholds, saved currents, validation cases,
and stop rules are unchanged; no further retry is allowed.


Corrected D053 attempt B passes all gates on 360/360 rows. Identity/replay
errors are at most `4.93441e-15/4.76837e-7`, call-1 propagation is zero, and all
outputs are admissible. At H30/H60, full propagated shares are
`0.88995/0.89043` with near-unit gains, while smooth-high-pass propagated shares
are only `0.15619/0.30733` with gains `0.1889/0.4056`. About 91% of full error
energy is propagated, but `94.75%/84.72%` of smooth-high-pass energy is fresh
teacher defect. The route is `mixed_or_split -> no_learned_method`.

Do not spend GPU on gain, basis, smoothing, local-corrector, generated-exposure,
seed-confirmation, or strength-OOD rows under the current contract. D044 remains
the serious residual baseline. Further Line-3 learned work requires an explicit
new target/representation hypothesis and a zero-training falsifier; all Line-4
flags remain unchanged.

D054 is now the only authorized next Line-3 action. It is a zero-training
fresh-defect locality falsifier, not another correction or basis sweep. Reuse
the exact six D052/D053 validation trajectories for all 60 calls and keep
strength OOD sealed. A truth-selected 20% smooth-node support must capture at
least 70% of fresh smooth-high-pass energy, and a current-state-only four-hop
top-decile pressure-jump halo must capture at least 50% while covering at most
25% of the interior, on at least five of six cases at both calls 30 and 60.
All 360 rows, raw admissibility, masks, and D053 norm replay are mandatory.
Only a joint pass may authorize writing one matched tiny-fit contract; it does
not itself authorize training. Do not vary the support, halo, sensor, or gates
after seeing the result.

D054 is complete in
`pcno_shock_vortex_fresh_defect_locality_d054_20260722a`. All 360 rows pass,
and the truth-selected 20% oracle captures `0.98741/0.92536` median energy at
calls 30/60. The four-hop current-shock halo captures only
`0.18751/0.20290` and passes zero cases despite bounded support. Classify this
as `localized_but_not_causally_shock_localizable`: sparse detail exists, but a
narrow current-shock gate is the wrong locator. Do not widen or sweep it.

D055 is the only authorized next action. On the same six validation cases and
calls `1/10/30/60`, score nodes by the high-pass amplitude of the frozen PCNO's
own proposed scaled update, exclude the fixed two-hop current-shock halo, and
select the top 20% of eligible nodes. The score is legal current/proposal data;
the target is evaluation only. At calls 30 and 60 it must capture at least 50%
of fresh smooth-high-pass energy on five of six cases, with at most 20%
interior support and exact D053/D054 replay. Passing authorizes only a tiny-fit
contract; failure stops this sensor-gated local-detail route. No sensor sweep,
training, test access, or Line-4 flag change is authorized.

D055 is complete in `pcno_shock_vortex_proposal_sensor_d055_20260722a`. All 24
rows and replay gates pass. Its legal proposal-update high-pass score captures
median `0.91224/0.80212` of fresh ripple energy at calls 30/60 with only
`0.17304/0.16866` interior support; all six cases pass at both calls. Spearman
correlation with fresh-defect amplitude is `0.87482/0.73318`. This establishes
legal localization, not correction realizability or learnability.

D056 is the only authorized next action and trains nothing. Rebuild the fixed
D055 support, let truth choose an upper-bound correction only inside it,
project that correction to zero physical-volume integral per component, cap it
at 10% of the frozen update norm, and enforce raw admissibility plus the frozen
anti-smearing metrics. Use the same six validation cases and calls
`1/10/30/60`. Promotion requires 15% median state and 20% median smooth-high-
pass reduction at calls 30/60, five-of-six joint nonworse cases, at most 20%
support, exact balance to `1e-10`, and every raw/anti-smearing gate. Only a pass
may authorize one frozen-global tiny detail-head fit. Do not train or alter the
contract under D056.

D056 is now complete in
`pcno_shock_vortex_proposal_correction_oracle_d056_20260722a`. All 24 rows,
source-replay, balance, admissibility, support, and anti-smearing checks pass,
but promotion fails. Median state-error reduction is only
`0.07185/0.07010` at calls 30/60; smooth-high-pass reduction is
`0.23763/-0.03592`, with only two of six call-60 cases jointly nonworse. The
10% cap is not binding: the truth-best balanced directions use median
`0.02361/0.05783` of the global-update norm. Thus D055 localizes the fresh
ripple, but its sparse support cannot repair enough of the state safely.
Reject the learned detail head and do not widen support, raise the cap, relax
anti-smearing, or sweep the sensor. No Line-3 learned run is currently
authorized; a new proposal must modify the global map and jointly address the
D053 propagated-state/fresh-high-pass split under a separately frozen gate.
Strength OOD and all Line-4 flags remain unchanged.

D057 is the only authorized follow-up and still performs zero optimizer steps.
On D048's exact four training pairs, compute float32 full-model gradients for
clean state MSE, teacher-defect smooth graph-high-pass MSE, and detached
depth-one generated-state MSE. The one candidate direction is the sum of the
three individually normalized aggregate gradients. It must have cosine at
least `0.05` with every training objective and at least `0.02` with all three
objectives on five of six D013 validation cases independently at calls 30 and
60. Raw generated inputs, positive smooth-mask volume, exact provenance, and a
zero-step ledger are mandatory. A pass permits only writing a short
full-resolution continuation contract. No optimization, loss/threshold sweep,
test access, conservation claim, or Line-4 flag change is allowed under D057.

D057 is complete and rejects that four-pair scalarization. All structural and
zero-step checks pass, but joint validation transfer reaches only 4/6 cases at
call 30 and 2/6 at call 60. Training clean/generated gradients are almost
collinear (`0.98776`), and the candidate improves the high-pass objective on
all 12 validation rows. The repeated failure is geometry-specific: every call-
60 `y00` row has negative clean/generated transfer. Do not run the D057
continuation or retune its weights.

D058 is the only authorized follow-up. It uses every one of the 84 training
trajectories at calls 30/60, constructs seven equal vortex-position-group
state/recurrence tasks plus one global smooth-high-pass task, and computes one
fixed minimum-norm convex combination by 500 deterministic Frank-Wolfe steps.
Every training-task cosine must reach `0.02`, and the unchanged three-objective
validation transfer gate remains 5/6 cases independently at calls 30 and 60.
D058 is full-resolution but zero-training; strength OOD and all Line-4 flags
remain unchanged.

D058 is now complete and rejects that continuation. All provenance, numerical,
zero-step, unchanged-state, admissibility, positive-mask, and test-nonaccess
checks pass. The canonical summary/row hashes are `45fe9019...` and
`57ccc449...`; 288 backward calls take `153.708` seconds and 2.875 GiB peak
CUDA memory. Frank-Wolfe reaches gap `5.55e-17`, and every training-task
cosine is at least `0.563997`. Validation improves from D057 to 6/6 joint
passes at call 30, but only 3/6 pass at call 60. All call-60 high-pass and
generated-state cosines remain positive; clean-state transfer is negative on
all three lower boundary-nearest `y00` cases and positive on all three
`y08` cases. Stop the current joint-objective route: do not train, regroup,
retune, or open strength OOD. No Line-3 learned run is authorized by D058. A
new proposal must change the target/representation or solver coupling and
predeclare both a physical necessary-condition test and a cost gate.

D059 is the sole new authorization and is tiny-fit only. It keeps D044's exact
full-resolution conservative-residual PCNO, split, seed, architecture,
optimizer, raw recurrence, and boundary contract, changing only
`step_stride=1` to `2`. Use four deterministic train pairs
`[(sv_e08_y07,8),(sv_e03_y03,7),(sv_e03_y01,44),(sv_e05_y07,40)]`, no noise
or generated exposure, at most 50 four-presentation epochs, and stop on the
unchanged `0.01` relative-L2 and loss-ratio tiny-fit gate. One raw 30-call
position-OOD rollout must remain admissible. One attempt and 0.05 GPU-hour are
available. A pass permits only writing the serious stride-2 contract; no
serious training, warm start, stride-4 fallback, or test access is yet
authorized.

D059 passes at epoch 35. Its initial/best tiny-bank relative L2 is
`0.0124691/0.00115519`; best loss ratio is `0.00977779`, all four decoded
states are admissible, and the fixed `sv_e01_y08` smoke completes 30/30 raw
calls. Runtime is `69.4304` seconds. The summary and split hashes begin
`40a15670` and `b2d2fc78`; code, data, configuration, normalization, and
pair-bank provenance are closed. This establishes stride-2 target fitability
only. The tiny stride-1/2 rollout errors use different cases and cannot be
compared.

D060 completed on 2026-07-22 under the exact one-seed cold contract.
The 50-record artifact
`pcno_shock_vortex_stride2_serious_d060_s20260718_20260722a` selects epoch 34,
checkpoint SHA-256 `95e6c180a329...`; summary/metrics SHA-256 values begin
`b92d8bef4dab...` and `231027ecb52b...`. The configuration and data-manifest
digests are `0064571b5414...` and `f8d228ae3c6e...`. Its scheduled raw H60
selection completes 24/24 position-OOD validation trajectories with mean state
error `0.00729369`, `12.56%` below D044's registered `0.00834190`; every one
of the 24 paired cases improves. The direct-frame-2/composed-D044 ratio is
`1.0325`, shock strength/thickness ratios are `0.7899/0.8619`, vortex-core is
`0.3424`, and physical-total mismatch is `0.5500`, so those components pass.

D060 nevertheless fails promotion. The frozen six-case H60 D013 median smooth-
high-pass RMS is `0.00548836` versus D044's `0.00535199` (`1.0255x`, not the
required `<=0.8x`). The separate all-24 endpoint high-pass energy is worse on
24/24 cases; its mean RMS ratio is `1.1673`. Mean front-centroid distance is
`0.0260282` versus `0.0181368` (`1.4351x`), failing the 5% position envelope,
although symmetric chamfer is nearly unchanged. The position failure is
systematic: the centroid ratio is `2.750` for `y00` and `0.749` for `y08`.
Synchronized median-call timing implies an H60 ratio `0.480`, but the D044
timing sample has a large p95 outlier; no strict cost claim is needed because
the physical conjunction already fails. Physical and D013 summary SHA-256
values for D060 begin `50ad14177f1e...` and `2f2b5d8c6414...`.

The mechanism result is narrower and useful. D060 is about 3% worse than two
D044 calls at the first matched step, crosses below D044 by `t=0.08`, and ends
at `0.8743x`: fewer recurrent compositions overcome a harder local map in
state L2. At H60 its teacher-forced error is `1.432x` D044 while raw rollout is
`0.854x` on the D013 cohort, yet teacher and rollout high-pass RMS are both
approximately `1.03x` D044. Larger stepping reduces propagated state error;
it does not remove the high-frequency source. Do not retry training, open
strength OOD, add a seed or stride, or combine a rescue method.

## Line 1 Frozen Closeout

D031--D036 and the frozen D038--D039 extensions are complete. D037's Line-3
benchmark/direct-impulse contract and D044's dynamic baseline, physical
validation, D013, and correction-oracle route are now closed. The matched
full-split frontier contains cold strides
1/2/4/8 at seed `20260707` plus stride-8 repeats at seeds `20260708` and
`20260709`, all on the 384/64/64 split with matched one-step and recurrent
sample presentations, four-call recurrent training depth, and H32 validation
selection. These six rows exhaust the training budget. Do not pool them with
D027, which used a 64/16/16 split, one seed, and H20 selection.

D032 corrects a Phase-0 prose-labeling error: the previously reported triplets
are physical H32/H64/H96, not H8/H16/H32. Under exact conservative recurrence,
selected endpoint error is best at stride 2 at H8, stride 4 at H16, and stride
8 at H32/H64/H96. Time-mean error selects stride 8 at H8, stride 4 at H16/H32,
and stride 8 at H64/H96. The stride-8 H96 error range is
`0.01767--0.01821` across three seeds with 192/192 completion. At an error
budget of `0.05`, H96 survival is 4/64, 52/64, 59/64, and 64/64 for strides
1/2/4/8.

Candidate checkpoints remain validation-only. The stride-1 epoch-60 candidate
is much more accurate on the test set than the selected pre-recurrent epoch-47
checkpoint but loses one additional H96 completion. Test-set candidate replay
must not replace the frozen validation selection. It does show that checkpoint
stage and selection variance reduce the strength of any stride-1 comparison.
The historical D032 `conservative_budget_relative_l2` field is only the
predicted-versus-truth mismatch in domain-integrated conserved totals; it is not
a boundary-flux closure residual. The evaluator now emits the accurate name
`global_conserved_total_mismatch_relative_l2` and retains the old field only as
a compatibility alias. The cell-residual FNO remains non-facewise-conservative.

D033 provides the main mechanism result. On truth states, one-call model error
increases with stride; at the same physical start frame, accumulated on-policy
error decreases with stride. A single reference-solver replacement changes H96
error only modestly and rescues no completion. This supports the proposed
harder-map/fewer-calls tradeoff and rejects a simple story in which one locally
inconsistent learned call explains the large-step advantage. Semigroup defect
and fixed-norm perturbation amplification remain supporting diagnostics only.
The initial D033 max-only replay gate stopped before model evaluation; the
documented mean/p99/max gate revision and clean rerun must remain in provenance.

D034 closes same-hardware timing on a fixed 16-case subset. At H96, stride-8
host-to-host batch-1 time is 15.78 ms and the accuracy-matched reference time is
2.015 s, a measured 127.71x speedup. Batch-16 stride-8 host-to-host throughput
is 991 cases/s versus 0.277 cases/s for the matched reference. These numbers are
specific to the documented Python solver, hardware, boundaries, and subset.
They are not an optimized-production-CFD claim.

D035 closes the frozen error-geometry and visualization request. Across all 12
truth-state starts from frame 0 through 88, absolute one-call error rises with
stride while error per physical step falls. At H96, stride 8 is best on global
L1/L2, shock-region, smooth-region, and post-hoc translation-oracle L2; stride
2 is best on explicit top-two-front position. The translation oracle does not
reverse the stride-8 ranking. The six-descriptor casewise router fails every
signal gate (`0.328` accuracy versus `0.547` for its majority control), so do
not build an adaptive timestep controller from it. Five selected raw-recurrence
GIFs and their NPZ arrays preserve qualitative behavior and censor the two
stride-1 pressure failures explicitly.

D036 resolves the visible-ripple concern without new training. At H96, the
primary stride-8 checkpoint retains its lower global L2 (`0.01766` versus
`0.02646` for stride 4), but has higher error roughness away from the two
detected fronts: smooth second-derivative error is `579.4` versus `484.1`, and
smooth predicted/truth TV is `1.891` versus `1.729`. Paired-bootstrap ratio
intervals are `[1.147, 1.244]` and `[1.069, 1.120]`. Under the diagnostic
smooth-TV budget `1.5`, median first exceedance is H68 for stride 4 versus
H48--H56 for the three stride-8 seeds. This is a thresholded ripple-lifetime
conflict, not evidence that stride 8 has a shorter raw-admissible or universal
physical lifetime; all stride-4/8 paths remain raw-admissible at H96.

D038 separates defect injection from recurrent accumulation. Pooled over
common truth starts `0:5:90`, every spectral band has higher stride-8
truth-normalized one-call update error than stride 4 across all three seeds;
the largest contrast is `2.465--2.744x` in modes 17--24. At H8, primary stride
8 has `1.218x` total modal error and a seed-consistently rougher error shape.
The legacy key `resolved_17_24` straddles the cutoff: `k=24` is the first mode
outside a 24-mode FNO and must not be described as retained.
At H32 it already has lower total modal error (`0.917x`) but higher centroid,
normalized `k^4` shape, and tail share (`1.203/1.327/1.857x`). At H96 its total
modal error is `0.522x`; the primary global shape remains slightly rougher, but
that global ordering is not repeat-stable. Preserve the narrower conclusion:
large steps inject a harder broadband defect, fewer calls later win in total
error, and the repeat-stable late D036 roughness is localized rather than a
global spectral blow-up.

D039 closes the cross-artifact operating envelope with no new training,
checkpoint evaluation, or solver call. All ten provenance/mechanism invariants
pass, including exact agreement of 72 duplicated D036/D038 rows. Endpoint-error
winners are stride 2 at H8, stride 4 at H16, and stride 8 at H32/H64/H96, but
the H8 and H32 winner-versus-runner paired case-bootstrap intervals cross
equality. Stride 8 first and sustainably beats stride 4 in endpoint error at
H32; for time-mean error, sustained crossover occurs only at H56. At H96,
stride 8 wins global error, reliability at budget `0.05`, and latency, while
stride 2 wins front position and away-front derivative/TV diagnostics. This is
the final empirical Line-1 result: the preferred stride is objective- and
horizon-dependent, and its global-error optimum remains right-censored.
The paired intervals are post-selection held-out-case uncertainty, not
training-seed uncertainty or confirmatory uncertainty for a predeclared winner.

A 2026-07-22 read-only audit registered the existing 32-case
`mild_support_extrapolation_v1` artifacts without rerunning them. At H96,
primary stride-2/4/8 errors are `0.07938/0.04287/0.04890`, with completion
`31/32`, `32/32`, and `32/32`; the two stride-8 repeats give `0.04074` and
`0.03611`, both `32/32`. Thus stride 4 and every observed stride-8 seed beat
the single stride-2 row, but stride 8 does not beat stride 4 for every seed.
Only stride 8 was repeated. The OOD NPZ is absent locally and historical rows
omit per-case regime labels, so high-inflow versus low-ambient block labels are
inferred from current generator order and are not claim-eligible. Future OOD
evaluation validates embedded suite/regime metadata, requires and records the
generator hash, and writes regime-specific summaries. Never pool this OOD
regime with matched-ID D032.

The six selected hashes, 360 candidate hashes, and full 608-file registry were
recorded before evaluation. The verified 931,943,871-byte archive SHA256 is
`81151ee33478935b53ba650dc9f57b4c90c42de158db71069e2ad6bc7491bb55`.
The ignored local evidence bundle remains required; compact repository notes
are not a substitute for it.

Collected ignored summaries are rooted at
`artifacts/time_dependent_no/line1_phase1a_20260718_collected/`. The primary
D032 directory is `d032_primary_selected_seed20260707/`; the two stride-8
repeat directories and six epoch-50/60 candidate directories sit beside it.
D032 writes `contract.json`, dense `direct_curves.jsonl`,
`direct_summary.csv`, `error_budget_survival.csv`, `wave_occupancy.json`,
and `metrics.json`. D033's claim-eligible rerun is
`d033_primary_selected_full_gate_v2/`; preserve the earlier stopped gate
directory separately. It writes truth replay, same-state, raw/rescue curve,
summary, survival, comparison, and report artifacts. D034 is
`d034_primary_selected_cases16/` and writes its contract, per-case reference
rows, reference latency/throughput summaries, direct learned timing, the
accuracy-matched Pareto table, and aggregate metrics. Do not commit these
artifacts or rewrite their recorded source paths into public documentation.

D035 artifacts are rooted separately at the ignored path
`artifacts/time_dependent_no/line1_frozen_visualization_20260719/`. The
`summary/` directory contains publication PDF/PNG figures built from registered
D032--D034 inputs, `rollout/` contains exact-recurrence metrics and the selected
GIF/NPZ gallery, and `d033_dense_starts/` contains the 12-start same-state
extension. The durable command is
`scripts/time_dependent_no/visualize_euler1d_flow_map_frontier.py`; preserve the
input hashes in its reports rather than treating generated figures as primary
evidence.

D036 artifacts are under ignored root
`artifacts/time_dependent_no/line1_frozen_ripple_20260719/`. Use
`d036_scale_spectra_smooth/` as the final six-checkpoint metric source and
`analysis/` for paired CSVs, threshold survival, reports, and publication
PNG/PDF figures. The earlier `d036_scale_spectra/` directory is the preliminary
global spectral pass before away-front derivatives were added; do not pool its
duplicate rows with the final table.

D038 artifacts are under ignored root
`artifacts/time_dependent_no/line1_frozen_modal_spectra_20260719/`.
`mode_spectra.csv` is the 41,088-row source table; `analysis/` contains the
common-scale relative-error and error-share heatmaps, teacher-update heatmaps,
stride-8/stride-4 mode ratios, band/shape CSVs, and the report. The new scalar
table uses teacher starts every five frames and canonical rollout batch size
one. Its rows overlapping D036 are numerically identical; do not pool the
denser duplicate teacher rows with D036.

D039 artifacts are under ignored root
`artifacts/time_dependent_no/line1_frozen_closeout_20260720/`. The durable
`closeout` subcommand in
`scripts/time_dependent_no/visualize_euler1d_flow_map_frontier.py` consumes the
registered D032--D038 source tables and writes the operating-envelope,
paired-uncertainty, crossover, and curve tables; a provenance report; and
PDF/PNG operating-envelope and global/local/runtime Pareto figures. It performs
no model inference and records every source-table hash.

The maximum additional Line-1 learned-training count remains zero. Do not add
stride 16, new data, matched-exposure retraining, new targets, limiters,
barriers, architecture variants, or timestep-conditioned training. Strides
1/2/4/8 and 101 saved frames are enough for the bounded tradeoff claim through
H96 with active waves, but not enough to bracket a universal optimal timestep.

`XLINE-001` is now registered as a documentation-only, frozen-checkpoint
rollout-instability attribution diagnostic. It does not reopen Line 1,
authorize a run, or alter any active Line-3 job. Keep four distinct outcomes:
global-error lifetime, ripple/roughness lifetime, raw-admissibility lifetime,
and learned perturbation growth relative to the same-state reference flow.
Larger-stride raw completion by itself does not identify the cause.

The bounded inputs are the existing D033 and D035--D038 Line-1 artifacts; at
most four stride-1/2 raw failures, their same-case stride-4/8 paths, and four
stable matched controls for new FNO forks; the three D041 bump-PCNO H79 failures
plus at most three matched completed paths; and D044's frozen six-case D013
validation cohort as a separate stable control. Do not regenerate or pool these
regimes. For the FNO family, the missing core readouts are the vector-valued
same-state decomposition, crossed state/operator matrix, and common-start
comparison of `G_8`, `G_4^2`, `G_2^4`, and `G_1^8`. Perturbation gains must
use the same norm and perturbation bank for learned and reference maps and be
normalized per physical time. Match controls deterministically on frozen
initial physical descriptors with a case-ID tie-break, before reading new
diagnostic traces.

Align front, shock/smooth, spectral or graph-high-pass, roughness, update,
pressure-margin, boundary, and OOD-proxy traces to the first outcome event.
Temporal order is only a selector; a causal label also needs a same-state
reference fork or a preregistered single intervention. The 1D same-state path
already exists. D044's lower-level reference stepper still needs a focused
generated-state restart, time/boundary, saved-time, and closure preflight. The
released bump/DG artifact has no validated generated-state solver restart, so
its PCNO evidence cannot support a solver-versus-model decomposition. Record
these limitations rather than using the next saved truth state as a substitute.
If the evidence does not isolate one mechanism within a frozen regime, close
XLINE-001 as mixed or unresolved. Any method change requires separate Line-3
authorization. Preregister at most two intervention types per regime and one
intervention time per failing path. Because every frozen cohort contains shocks
or sharp fronts, this diagnostic cannot establish that discontinuities are
necessary; a smooth-control family would need separate authorization.

A 2D extension is conditional on the Line-3 baseline and legal data contract.
If separately authorized, use two physically interpretable output intervals
and at most three seeds per interval; do not count it as continuation of the
1D ladder.

## Current Diagnostic State

The available local-bundle CPGNet run did not match the paper's Supersonic Bump
Table 2 autoregressive errors, but it is not yet an apples-to-apples
reproduction because dataset, split, checkpoint, configuration, and evaluator
identity remain unresolved. Under the audited local contract, teacher-forced
one-step errors are near the paper scale while autoregressive errors are much
larger and concentrate around shock phase, shape, amplitude, and multiscale
degradation.

### Line 2 legal-boundary closeout (2026-07-20)

The release-bundle boundary question is now empirically closed on the same
20 test trajectories, 79 rollout calls, dataset hash `d97abde1...`, and archived
closeout-evaluator hash `97a9c960...`. The metric below is that evaluator's
arithmetic mean of per-trajectory normal-node primitive RMSE:

| Checkpoint and rollout boundary contract | rho | v1 | v2 | pres |
| --- | ---: | ---: | ---: | ---: |
| Public checkpoint, next-reference injection and clamp | 0.177032 | 0.073653 | 0.082204 | 0.319612 |
| Public checkpoint, causal nodal boundary sensitivity | 0.563023 | 0.263983 | 0.242344 | 1.137027 |
| One legal-boundary-trained checkpoint, causal nodal rollout | 0.368161 | 0.157947 | 0.133379 | 0.753202 |

The archived closeout-evaluator oracle aggregate differs from the earlier oracle
replay by at most `8.5e-7`, so evaluator drift does not explain the boundary
gap. Relative to the frozen public-checkpoint sensitivity, legal-boundary
training reduces RMSE by `34.6%/40.2%/45.0%/33.8%` and improves all four
variables on 19/20 trajectories. At q0.90 it improves shock-centroid distance
and symmetric Chamfer on 20/20 trajectories and front-union and smooth-region
pressure RMSE on 19/20. All three 20-trajectory rollouts are finite and have
no nonpositive normal-node density or pressure. The trained row nevertheless
remains `1.6--2.4x` worse than the oracle row, improves IoU/F1 on only 11/20
trajectories, and raises the worst post-policy boundary absolute error from
`15.64` to `19.65` relative to the frozen legal sensitivity.

The locked ignored artifacts are:

- `cpg_phase1d_oracle_current_primary20_20260720a/`;
- `cpg_phase1d_frozen_legal_matched_primary20_20260720a/`;
- `cpg_phase1d_legal_trained_eval_primary20_seed20260718_20260718a/`; and
- `cpg_phase1d_legal_boundary_closeout_primary20_20260720a/`.

The trained checkpoint is one seed (`20260718`), selected at the final
completed epoch after 15 teacher-forced and five multistep epochs, with no
validation split. Train and test Mach ranges overlap, no grouped geometry
holdout was run, and the causal freestream-inflow/interior-slip-wall/
supersonic-outflow nodal policy is not exact DG boundary replay. Paper dataset,
checkpoint, seed, and compared-baseline evaluator identity remain unresolved.
Accordingly this is a release-bundle legal-boundary result, not a paper-table
reproduction, a fair autonomous comparison for the released oracle row, or
evidence of a general learned solver.

The current code adds stricter source closure, one-based case and complete
stencil identity, stage-2 recurrent-outflow legality, and physical
admissible-prefix accounting. These checks are prospective and do not
retroactively strengthen the archived closeout runs.

Stop Line-2 training here. D014 did not expose an endpoint-sampled
finite-radius violation, the conditional-manifold tests remain unexecuted, and
the available graph lacks the validated control-volume/physical-face mapping
needed for conservation or decoder-balance attribution. Those are limits on
claims, not reasons for another broad CPG experiment. No matched decoder
ablation is authorized; carry the legal-boundary, recurrent-exposure,
message-depth, shared-face, and shock-diagnostic lessons into Line 3.

The interface-latent investigation on local bs2 reproduction checkpoints found
that `reconstruct_prims` behaves like a nonphysical flux-control coordinate
rather than a physical one-sided trace. This is not yet a result about an
identified paper checkpoint. The probe code was retired from the active tree;
recover it from git history only if the exact local test must be rerun.

For PCNO, use `scripts/time_dependent_no/rollout_pcno_preprocessed.py`, not the retired raw-HDF5 graph adapter. The corrected PCNO rollouts show good early shock placement but visible Fourier-style rippling, followed by instability/crash in longer autoregressive rollout.

The completed 1D Euler target ladder is the bridge from these diagnostics to
2D method design. Its evidence says raw conservation structure is not enough:
flux and interface objectives need target-fit, rollout, shock, conservation,
and admissibility diagnostics before they can support a structure-preserving
claim. Do not reopen the ladder as a broad sweep.

The two Section 1.2 rows formerly labeled CPGNet were produced by a generic
directed target head with `limited_residual`; they did not execute the paper's
interface reconstruction, Rusanov flux, and finite-volume update. They are now
deprecated. The corrected M0 path is `CPGNetEuler1D + cpg_interface`: positive
directed interface states, one shared face flux, exact 1D geometry, physical
ghost boundaries, no post-update limiter, and raw failure at the first
inadmissible recurrent state. AutoDL validation is complete. The paper-faithful
h128/mp12 reference remains locality-limited on the stride-4 dataset: 34/64
test rollouts complete and one-step relative L2 is 0.0334. Increasing only
unshared message depth to 28 gives one-step relative L2 0.00614 and 64/64 raw
completion, with final L2 0.0298 and no nonpositive recurrent states. This is a
strong baseline, but depth also raises parameters from 1.50M to 3.35M, so
matched-parameter controls were required before a causal depth claim.

The two parameter-matched controls are complete. The wide mp12 model does not
recover the deep model's behavior, while the narrow mp28 model retains the
important gain. Receptive-field depth, not parameter count alone, is therefore
the supported mechanism. Freeze h128/mp28 as the strong corrected CPGNet
reference and do not append another graph-depth or width sweep.

The mp28 mean shock error is tail-dominated. Replay of the three outliers shows
a persistent nearly stationary pressure gradient near the original
discontinuity while the target front travels. The argmax front metric magnifies
switching between two comparable gradients, but the ghost front is genuine.
Track the top two fronts; do not add blanket TV regularization to repair this
secondary baseline.

The accepted direction is recorded in
`docs/time_dependent_no/RESEARCH_DIRECTION_DECISION.md`. The four-way FNO
coordinate matrix is complete. Conservative input, conservative loss, fixed
physical scaling, and conservative recurrence are the provisional contract for
the target screen; this is a within-screen choice, not a universal claim that
primitive coordinates are unsuitable.

The prediction-target path is implemented and validated. The active
ADER generator can optionally save the owner-oriented conservative face-flux
impulse summed over every accepted CFL substep. Rejected retry attempts
contribute zero; the final accepted retry or first-order fallback contributes
exactly once. The loader aggregates those impulses over the requested macro
stride and exposes their time average to the existing conservative flux
decoder. The cumulative conservative face impulse is the solver-exported
semantic label; its time average is an exactly invertible normalization for
training. Do not confuse that label contract with the separate gauge-canonical
representative used in one loss ablation.
The FNO trainer now distinguishes state-only, direct-flux, and joint flux/state
supervision with component normalization. The state-loss-only flux row is the
underidentification/conditioning control, while direct and joint rows isolate
solver-facing supervision. Direct solver-flux labels
require clean inputs because adding input noise without recomputing the solver
flux makes the label inconsistent with the decoded update. The 512-case ADER
flux dataset has shape `[512, 101, 256, 3]` with `[512, 100, 257, 3]` face
impulses. It preserves the original state arrays bitwise, has no solver fallback,
closes before serialization to about `7.1e-14`, and has maximum float32 decoded
closure about `2.90e-5`.

The stride-1 supervision screen is complete through a midscale stop decision.
Two tiny-set seeds, a lower-learning-rate/longer control, and a matched
gauge-mode by joint-weight ablation established that raw reference-gauge fitting
is an optimization nuisance but not the main rollout bottleneck. Gauge-canonical
joint supervision with flux weight `0.1` was the only solver-flux row that
preserved tiny-set state fit while materially improving divergence-active flux
error, so it alone advanced with the state-loss-only flux head.

The promoted midscale screen used the same 316,739-parameter FNO 64/24/4,
64/16/16 trajectories, 6,400/1,600/1,600 one-step pairs, 50 epochs, and 40,000
optimizer updates per row. Gauge-canonical joint supervision reduced held-out
divergence-active flux MSE to `0.301x` state-only, but test one-step relative L2
rose from `0.00461` to `0.00623` (`1.350x`) and mean raw rollout survival fell
from `0.51875` to `0.300` (`0.578x`). Both rows completed 0/16 rollouts, and the
joint row failed earlier on every paired trajectory: 6.0 valid steps on average
versus 10.375. All failures were raw nonpositive states, predominantly pressure.
The lower joint prefix error, shock error, and total-state error are not
long-horizon wins because they are evaluated on a much shorter surviving prefix.

This result rejects full-scale promotion of this exact face-flux MSE objective;
it does not reject conservative flux-form FNOs or all solver-facing targets. In
1D, after removing the single constant-flux null mode, the face field is fixed
by its divergence. Gauge-canonical face-flux MSE is therefore an
inverse-divergence reweighting of the same state increment, emphasizing global
low-frequency modes. The observed active-flux gain with worse shock-adjacent
state and rollout behavior is consistent with that loss geometry.

The matched second-stage stability screen is complete. Both rows reproduced an
identical 50-epoch one-step trajectory, then reset AdamW at `3e-5` for ten
epochs of four-step conservative recurrence. Unroll-only improved test one-step
relative L2 from `0.004612` to `0.003438` and mean raw survival from `0.51875`
to `0.759375` (`1.464x`), with 2/16 completed rollouts instead of 0/16. It
survived longer on 14/16 paired cases, by a median five saved steps, and had
lower relative error on all 16 cases at their common endpoint. The
rollout-selected checkpoint is epoch 60, so the intervention passes its
predeclared promotion gate.

The weight-`0.1` training-only density/pressure barrier reached one-step
relative L2 `0.003418`, survival `0.75625`, and the same 2/16 completion and 14
nonpositive terminations. Its survival is `0.996x` unroll-only, with four cases
longer, eight tied, and four shorter. The weighted barrier was about 4.5% of
state loss in its first stage epoch and below 0.1% thereafter. This tested
barrier therefore fails attribution; do not carry it into the full-scale run.
Both midscale unrolled rows remain incomplete-horizon models.

The full 384/64/64 unroll-only confirmation is complete. It used the frozen
316,739-parameter FNO, 50 one-step epochs plus ten four-step recurrent epochs,
and no noise, limiter, floor, positive transform, or barrier. The selected
epoch-59 checkpoint reaches test one-step relative L2 `0.001305`, mean survival
`0.97734`, and 57/64 completed 20-call raw rollouts. All seven terminations are
nonpositive proposals. The initial effective CFL is `3.84` on average and
`5.64` at maximum. The run passes the original scale and 20-call promotion
gates but misses the strict 90% completion gate by exactly one trajectory. On
the same validation cases, four-step recurrence raises completion from 5/64 at
the best one-step checkpoint to 56/64 at the selected recurrent checkpoint.
This supports recurrent-distribution training as the decisive intervention on
this fixed dataset, but it does not establish unconditional positivity,
long-horizon robustness, or timestep/resolution transfer.

The frozen epoch-59 flux checkpoint has now been evaluated without retraining at
20, 50, and 100 raw calls. The 20-call metrics reproduce exactly. At 50 calls,
mean survival falls to `0.566`, only 1/64 cases completes, and 63 terminate on
raw nonpositive proposals. At 100 calls, 0/64 completes and every case has
terminated by call 51. The earlier 57/64 result is a strong short-horizon
baseline, not evidence of medium-horizon stability without positivity control.
Median termination is call 27; density is nonpositive in 56/64 cases and
pressure in 13/64, with five overlaps. This later population is predominantly a
density-undershoot failure, not simply the three early pressure/shock failures.

The strict target-parameterization screen now contains a faithful direct next
conservative-state head. Direct state and residual labels both pass float32
closure. Direct state fails the fixed tiny-fit gate on both seeds: minimum train
relative L2 is `0.01658` and `0.01711`, versus the `0.01` threshold.
Conservative residual passes on the first seed at `0.00648`; this is an
optimization/centering failure for direct state, not a rollout-based rejection.

The promoted residual row is now complete at full 384/64/64 scale. It uses the
316,419-parameter FNO cell head, the same 50+10 schedule and conservative
coordinate contract as the flux head, and no noise, limiter, floor, positive
transform, or admissibility penalty. The rollout-selected epoch-60 checkpoint
reaches test one-step relative L2 `0.001123`, final 20-call relative L2
`0.01215`, survival `1.0`, 64/64 completion, shock-position MAE `0.00428`, and
conserved-total error `0.00316`. Four-step recurrence lowers the best validation
20-call error from `0.01883` after one-step training to `0.01143` (`39.3%`) while
retaining full survival.

The frozen checkpoint also passes the actual horizon test: 62/64 trajectories
complete 50 calls with mean survival `0.990`, and 61/64 complete 100 calls with
mean survival `0.97781`. At 100 calls, completed-case final relative L2 has mean
`0.0551`, median `0.0482`, and p90 `0.104`; conserved-total error is `0.00927`
over all valid prefixes. There is no density or nonfinite termination. The three
failures occur at calls 33, 37, and 91 when pressure collapses near an interior
moving wave after velocity overshoot; valid length is essentially uncorrelated
with truth effective CFL (`r` about `0.01`). These failures motivated later
generated-state and training-only pressure/internal-energy tests. The tested
mean barrier did not improve held-out survival, so this paragraph is evidence
history rather than an active penalty recommendation.

The residual advantage is paired, not just aggregate. At the 20-call common
endpoint it has lower relative error on 58/64 cases; the mean error difference
is `-0.0189` with trajectory-bootstrap 95% interval about
`[-0.0304, -0.00975]`. At the longer common endpoint, which is usually set by
the flux model's failure, residual is lower on 63/64 cases and its median error
ratio is `0.165`. The pressure-argmax shock metric has a known multi-wave caveat:
its largest residual outliers are rank swaps between two correctly located
gradient peaks, reflecting relative wave-amplitude error rather than a missing
or displaced front. Keep top-k wave matching alongside the legacy scalar.

The cell residual head is not structurally facewise conservative, so
conserved-total error remains an outcome metric. This observation motivated the
projected-residual comparison below; it is not a pending method instruction.

That projected-residual comparison is now complete over seeds `20260707--09`.
The 316,806-parameter model enforces decoded closure to its learned boundary
budget at about `1e-7` and predicts that budget with mean one-step relative L2
`0.00722`. It improves pooled completion from 182/192 to 187/192 at 100 calls,
but does not improve accuracy robustly: the three-seed mean 100-call state-error
ratio is `1.016`, and only one seed improves state error. Among the 181
seed-case pairs completed by both methods, projected residual is 9.8% worse in
state L2, 5.9% better in conserved-total error, and 4.8% worse in shock MAE.
Only stability noninferiority passes the preregistered family gates. Keep raw
residual as the accuracy baseline and projected residual as a stability/
conservation ablation; neither result supports a general positivity claim.

The first generated-state exposure pilot is also complete on projected
residual at 64/16/16 scale. Its 50 one-step epochs reproduce the matched clean
control exactly. The clean second stage uses four-step BPTT; the intervention
uses eight detached model steps followed by the same four supervised steps,
with 10 versus 11 recurrent epochs to match optimizer updates within 1%. Both
rows select recurrent epoch 58. Burn-in changes 20/50/100-call state-L2 ratios
to `1.021`, `0.768`, and `0.638`; conserved-total ratios are `0.924`, `0.659`,
and `0.537`; shock ratios are `1.382`, `1.068`, and `0.936`. At 100 calls both
complete 14/16, but survival falls from `0.9650` to `0.9394`. It rescues case 21,
which fails under the control at call 96, while creating a call-58 failure on
case 234 and advancing case 238's failure from call 50 to 47. Training burn-in
states remain admissible: zero nonpositive samples, minimum density `0.0831`,
and minimum pressure `0.0675`. Classify this as partial evidence that later-
state exposure repairs broad error drift without controlling rare long-horizon
failure basins. Do not full-scale the projected row. The completed matched
plain-residual gate is recorded next.

The matched plain-residual exposure gate is now complete at the same 64/16/16
scale and seed. Its one-step histories again match exactly; the clean row uses
7,760 recurrent optimizer updates and the eight-step generated-burn row uses
7,832. Generated exposure lowers final state L2 by 4.6% at 20 calls and 32% at
50 calls. At 100 calls it lowers common-endpoint state error by 51.4%, wins
14/16 paired cases, and changes completion from 15/16 to 16/16. Conserved-total
error falls by 7.7%, 40.8%, and 47.4% at 20/50/100 calls. The formal gate still
fails because legacy pressure-argmax shock MAE rises 45.6% at 20 calls.
Focused replay attributes most of that aggregate increase to three cases:
two contain front-strength rank changes, while case 238 has a genuine
spurious/displaced-front regression. Keep the formal failure; report separated
top-two front position and strength metrics in subsequent gates.

The teacher-offset mechanism control is complete. It uses the identical 8+4
windows and 7,832 recurrent optimizer updates but initializes the supervised
segment from the exact offset state. One-step histories match both frozen
controls exactly; its prefix error is zero and all prefix states are
admissible. Teacher offset is 10.9% worse than clean at 20 calls, essentially
tied at 50/100 calls, and changes H100 completion from 15/16 to 14/16. It
postpones clean case 21's pressure failure from call 49 to 51 but introduces a
case-114 pressure failure at call 89.

Generated exposure decisively beats that time-offset control. Its
generated/teacher common-endpoint state-error ratios are `0.861`, `0.661`,
and `0.492` at 20/50/100 calls; trajectory-bootstrap 95% intervals are
approximately `[0.782,0.948]`, `[0.470,0.854]`, and `[0.348,0.669]`.
It wins 13/16, 13/16, and 15/16 cases and completes all H100 cases versus 14/16
teacher. Conserved-total error improves by 24%, 46%, and 50%. This supports
off-manifold generated-state exposure as a real mechanism beyond later-time
sampling on this fixed split and seed. The one-step ordering is opposite:
teacher reaches `0.002660` versus generated `0.002886`, reinforcing that
one-step fit does not select the better recurrent operator.

Do not full-scale yet. Against clean, generated exposure still worsens separated
top-two front-position error by 17.8% at H20 and 10.2% at H50; both paired
bootstrap intervals exclude one. D023 is complete. At generated-prefix depth
eight, generated/teacher fixed-scale conservative errors are `0.992` to the
original next state and `1.017` to the same-state WENO-HLLC-ADER continuation;
clean is `0.974` of generated on the solver-continuation metric. The generated
correction-to-original-trajectory cosine is only `0.074`. Its shock-region
solver defect is 6.3% worse than teacher and 8.0% worse than clean. This rejects
a large local PDE-map improvement and does not meet the preregistered strong
denoising threshold. The long-rollout gain is more consistent with a small
closed-loop modified-dynamics or self-consistency effect. Keep full-scale
promotion paused. D013 subsequently completed the frozen scale/spectral
diagnostic and did not reveal a teacher-forced spectral-fit deficit.

D029 is complete and separates two claims that were previously entangled. The
frozen stride-1 and cold stride-2 residual FNOs do not satisfy native-grid
zero-shot resolution transfer: native one-step error rises by roughly 5.5 to
8.3 times at 128 and 512 cells, and the 512-cell H20 shock error rises by 2.10
times for stride 1 and 2.99 times for stride 2. This is a persistent discrete
closure mismatch rather than a first-frame artifact. Along separately evolved
paired native trajectories, update labels differ by 28% to 58% of the coarse
increment norm, while the current inputs contain absolute cell centers but no
cell width or timestep channel. This is strong evidence of grid-dependent
labels, not an exact same-input operator commutator measurement.

The large-step result is nevertheless robust. At 128 and 512 cells, cold
stride 2 beats stride 1 on common-case H50/H100 state error, with H100 ratios
of 0.474 and 0.523 and no lower common-case completion. The correct
classification is therefore stable large-step advantage without native
resolution transfer. D030 subsequently built restriction-consistent
cell-average targets and trained the shared multiresolution FNO; that completed
result is recorded below and in the diagnostic tracker.

## Active Code Surface

Reusable utilities are under `utility/time_dependent_no/`. The active 1D Euler target-ladder utilities are:

- `utility/time_dependent_no/fv.py`
- `utility/time_dependent_no/euler1d.py`
- `utility/time_dependent_no/euler1d_data.py`
- `utility/time_dependent_no/euler1d_models.py`
- `utility/time_dependent_no/euler1d_targets.py`

Active 1D Euler scripts are:

- `scripts/time_dependent_no/euler1d_weno_hllc_ader_dataset.py`
- `scripts/time_dependent_no/train_euler1d_target_ladder.py`
- `scripts/time_dependent_no/analyze_euler1d_target_ladder.py`
- `scripts/time_dependent_no/evaluate_euler1d_flow_map_frontier.py`
- `scripts/time_dependent_no/benchmark_euler1d_flow_map_runtime.py`
- `scripts/time_dependent_no/generate_euler1d_flow_map_ood.py`
- `scripts/time_dependent_no/visualize_euler1d_flow_map_frontier.py`

Commit `729091b` preserves the exact pre-cleanup source surface for retired
one-off probes, failed method scaffolds, and stale visualization entry points.

Active 2D diagnostic scripts are:

- `scripts/time_dependent_no/audit_cpg_release_provenance.py`
- `scripts/time_dependent_no/audit_cpg_mesh_contract.py`
- `scripts/time_dependent_no/evaluate_cpg_release.py`
- `scripts/time_dependent_no/diagnose_cpg_characteristic_reach.py`
- `scripts/time_dependent_no/train_cpg_legal_boundary.py`
- `scripts/time_dependent_no/prepare_pcno_euler2d_shards.py`
- `scripts/time_dependent_no/train_pcno_euler2d_residual.py`
- `scripts/time_dependent_no/diagnose_pcno_euler2d_ripples.py`
- `scripts/time_dependent_no/visualize_official_cpg_rollout.py`
- `scripts/time_dependent_no/rollout_pcno_preprocessed.py`

The CPG audit, evaluation, reach, and legal-training files preserve the frozen
Line-2 closeout and are not an active experiment queue. Their reusable support
lives in `cpg_release.py`, `cpg_mesh_contract.py`, and `cpg_reach.py` under
`utility/time_dependent_no/`.

Generated reports, HDF5 rollout arrays, GIFs, checkpoints, and large logs remain ignored under `artifacts/time_dependent_no/`.

## Key References

- `docs/time_dependent_no/README.md`
- `docs/time_dependent_no/RESEARCH_DIRECTION_DECISION.md`
- `docs/time_dependent_no/CPG_EULER_DATASET_CONTRACT.md`
- `docs/time_dependent_no/MECHANISTIC_DIAGNOSTIC_TRACKER.md`
- `docs/time_dependent_no/MECHANISTIC_DIAGNOSTIC_PLAN.md`
- `docs/time_dependent_no/BUMP_300_DATASET_AUDIT.md`
- `docs/time_dependent_no/CPGGNSPDES_REFERENCE_AUDIT.md`
- `docs/time_dependent_no/prompts/LINE_1_LARGE_STEP_FLOW_MAP_PROMPT.md`
- `docs/time_dependent_no/prompts/LINE_2_CPGNET_MECHANISM_PROMPT.md`
- `docs/time_dependent_no/prompts/LINE_3_2D_ROLLOUT_PROMPT.md`
- `docs/time_dependent_no/prompts/LINE_4_LATENT_FORECAST_ASSIMILATION_PROMPT.md`

## Completed 1D Decision Record

The numbered list below preserves the decisions that closed the 1D target
program. It is not an active experiment queue. Any forward-looking wording in
the historical entries is superseded by the four-line program in
`RESEARCH_DIRECTION_DECISION.md`.

1. Keep h128/mp28 frozen as the strong corrected CPGNet reference. The matched
   controls support a receptive-field explanation; do not run another CPG
   architecture sweep.
2. Keep the conservative-input/conservative-loss/recurrent contract frozen for
   the remaining strict target comparisons. Record it as provisional rather
   than claiming universal coordinate superiority.
3. Stop scaling the tested gauge-canonical joint face-flux MSE row. Preserve it
   as a label-valid negative result: it fits the identifiable flux target but
   loses decoded-state accuracy and raw rollout survival at midscale.
4. The matched second-stage stability screen is complete. Unroll-only passes;
   the tested admissibility barrier does not. Preserve inference as the exact
   unmodified flux divergence with no limiter, floor, or positive transform.
5. Freeze the completed flux-form row as a strong 20-call result and a negative
   50-call stability result. Do not call 57/64 at 20 calls a medium-horizon or
   positivity-preserving baseline; only 1/64 reaches call 50.
6. Stop the uncentered direct-next-state row in the strict matched regime after
   its two-seed tiny-fit failure. Treat conservative residual as the surviving
   information-equivalent parameterization; an explicit identity bypass would
   make direct state algebraically a residual head rather than rescue a distinct
   target.
7. Freeze the completed three-seed residual FNO as the strong fixed-setting
   accuracy baseline. Its 100-call pooled completion is 182/192; report both
   mixed-prefix and completed/common-case metrics.
8. Freeze projected residual as a structural ablation, not the new accuracy
   baseline. It improves pooled 100-call completion to 187/192 and exact budget
   closure, but fails the state, conservation-robustness, and shock family gates.
9. The teacher-offset 8+4 control is complete. Later-time sampling alone does
   not improve H50/H100 error, while generated exposure decisively does, so the
   off-manifold intervention is active on this split. D023 is complete and does
   not find a material same-state PDE-map gain: generated is 1.7% worse than
   teacher against the solver continuation at depth eight while only 0.8%
   better against the original trajectory. Treat the rollout gain as a
   closed-loop dynamics effect. D013 finds recurrent high-frequency growth but
   no teacher-forced spectral-fit deficit, and D024 rejects blind conservative
   Laplacian viscosity as its remedy. Keep full-scale burn-in paused because
   H20/H50 top-two front position remains worse than clean.
10. D013 is complete for 1D Euler. The state-loss-only flux head has
    teacher-forced high-band fit comparable to residual, but its failure state
    has 10.4 times the matched residual high-band error and 154 times its
    second-difference error. Error begins shock-local and spreads into smooth
    cells. Keep any later 2D extension graph-native.
11. D024 is complete and negative. No tested small interior diffusive flux
    improves H50 completion; small coefficients worsen state and Nyquist-tail
    error, and larger coefficients shorten survival. Do not promote global
    smoothing or repeat blind current-state viscosity sweeps.
12. D025 is complete and fails its successive-halving gate. The tested global
    relative-interface FNO reaches selected test one-step relative L2 `0.00786`,
    but uniform two-step training, frame-zero weight `15`, and a training-only
    barrier all give 0/16 H50 completion and mean survival below `0.05`.
    Frame weighting improves the initial shock/front error substantially, so
    rarity was real but not sufficient. Four-step loss becomes nonfinite only
    after recurrence has already produced negative density/pressure; raw model
    outputs remain finite. Do not full-scale or add an inference limiter.
13. The pressure/internal-energy-loss control is complete for D025. Barrier
    weight `0.1` drives its two-step training violation from `0.00443` to about
    `1e-7`, but does not improve held-out H50 survival. Do not sweep blanket TV,
    smoothing, or more mean barrier weights. Route new conservative-target work
    toward centered residual/projection or identifiable cumulative-impulse
    supervision rather than another underidentified interface-state row.
14. D026 is complete at the matched 64/16/16 gate. Directly supervising the
    identifiable net solver boundary exchange improves boundary and
    conserved-total errors at weights `0.01` and `0.1`, but worsens one-step,
    H20 state, and shock errors while leaving completion at 16/16. Treat it as
    a negative Pareto ablation; do not search more weights or run full seeds.
15. D027 is complete and partially positive. Cold stride 2 beats stride-1
    composition by 25.0%, 36.2%, and 48.6% in common-case H20/H50/H100 state
    error and finishes 16/16 at H100, despite mean truth effective-CFL maxima
    near 9.9. It misses only the direct frame-2 gate at 1.216 times composition.
    Preserve it as the large-step accuracy baseline, not a timestep-robust model.
16. D028 is complete and partial. Weight-only continuation fixes frame 2 and
    lowers final-target training floors by more than 40%, but H50 state and
    shock errors regress to 1.095 and 1.565 times cold. Do not run the
    conditional total-exposure control or claim continuation-sensitive
    optimization.
17. D029 is complete and partial. Neither frozen checkpoint passes native-grid
    zero-shot transfer: one-step error is 5.5 to 8.3 times its 256-cell value,
    and the 512-cell shock metric regresses. Cold stride 2 still beats stride 1
    at H50/H100 on both off-grid resolutions, with no lower common-case
    completion. Preserve the large-step result, but do not call the present
    residual FNO mesh invariant.
18. D030 is complete and partial. Exact cell-average restriction and stride-1/
    stride-2 update commutation pass at machine precision. The equal-presentation
    shared 128/256/512 residual FNO passes every same-grid-oracle gate and cuts
    frozen off-grid one-step error by 68.1%/78.3% at nx128/nx512 on common
    restriction-consistent truth. It does not match independently evolved
    native nx128/nx256 flow maps, so preserve the classification
    `shared_restriction_operator_without_native_solver_equivalence`; do not add
    cell width, timestep, or modes as a response to D029 alone.
19. The shared row still completes only 13/16 at H100. The same cases fail by
    negative pressure at similar calls on all three resolutions. Predeclare a
    post-fit tail-risk/admissibility stage on the strong residual baseline with
    an equal-update, equal-generated-exposure zero-penalty control. Change one
    constraint family at a time, keep inference raw, and do not repeat blanket
    Laplacian smoothing or the failed implicit-interface parameterization.
20. Evaluate every finalist with teacher-forced one-step, direct horizon, at
    least 100 raw autoregressive calls, timestep/resolution transfer, and a
    coarse-CFD comparison. Only after this gate should the method transfer to
    CPG geometry and a dynamic 2D shock benchmark.

## Agent Execution Rules

- Read `RESEARCH_DIRECTION_DECISION.md` before changing the trainer, target
  adapters, generator contract, or experiment matrix.
- Change one causal axis per primary comparison. A multi-component engineering
  prototype is not a substitute for the coordinate and supervision diagnostics.
- Record input/recurrent coordinates, predicted quantity, loss coordinates,
  normalization, timestep, effective CFL, geometry scaling, and all hidden
  floors or clamps in every run summary. Also record the comparison regime,
  canonical label semantics, closure error, initialization stride, stride
  curriculum, tiny-set fit result, and final failure classification when
  applicable.
- Predeclare closure and tiny-set pass thresholds, per-row diagnostic and
  optimizer budgets, data exposure, and the failure-repeat seed before a target
  batch starts. Do not infer gates from completed results.
- Do not conclude that a target family failed from rollout alone. Separate
  label/implementation failure, inability to fit, continuation-sensitive
  optimization, train/validation generalization, conditional closure ambiguity,
  and recurrent stability or distribution-shift failure.
- Preserve one primary inference path. Do not add independent state and flux
  heads that can disagree; compute derived states through the conservative
  update.
- New auxiliary labels require a documented source, identifiability argument,
  inference role, normalization, and ablation. Do not launch a broad dense-loss
  sweep by default.

## 1D Euler Experiment Todo

This section preserves experiment history. Unchecked historical items are not
automatically approved work; the current four-line program and accepted
research direction above take precedence.

Completed follow-up batch on AutoDL:

- [x] Analyze `euler1d_noise_followups_v2`. Use only `v2` output directories; the earlier non-v2 launch was stopped because it did not pass the intended `--seed` argument.
- [x] Check seed stability for `FNO + limited_residual + stride 4` at noise `0`, `0.003`, and `0.02` over seeds `20260707`, `20260708`, and `20260709`.
- [x] Test whether noise can replace the limiter: compare `FNO + residual + stride 4` at noise `0` and `0.02` against `limited_residual`.
- [x] Test whether denoising helps shorter-step accumulation: compare `FNO + limited_residual + stride 2` at noise `0`, `0.003`, and `0.02`.

Result summary: over three stride-4 seeds, noise `0.003` and `0.02` both improved final rollout L2 by about 12% relative to zero noise. Noise `0.003` is the safer default for the next selector because it gives the best mean rollout/conservation without pressure-floor hugging; noise `0.02` is a useful shock-stability stress setting because it sharply improves shock MAE but worsens one-step error and sits at the pressure floor. Stride 2 also needed noise: zero-noise stride 2 had raw-pressure violations, while noise `0.02` greatly improved final, conservation, and shock metrics.

Ignored compact analysis artifacts are under `artifacts/time_dependent_no/euler1d_noise_followups_v2_analysis_20260709/`.

Target-objective extensions to implement after the active batch:

- [x] Select one noise level from the active follow-up before expanding the target matrix: use `0.003` as the default nonzero-noise selector value, and keep `0.02` only as a shock-stability stress setting.
- [x] Factor a shared conservative admissibility limiter for decoded updates so residual, flux, and interface adapters can all keep updated density and pressure admissible. Flux/interface targets use samplewise scaling to preserve finite-volume face-pair accounting.
- [x] Add `limited_flux`: predict face fluxes, apply the finite-volume update, then limit the induced conservative update to keep updated density and pressure admissible.
- [x] Add `positive_limited_interface`: decode positive interface density/pressure, compute Rusanov fluxes, apply the finite-volume update, then limit the updated cell averages.
- [x] Keep training noise objective-independent: perturb only the current primitive input during training, keep the clean next state as the supervised target, and evaluate clean rollouts.
- [x] Log limiter diagnostics for all limited objectives: activation fraction, mean/min `theta`, pressure-floor cases, and whether the limiter preserved the intended conservation accounting.
- [x] Run the first extended target selector on `FNO` at stride 4 with only noise `0` and `0.003`: `limited_residual`, `limited_flux`, and `positive_limited_interface`. Result: only `limited_residual` remained viable; current absolute flux/interface targets failed under rollout.
- [ ] Fully seed-confirm only the best one or two stabilized target variants;
  losing variants receive the required predeclared diagnostic repeat, not a
  full seed sweep.
- [x] Completed the corrected `cpg_interface` CPGNet baseline and matched
  parameter controls. mp28 is the frozen strong reference; do not spend GPU on
  the deprecated CPG residual head or another depth sweep.
- [ ] Generate animations for the best and worst noise/limiter cases, especially trajectories where pressure reaches the limiter floor.

Flux-indeterminacy and Hodge-style follow-ups:

- [x] The ADER generator exports the solver's exact cumulative conservative face
  impulse and validates endpoint/boundary closure. The raw/gauge-canonical and
  direct/joint D021 screens are complete through the midscale stop. Do not scale
  the rejected face-value MSE objective or treat flux reconstructed only from
  state differences as a unique physical target.
- [x] Add a physical-flux-plus-correction objective before more exotic gauges: `F_pred = F_Rusanov(current) + delta_F`, with regularization/diagnostics on the learned correction rather than on the whole physical flux.
- [ ] For 2D only, test curl or loop-circulation regularization on the learned flux correction after mesh face orientations, areas, and cell volumes are validated.
- [ ] Treat scalar-potential flux prediction as an ablation rather than the first mainline method, because a gradient-only gauge may over-constrain shocks and multidimensional flow.
- [ ] Do not combine Hodge regularization with the broad noise/positivity target ladder until the stabilized FNO selector identifies at least one viable target family.


Physical flux-correction status:

- [x] Implemented `physical_flux_correction` as current-state Rusanov base flux plus bounded learned correction, decoded by the FV update and shared samplewise limiter.
- [x] Added live correction diagnostics: correction/bound mean, correction/bound max, and saturation fraction.
- [x] Ran zero-correction base audit on the real 1D dataset; stride-4 base Rusanov is a poor macro-step anchor and heavily activates the rollout limiter.
- [x] Finished the short correction-scale probe over scales `1`, `2`, and `4`; all variants were rejected, so do not run the full noise selector for this exact macro-step Rusanov-base target.
Engineering controls to consider after the selector:

- [x] Replace the mislabeled CPG residual head with the solver-level paper adaptation: geometry-only edge encoder, 12 directed flow layers, positive interface decoding, shared Rusanov flux, exact 1D geometry, physical ghosts, and 15+5 training.
- [x] Evaluate that baseline without a cell-state limiter or recurrence floor; mp12 completes 34/64, while the mp28 receptive-field control completes 64/64 with clean raw positivity.
- [ ] For FNO, keep fixed-step operators without `dt` parameterization and keep identity/zero-update initialization for residual-like heads; treat spectral smoothing or high-frequency penalties as later ablations, not part of the first target selector.

## Do Not Do

- Do not touch collaborator-owned checkpoint or preprocessing artifacts unless explicitly asked.
- Do not use the retired raw-HDF5 PCNO adapter path for model conclusions.
- Do not launch broad new training sweeps before ripple and shock-position
  diagnostics identify the mechanism to target. The old 1D target-family
  screen is closed; only the bounded four-line work packages in
  `RESEARCH_DIRECTION_DECISION.md` may be proposed for later authorization.
- Do not combine coordinate, target, dense-supervision, constraint, and
  architecture changes in one primary experiment.
- Do not impose blanket componentwise TVD or global spectral/variation damping
  on Euler without a smooth/shock-region contract.
- Do not make mesh-weighted conservation claims until cell/mesh weights are validated.
- Do not treat boundary leakage as evidence of model quality under clamped official rollout.
- Do not commit private paths, credentials, raw data, checkpoints, extracted rollout arrays, heavy figures, or local machine paths.
